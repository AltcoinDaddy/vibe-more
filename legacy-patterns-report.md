# Legacy Cadence Patterns Scan Report

Generated: 2025-10-07T20:31:37.547Z

## Executive Summary

Scanned 140 files and found 4703 legacy patterns in 38 files.

🚨 CRITICAL: 58 patterns that must be fixed immediately
⚠️  WARNING: 10 patterns that should be addressed
💡 SUGGESTION: 4635 patterns that could be improved

Priority: Fix critical patterns first, then warnings, then suggestions.

## Scan Statistics

- **Total Files Scanned**: 140
- **Files with Legacy Patterns**: 38
- **Total Patterns Found**: 4703

### Patterns by Type

| Pattern Type | Count |
|--------------|-------|
| pub-keyword | 3270 |
| storage-api | 504 |
| interface-conformance | 197 |
| function-signature | 717 |
| import-statement | 15 |

### Patterns by Severity

| Severity | Count | Priority |
|----------|-------|----------|
| suggestion | 4635 | 💡 LOW |
| warning | 10 | ⚠️ MEDIUM |
| critical | 58 | 🚨 HIGH |

## Detailed Findings (Grouped by Severity)

### 🚨 CRITICAL (58 patterns)

#### 1. Legacy pub keyword found

- **File**: lib/migration/config.ts
- **Location**: Line 17, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub to access(all)',
      category: 'access-modifier'
```

#### 2. Legacy pub(set) keyword found

- **File**: lib/migration/config.ts
- **Location**: Line 23, Column 29
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub(set) to access(all) with setter restrictions',
      category: 'access-modifier'
```

#### 3. Legacy account.save() found

- **File**: lib/migration/config.ts
- **Location**: Line 39, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
      replacement: 'account.storage.save(',
      description: 'Update account.save() to account.storage.save()',
      category: 'storage'
```

#### 4. Legacy account.borrow() found

- **File**: lib/migration/config.ts
- **Location**: Line 45, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.borrow()

**Context:**
```cadence
      replacement: 'account.capabilities.borrow(',
      description: 'Update account.borrow() to account.capabilities.borrow()',
      category: 'storage'
```

#### 5. Legacy pub keyword found

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 16, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 6. Legacy pub keyword found

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 20, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 7. Legacy pub keyword found

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 21, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 8. Legacy account.save() found

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 25, Column 11
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
          self.balance = balance
          account.save(<-create Vault(balance: balance), to: /storage/vault)
        }
```

#### 9. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 11, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```

#### 10. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 14, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 11. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 18, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```

#### 12. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 22, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    
    pub fun withdraw(): @Vault {
        return <-account.load<@Vault>(from: /storage/vault)!
```

#### 13. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 26, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 14. Legacy pub keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 64, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    console.log('---------------------------')
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```

#### 15. Legacy pub(set) keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 12, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
    
```

#### 16. Legacy pub(set) keyword found

- **File**: lib/migration/demo.ts
- **Location**: Line 65, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
    console.log('✓ Interface1, Interface2 → Interface1 & Interface2')
```

#### 17. Legacy account.save() found

- **File**: lib/migration/demo.ts
- **Location**: Line 19, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
    }
```

#### 18. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 289, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
  private getPatternName(pattern: LegacyPattern): string {
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 19. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 290, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 20. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 290, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 21. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 291, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 22. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 291, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 23. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 291, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 24. Legacy pub keyword found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 390, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 25. Legacy account.save() found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 396, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
    this.exampleDatabase.set('storage-api', {
      before: 'account.save(<-vault, to: /storage/vault)',
      after: 'account.storage.save(<-vault, to: /storage/vault)',
```

#### 26. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 18, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
   * Transform access modifiers from legacy to Cadence 1.0 syntax
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```

#### 27. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 20, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
   */
```

#### 28. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 28, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 29. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 37, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence

    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```

#### 30. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 38, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
    const pubPattern = /\bpub\s+(?=(?:var|let|fun|resource|struct|contract|interface|event))/g
```

#### 31. Legacy pub keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 42, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 32. Legacy pub(set) keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 19, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```

#### 33. Legacy pub(set) keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 28, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 34. Legacy pub(set) keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 29, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
    const pubSetPattern = /\bpub\(set\)\s+(?=(?:var|let|fun|resource|struct|contract|interface))/g
```

#### 35. Legacy pub(set) keyword found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 33, Column 38
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub(set) to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 36. Legacy account.save() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 103, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
   * Transform storage API calls to modern capability-based patterns
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```

#### 37. Legacy account.save() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 113, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence

    // Transform account.save() to account.storage.save()
    const savePattern = /\baccount\.save\b/g
```

#### 38. Legacy account.link() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 104, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.storage.issue() + account.capabilities.publish()

**Context:**
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```

#### 39. Legacy account.link() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 145, Column 14
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.storage.issue() + account.capabilities.publish()

**Context:**
```cadence

    // Note: account.link() transformation is more complex and may require
    // manual intervention as it involves capability patterns
```

#### 40. Legacy account.link() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 150, Column 31
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.storage.issue() + account.capabilities.publish()

**Context:**
```cadence
    if (linkMatches) {
      this.logger.warn('Found account.link() calls that require manual migration to capability patterns', {
        count: linkMatches.length
```

#### 41. Legacy account.borrow() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 105, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.borrow()

**Context:**
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
   */
```

#### 42. Legacy account.borrow() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 129, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.borrow()

**Context:**
```cadence

    // Transform account.borrow() to account.storage.borrow()
    const borrowPattern = /\baccount\.borrow\b/g
```

#### 43. Legacy pub keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 150, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 44. Legacy pub keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 165, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence

    // Check for pub keywords
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 45. Legacy pub keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 167, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      patterns.push('pub keyword usage')
    }
```

#### 46. Legacy pub(set) keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 151, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 47. Legacy pub(set) keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 170, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 48. Legacy pub(set) keyword found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 172, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      patterns.push('pub(set) keyword usage')
    }
```

#### 49. Legacy pub keyword found

- **File**: lib/migration/template-scanner.ts
- **Location**: Line 248, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 50. Legacy pub keyword found

- **File**: lib/migration/template-scanner.ts
- **Location**: Line 264, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 51. Legacy pub keyword found

- **File**: lib/migration/template-scanner.ts
- **Location**: Line 264, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 52. Legacy pub(set) keyword found

- **File**: lib/migration/template-scanner.ts
- **Location**: Line 249, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 53. Legacy pub keyword found

- **File**: lib/migration/validate-migrated-templates.ts
- **Location**: Line 165, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence

    // Check for pub keywords (should be replaced with access modifiers)
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 54. Legacy pub keyword found

- **File**: lib/migration/validate-migrated-templates.ts
- **Location**: Line 167, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) or appropriate access modifier

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      legacyPatterns.push('pub keyword usage')
    }
```

#### 55. Legacy pub(set) keyword found

- **File**: lib/migration/validate-migrated-templates.ts
- **Location**: Line 170, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 56. Legacy pub(set) keyword found

- **File**: lib/migration/validate-migrated-templates.ts
- **Location**: Line 172, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: high
- **Suggested Fix**: Replace with access(all) and implement setter restrictions

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      legacyPatterns.push('pub(set) keyword usage')
    }
```

#### 57. Legacy account.save() found

- **File**: lib/vibesdk.ts
- **Location**: Line 104, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.storage.save()

**Context:**
```cadence
- NEVER use "AuthAccount" - use modern account access patterns
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```

#### 58. Legacy account.link() found

- **File**: lib/vibesdk.ts
- **Location**: Line 105, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: high
- **Suggested Fix**: Replace with account.capabilities.storage.issue() + account.capabilities.publish()

**Context:**
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
- Use proper access modifiers: access(all), access(self), access(contract), access(account)
```

### ⚠️ WARNING (10 patterns)

#### 1. Legacy interface conformance syntax found

- **File**: components/ui/calendar.tsx
- **Location**: Line 158, Column 18
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
        },
        DayButton: CalendarDayButton,
        WeekNumber: ({ children, ...props }) => {
```

#### 2. Legacy interface conformance syntax found

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 20, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3. Legacy interface conformance syntax found

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 402, Column 30
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
    this.exampleDatabase.set('interface-conformance', {
      before: 'resource Vault: Provider, Receiver, Balance',
      after: 'resource Vault: Provider & Receiver & Balance',
```

#### 4. Legacy account.load() found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 121, Column 18
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: medium
- **Suggested Fix**: Replace with account.storage.load()

**Context:**
```cadence

    // Transform account.load() to account.storage.load()
    const loadPattern = /\baccount\.load\b/g
```

#### 5. Legacy interface conformance syntax found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 55, Column 16
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
   * Transform interface conformance from comma-separated to ampersand-separated
   * - Resource: Interface1, Interface2 -> Resource: Interface1 & Interface2
   */
```

#### 6. Legacy interface conformance syntax found

- **File**: lib/migration/syntax-transformer.ts
- **Location**: Line 64, Column 37
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
    // Pattern to match interface conformance with comma separation
    // Matches: resource/struct Name: Interface1, Interface2, Interface3
    const interfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+?)(\s*\{)/g
```

#### 7. Legacy interface conformance syntax found

- **File**: lib/migration/template-migrator.ts
- **Location**: Line 176, Column 45
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
    // Check for comma-separated interface conformance
    // Look for patterns like "resource Name: Interface1, Interface2 {" but exclude type annotations like "&{Interface}"
    const commaInterfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+,\s*[^{]+)(\s*\{)/
```

#### 8. Legacy interface conformance syntax found

- **File**: lib/vibesdk.ts
- **Location**: Line 109, Column 28
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
- Use modern storage API: account.storage.save(), account.capabilities.storage.issue()
- Import standard contracts: NonFungibleToken, FungibleToken, ViewResolver, MetadataViews
- Use view functions for read-only operations: access(all) view fun
```

#### 9. Legacy interface conformance syntax found

- **File**: lib/vibesdk.ts
- **Location**: Line 114, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
- Implement ALL views listed in getViews() in the resolveView() function
- Required views: Display, Serial, Traits, Royalties, ExternalURL, NFTCollectionData, NFTCollectionDisplay
- Never return nil for declared views - provide complete implementations
```

#### 10. Legacy interface conformance syntax found

- **File**: lib/vibesdk.ts
- **Location**: Line 124, Column 31
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
- **Impact**: medium
- **Suggested Fix**: Replace comma-separated interfaces with ampersand (&) syntax

**Context:**
```cadence
- Remove redundant createEmptyCollection from NFT resource
- Use structured storage paths: StoragePath, PublicPath, PrivatePath

```

### 💡 SUGGESTION (4635 patterns)

#### 1. Legacy pub keyword found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 244, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
describe('SyntaxTransformer', () => {
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 2. Legacy pub keyword found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 245, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3. Legacy pub keyword found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 264, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = migrator.migrateTemplate(originalTemplate)
    expect(result.code).not.toContain('pub ')
    expect(result.code).toContain('access(all)')
```

#### 4. Legacy pub keyword found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 276, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = await vibeSDK.generateCode({ prompt })
    expect(result).not.toContain('pub ')
    expect(result).toContain('access(all)')
```

#### 5. Legacy account.save() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 141, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Storage API Modernization
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```

#### 6. Legacy account.link() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 142, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```

#### 7. Legacy account.borrow() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 143, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
- Capability-based access pattern updates
```

#### 8. Legacy interface conformance syntax found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 136, Column 12
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Interface Conformance Updates
- `Resource: Interface1, Interface2` → `Resource: Interface1 & Interface2`
- Multiple interface inheritance syntax modernization
```

#### 9. Legacy interface conformance syntax found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 251, Column 34
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms interface conformance', () => {
    const input = 'resource Vault: Provider, Receiver'
    const expected = 'resource Vault: Provider & Receiver'
```

#### 10. Potential legacy function signature (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 245, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 11. Potential legacy function signature (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/design.md
- **Location**: Line 246, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
    expect(transformer.transformAccessModifiers(input)).toBe(expected)
```

#### 12. Legacy interface conformance syntax found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/requirements.md
- **Location**: Line 17, Column 114
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
2. WHEN processing function signatures THEN the system SHALL update parameter and return type syntax to Cadence 1.0 format
3. WHEN handling resource interfaces THEN the system SHALL update interface conformance syntax from `ResourceName: Interface1, Interface2` to `ResourceName: Interface1 & Interface2`
4. WHEN processing import statements THEN the system SHALL maintain compatibility with Flow blockchain standard contracts
```

#### 13. Legacy account.save() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/tasks.md
- **Location**: Line 25, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- [x] 2.3 Create storage API transformer
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```

#### 14. Legacy account.link() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/tasks.md
- **Location**: Line 26, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```

#### 15. Legacy account.borrow() found (in documentation context)

- **File**: .kiro/specs/cadence-syntax-migration/tasks.md
- **Location**: Line 27, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
  - Write unit tests for storage API transformations
```

#### 16. Potential legacy function signature

- **File**: app/playground/page.tsx
- **Location**: Line 16, Column 13
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 17. Potential legacy function signature

- **File**: app/playground/page.tsx
- **Location**: Line 183, Column 42
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 18. Potential legacy function signature

- **File**: app/playground/page.tsx
- **Location**: Line 197, Column 42
- **Pattern**: `fun main(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(): UInt64 {
    return getCurrentBlock().height
```

#### 19. Potential legacy function signature

- **File**: app/playground/page.tsx
- **Location**: Line 210, Column 42
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): String {
    let account = getAccount(address)
```

#### 20. Import statement with hardcoded address

- **File**: app/playground/page.tsx
- **Location**: Line 14, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Verify import uses current Flow standard contract addresses

**Context:**
```cadence
  const [script, setScript] = useState(`// Example: Query account balance
import FungibleToken from 0x9a0766d93b6608b7

```

#### 21. Import statement with hardcoded address

- **File**: app/playground/page.tsx
- **Location**: Line 21, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Verify import uses current Flow standard contract addresses

**Context:**
```cadence
  const [transaction, setTransaction] = useState(`// Example: Transfer tokens
import FungibleToken from 0x9a0766d93b6608b7

```

#### 22. Potential legacy function signature

- **File**: components/code-editor.tsx
- **Location**: Line 37, Column 22
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun hello(): String {
        return self.greeting
```

#### 23. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 45, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 24. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 47, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 244, Column 20
```

#### 25. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 56, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
describe('SyntaxTransformer', () => {
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 26. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 57, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```
```

#### 27. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 60, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 28. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 62, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 245, Column 20
```

#### 29. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 70, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 30. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 71, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 31. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 75, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 32. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 77, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 264, Column 40
```

#### 33. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 86, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = migrator.migrateTemplate(originalTemplate)
    expect(result.code).not.toContain('pub ')
    expect(result.code).toContain('access(all)')
```

#### 34. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 90, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 35. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 92, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 276, Column 35
```

#### 36. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 101, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = await vibeSDK.generateCode({ prompt })
    expect(result).not.toContain('pub ')
    expect(result).toContain('access(all)')
```

#### 37. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 190, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 38. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 191, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 39. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 205, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 40. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 263, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 41. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 265, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 104, Column 4
```

#### 42. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 274, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
|---------|---------------|----------|
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```

#### 43. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 278, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 44. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 280, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 109, Column 4
```

#### 45. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 289, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |

```

#### 46. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 303, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```

#### 47. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 335, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```
```

#### 48. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 342, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 49. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 344, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 25, Column 37
```

#### 50. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 353, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 51. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 357, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 52. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 359, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 44, Column 37
```

#### 53. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 368, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 54. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 372, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 55. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 374, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 60, Column 37
```

#### 56. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 383, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 57. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 387, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 58. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 389, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 76, Column 37
```

#### 59. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 398, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 60. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 402, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 61. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 404, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 92, Column 37
```

#### 62. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 413, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 63. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 417, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 64. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 419, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 108, Column 37
```

#### 65. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 428, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax anywhere in complex contract
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 66. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 432, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 67. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 434, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 159, Column 37
```

#### 68. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 443, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should also use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 69. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 447, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 70. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 449, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 169, Column 37
```

#### 71. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 458, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Default mock should use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 72. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 462, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 73. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 464, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 210, Column 9
```

#### 74. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 473, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 75. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 474, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 76. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 477, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 77. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 479, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 211, Column 11
```

#### 78. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 487, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 79. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 488, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          
```

#### 80. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 492, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 81. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 494, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 213, Column 11
```

#### 82. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 503, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 83. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 507, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 84. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 509, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 232, Column 11
```

#### 85. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 518, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub var legacyVar: String
          access(all) var modernVar: String
```

#### 86. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 522, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 87. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 524, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 254, Column 27
```

#### 88. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 533, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 89. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 533, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 90. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 537, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 91. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 539, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 254, Column 47
```

#### 92. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 548, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 93. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 548, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 94. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 552, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 95. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 554, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 268, Column 9
```

#### 96. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 563, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 97. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 564, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 98. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 567, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 99. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 569, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 269, Column 11
```

#### 100. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 577, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 101. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 578, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub fun getValue(): String
```

#### 102. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 579, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
```
```

#### 103. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 582, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 104. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 584, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 270, Column 11
```

#### 105. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 592, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub fun getValue(): String
```

#### 106. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 593, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 107. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 597, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 108. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 599, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 283, Column 9
```

#### 109. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 608, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 110. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 609, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 111. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 612, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 112. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 614, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 284, Column 11
```

#### 113. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 622, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 114. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 623, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub(set) var settableValue: String
```

#### 115. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 627, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 116. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 629, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 329, Column 46
```

#### 117. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 638, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Each template should use modern syntax
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 118. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 642, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 119. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 644, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 341, Column 46
```

#### 120. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 653, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      nftTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 121. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 657, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 122. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 659, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 351, Column 46
```

#### 123. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 668, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      tokenTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 124. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 672, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 125. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 674, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 360, Column 46
```

#### 126. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 683, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      marketplaceTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 127. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 687, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 128. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 689, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 369, Column 46
```

#### 129. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 698, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      daoTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 130. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 702, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub keyword found

```

#### 131. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 704, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 378, Column 46
```

#### 132. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 713, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      defiTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 133. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 717, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub keyword found

```

#### 134. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 719, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 387, Column 46
```

#### 135. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 728, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      utilityTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 136. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 732, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy pub keyword found

```

#### 137. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 734, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 398, Column 46
```

#### 138. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 743, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featuredTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 139. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 747, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy pub keyword found

```

#### 140. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 749, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 407, Column 48
```

#### 141. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 758, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(nftTemplate).toBeDefined()
      expect(nftTemplate!.code).not.toContain('pub ')
      expect(nftTemplate!.code).toContain('access(all)')
```

#### 142. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 762, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy pub keyword found

```

#### 143. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 764, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 417, Column 46
```

#### 144. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 773, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      searchResults.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 145. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 777, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy pub keyword found

```

#### 146. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 779, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 424, Column 60
```

#### 147. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 788, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      templates.forEach(template => {
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```

#### 148. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 789, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```
```

#### 149. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 792, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy pub keyword found

```

#### 150. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 794, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 425, Column 46
```

#### 151. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 802, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```

#### 152. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 803, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 153. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 807, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 32. 🚨 Legacy pub keyword found

```

#### 154. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 809, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 460, Column 37
```

#### 155. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 818, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result).toContain('access(all)')
      expect(result).not.toContain('pub ')
      
```

#### 156. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 822, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 33. 🚨 Legacy pub keyword found

```

#### 157. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 824, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 508, Column 27
```

#### 158. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 833, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 159. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 833, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 160. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 837, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 34. 🚨 Legacy pub keyword found

```

#### 161. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 839, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 508, Column 47
```

#### 162. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 848, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 163. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 848, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 164. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 852, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 35. 🚨 Legacy pub keyword found

```

#### 165. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 854, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 553, Column 41
```

#### 166. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 863, Column 41
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Streamed code should use modern syntax
        expect(fullCode).not.toContain('pub ')
        expect(fullCode).toContain('access(all)')
```

#### 167. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 867, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 36. 🚨 Legacy pub keyword found

```

#### 168. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 869, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 628, Column 42
```

#### 169. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 878, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Refined code should still use modern syntax
      expect(refinedCode).not.toContain('pub ')
      expect(refinedCode).toContain('access(all)')
```

#### 170. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 882, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 37. 🚨 Legacy pub keyword found

```

#### 171. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 884, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 698, Column 37
```

#### 172. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 893, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.length).toBeGreaterThan(0)
      expect(result).not.toContain('pub ')
    })
```

#### 173. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 907, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub(set) var settableValue: String
```

#### 174. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 953, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 175. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 982, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub fun getValue(): String
```

#### 176. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 983, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 177. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1006, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 178. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1008, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 12, Column 32
```

#### 179. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1017, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  return Promise.resolve({
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```

#### 180. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1018, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```
```

#### 181. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1021, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 182. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1023, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 13, Column 36
```

#### 183. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1031, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```

#### 184. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1032, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
    json: () => Promise.resolve({
```

#### 185. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1036, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 186. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1038, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 15, Column 37
```

#### 187. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1047, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    json: () => Promise.resolve({
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```

#### 188. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1048, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```
```

#### 189. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1051, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 190. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1053, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 16, Column 40
```

#### 191. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1061, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```

#### 192. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1062, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
      validation: {
```

#### 193. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1066, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 194. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1068, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 51
```

#### 195. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1077, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      validation: {
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```

#### 196. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1078, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```
```

#### 197. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1081, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 198. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1083, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 19, Column 42
```

#### 199. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1091, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```

#### 200. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1092, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```

#### 201. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1093, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```
```

#### 202. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1096, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 203. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1098, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 20, Column 74
```

#### 204. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1106, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```

#### 205. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1107, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
        ] : []
```

#### 206. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1111, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 207. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1113, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 29, Column 5
```

#### 208. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1122, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 209. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1123, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 210. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1126, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 211. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1128, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 30, Column 7
```

#### 212. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1136, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 213. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1137, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 214. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1141, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 215. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1143, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 50, Column 43
```

#### 216. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1152, Column 43
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        body: JSON.stringify({
          prompt: 'Create a contract with pub functions',
          context: legacyCode
```

#### 217. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1156, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 218. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1158, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 188, Column 35
```

#### 219. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1167, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          code: modernCode,
          refinementRequest: 'Add pub functions' // This would generate legacy syntax
        })
```

#### 220. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1171, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 221. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1173, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 243, Column 39
```

#### 222. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1182, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const streamData = [
        { type: 'code_chunk', chunk: 'pub contract Test {' },
        { type: 'validation_warning', message: 'Legacy syntax patterns detected' },
```

#### 223. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1186, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 224. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1188, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 283, Column 62
```

#### 225. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1197, Column 62
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const testCases = [
        { endpoint: '/api/generate', body: { prompt: 'create pub contract' } },
        { endpoint: '/api/explain', body: { code: legacyCode } },
```

#### 226. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1211, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 227. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1212, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 228. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1235, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 229. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1237, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 13, Column 5
```

#### 230. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1246, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 231. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1247, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 232. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1250, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 233. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1252, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 14, Column 7
```

#### 234. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1260, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 235. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1261, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 236. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1265, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 237. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1267, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 7
```

#### 238. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1276, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 239. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1277, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 240. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1280, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 241. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1282, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 19, Column 9
```

#### 242. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1290, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 243. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1291, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 244. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1295, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 245. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1297, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 98, Column 52
```

#### 246. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1306, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toBeDefined()
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.modernizedCode).toContain('access(all)')
```

#### 247. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1310, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 248. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1312, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 106, Column 9
```

#### 249. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1321, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 250. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1322, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```
```

#### 251. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1325, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 252. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1327, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 107, Column 11
```

#### 253. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1335, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 254. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1336, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
        }
```

#### 255. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1340, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 256. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1342, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 124, Column 52
```

#### 257. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1351, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toContain('access(all)')
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.transformationsApplied.length).toBeGreaterThan(0)
```

#### 258. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1355, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 259. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1357, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 196, Column 9
```

#### 260. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1366, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexLegacyCode = `
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 261. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1367, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```
```

#### 262. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1370, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 263. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1372, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 197, Column 11
```

#### 264. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1380, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 265. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1381, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 266. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1385, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 267. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1387, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 201, Column 11
```

#### 268. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1396, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub var publicVariable: String
          
```

#### 269. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1400, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 270. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1402, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 203, Column 11
```

#### 271. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1411, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 272. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1412, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```
```

#### 273. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1415, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 274. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1417, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 204, Column 13
```

#### 275. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1425, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 276. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1426, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```

#### 277. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1430, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 278. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1432, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 261, Column 30
```

#### 279. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1441, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle malformed code gracefully', async () => {
      const malformedCode = 'pub contract { invalid syntax }'
      
```

#### 280. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1445, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 281. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 273, Column 11
```

#### 282. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1456, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 283. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1470, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```

#### 284. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1486, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 285. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1487, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 286. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1501, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 287. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1502, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```
```

#### 288. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1516, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 289. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1517, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```
```

#### 290. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1530, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 291. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1531, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 292. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1560, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 293. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1561, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 294. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1591, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 295. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1614, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 296. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1616, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 34, Column 45
```

#### 297. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1625, Column 45
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub keyword', () => {
      const legacyCode = `
```

#### 298. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1629, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 299. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1631, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 36, Column 9
```

#### 300. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1640, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 301. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1641, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 302. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1644, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 303. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1646, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 37, Column 11
```

#### 304. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1654, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 305. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1655, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 306. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1659, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 307. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1661, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 39, Column 11
```

#### 308. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1670, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 309. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1674, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 310. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1676, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 137, Column 25
```

#### 311. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1685, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('containsLegacyPubKeywords', () => {
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 312. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1686, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```
```

#### 313. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1686, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```
```

#### 314. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1689, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 315. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1691, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 138, Column 28
```

#### 316. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1699, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 317. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1700, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 318. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1700, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 319. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1704, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 320. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1706, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 138, Column 48
```

#### 321. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1714, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 322. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1715, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 323. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1715, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 324. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1719, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 325. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1721, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 152, Column 29
```

#### 326. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1730, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should not detect pub in comments or strings', () => {
      const codeWithPubInString = `
```

#### 327. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1734, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 328. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1736, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 155, Column 38
```

#### 329. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1745, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```

#### 330. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1746, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```
```

#### 331. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1749, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 332. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1751, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 156, Column 68
```

#### 333. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1759, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```

#### 334. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1760, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
        }
```

#### 335. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1764, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 336. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1766, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 164, Column 35
```

#### 337. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1775, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```

#### 338. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1776, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```
```

#### 339. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1779, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 340. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1781, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 165, Column 27
```

#### 341. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1789, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```

#### 342. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1790, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
      const result = validator.shouldRejectCode(legacyCode)
```

#### 343. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1794, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 344. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1796, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 203, Column 9
```

#### 345. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1805, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 346. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1806, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 347. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1809, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 348. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1811, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 204, Column 11
```

#### 349. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1819, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 350. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1820, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 351. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1824, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 352. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1826, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 243, Column 9
```

#### 353. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1835, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 354. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1836, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 355. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1839, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 356. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1841, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 244, Column 11
```

#### 357. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1849, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 358. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1850, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 359. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1854, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 360. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1856, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 306, Column 9
```

#### 361. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1865, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 362. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1866, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 363. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1869, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 364. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1871, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 307, Column 11
```

#### 365. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1879, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 366. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1880, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 367. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1884, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 368. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1886, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 347, Column 9
```

#### 369. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1895, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithMultipleIssues = `
        pub contract TestContract {
          pub var value: String
```

#### 370. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1896, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 371. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1899, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 372. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1901, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 348, Column 11
```

#### 373. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1909, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 374. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1910, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          fun missingAccess(): String {
```

#### 375. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1914, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 376. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1916, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 418, Column 11
```

#### 377. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1925, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract {
          pub var incomplete
          fun missingBraces() {
```

#### 378. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1929, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 379. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1931, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 429, Column 25
```

#### 380. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1940, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle very long lines', () => {
      const longLine = 'pub var ' + 'a'.repeat(10000) + ': String'
      const result = validator.validateCode(longLine)
```

#### 381. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2120, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 382. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2149, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          fun missingAccess(): String {
```

#### 383. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2173, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 384. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2175, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 27, Column 12
```

#### 385. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2184, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    featured: false,
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```

#### 386. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2185, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```
```

#### 387. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2188, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 388. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2190, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 28, Column 5
```

#### 389. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2198, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```

#### 390. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2199, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64

```

#### 391. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2203, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 392. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2205, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 30, Column 5
```

#### 393. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2214, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```

#### 394. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2215, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```
```

#### 395. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2218, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 396. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2220, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 31, Column 5
```

#### 397. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2228, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```

#### 398. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2229, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```

#### 399. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2230, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```
```

#### 400. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2233, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 401. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2235, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 32, Column 5
```

#### 402. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2243, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```

#### 403. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2244, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)

```

#### 404. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2248, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 405. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2250, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 34, Column 5
```

#### 406. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2259, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub struct Proposal {
        pub let id: UInt64
```

#### 407. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2260, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub struct Proposal {
        pub let id: UInt64
```
```

#### 408. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2263, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 409. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2265, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 35, Column 9
```

#### 410. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2273, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub struct Proposal {
        pub let id: UInt64
```

#### 411. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2274, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub struct Proposal {
        pub let id: UInt64
        pub let title: String
```

#### 412. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2275, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let id: UInt64
        pub let title: String
```
```

#### 413. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2278, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 414. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2280, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 36, Column 9
```

#### 415. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2288, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let id: UInt64
        pub let title: String
```

#### 416. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2289, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let id: UInt64
        pub let title: String
        pub let description: String
```

#### 417. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2290, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let title: String
        pub let description: String
```
```

#### 418. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2293, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 419. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 37, Column 9
```

#### 420. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2303, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let title: String
        pub let description: String
```

#### 421. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2304, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let title: String
        pub let description: String
        pub let creator: Address
```

#### 422. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2305, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let description: String
        pub let creator: Address
```
```

#### 423. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2308, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 424. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2310, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 38, Column 9
```

#### 425. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2318, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let description: String
        pub let creator: Address
```

#### 426. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2319, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let description: String
        pub let creator: Address
        pub var votesFor: UInt64
```

#### 427. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2320, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
```
```

#### 428. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2323, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 429. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2325, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 39, Column 9
```

#### 430. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2333, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
```

#### 431. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2334, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```

#### 432. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2335, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```
```

#### 433. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2338, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 434. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2340, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 40, Column 9
```

#### 435. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2348, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```

#### 436. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2349, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
        pub var executed: Bool
```

#### 437. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2350, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
```
```

#### 438. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2353, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 439. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2355, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 41, Column 9
```

#### 440. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2363, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
```

#### 441. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2364, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
        pub let createdAt: UFix64
```

#### 442. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2365, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64
```
```

#### 443. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2368, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 444. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2370, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 42, Column 9
```

#### 445. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2378, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64
```

#### 446. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2379, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64

```

#### 447. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2383, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 448. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2385, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 55, Column 9
```

#### 449. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2394, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun addVote(support: Bool) {
            if support {
```

#### 450. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2398, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 451. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2400, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 63, Column 9
```

#### 452. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2409, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun execute() {
            self.executed = true
```

#### 453. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2413, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 454. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2415, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 71, Column 5
```

#### 455. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2424, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 456. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2428, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 457. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2430, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 85, Column 5
```

#### 458. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2439, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun vote(proposalId: UInt64, voter: Address, support: Bool) {
        pre {
```

#### 459. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2443, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 460. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2445, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 98, Column 5
```

#### 461. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2454, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 462. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2458, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 463. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2460, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 102, Column 5
```

#### 464. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2469, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getAllProposals(): [Proposal] {
        return self.proposals.values
```

#### 465. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2473, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 466. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2475, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 121, Column 30
```

#### 467. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2484, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Syntax Transformation', () => {
    it('should transform all pub keywords to access(all)', () => {
      const transformedCode = transformer.transformAccessModifiers(originalDAOTemplate.code)
```

#### 468. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2488, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 469. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2490, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 124, Column 33
```

#### 470. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2499, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should not contain any pub keywords followed by Cadence keywords
      expect(transformedCode).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 471. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2503, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 472. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2505, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 223, Column 68
```

#### 473. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2514, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(validationResult.errors.length).toBeGreaterThan(0)
      expect(validationResult.errors.some(error => error.includes('pub keyword usage'))).toBe(true)
    })
```

#### 474. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2518, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 475. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2520, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 232, Column 25
```

#### 476. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2529, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const hasLegacySyntaxErrors = validationResult.errors.some(error => 
        error.includes('pub keyword usage') || 
        error.includes('Legacy syntax found')
```

#### 477. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2544, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 478. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2559, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 479. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2567, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 480. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2569, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 37, Column 30
```

#### 481. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2578, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform all pub keywords to access(all)', () => {
    const template = getTemplateById('fungible-token')
```

#### 482. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2582, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 483. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2584, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 43, Column 22
```

#### 484. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2593, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check that no pub keywords remain
    expect(template.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 485. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2601, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 486. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2603, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 12, Column 19
```

#### 487. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2612, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Access Modifier Detection', () => {
    test('detects pub keyword in various contexts', () => {
      const code = `
```

#### 488. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2616, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 489. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2618, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 14, Column 9
```

#### 490. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2627, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 491. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2631, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 492. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2633, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 17, Column 9
```

#### 493. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2642, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64
        pub let name: String
```

#### 494. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2643, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        pub let name: String
```
```

#### 495. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2646, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 496. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2648, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 9
```

#### 497. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2656, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var balance: UFix64
        pub let name: String
```

#### 498. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2657, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        pub let name: String
      `;
```

#### 499. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2661, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 500. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2663, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 25, Column 49
```

#### 501. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2672, Column 49
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(pubPatterns).toHaveLength(3);
      expect(pubPatterns[0].originalText).toBe('pub fun ');
      expect(pubPatterns[0].modernReplacement).toBe('access(all) fun ');
```

#### 502. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2676, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 503. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2678, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 43, Column 3
```

#### 504. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2687, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 505. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2691, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 506. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2693, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 49, Column 65
```

#### 507. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2702, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const patterns = detector.detectAllLegacyPatterns(code);
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      
```

#### 508. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2706, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 509. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2708, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 113, Column 9
```

#### 510. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2717, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 511. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2721, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 512. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2723, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 138, Column 9
```

#### 513. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2732, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 514. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2736, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 515. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2738, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 140, Column 9
```

#### 516. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2747, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.link<&Vault>(/public/vault, target: /storage/vault)
        pub var balance: UFix64
      `;
```

#### 517. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2751, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 518. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2753, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 164, Column 9
```

#### 519. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2762, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 520. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2763, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```
```

#### 521. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2766, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 522. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2768, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 165, Column 11
```

#### 523. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2776, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 524. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2777, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 525. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2778, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```
```

#### 526. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2781, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 527. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2783, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 166, Column 11
```

#### 528. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2791, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 529. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2792, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
          
```

#### 530. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2796, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 531. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2798, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 183, Column 28
```

#### 532. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2807, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 533. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2811, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 534. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2813, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 185, Column 9
```

#### 535. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2822, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const highRiskCode = `
        pub fun test1() {}
        pub fun test2() {}
```

#### 536. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2823, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test1() {}
        pub fun test2() {}
```
```

#### 537. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2826, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 538. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2828, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 186, Column 9
```

#### 539. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2836, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test1() {}
        pub fun test2() {}
```

#### 540. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2837, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test1() {}
        pub fun test2() {}
        pub fun test3() {}
```

#### 541. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2838, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test2() {}
        pub fun test3() {}
```
```

#### 542. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2841, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 543. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2843, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 187, Column 9
```

#### 544. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2851, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test2() {}
        pub fun test3() {}
```

#### 545. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2852, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test2() {}
        pub fun test3() {}
        pub fun test4() {}
```

#### 546. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2853, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test3() {}
        pub fun test4() {}
```
```

#### 547. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2856, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 548. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2858, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 188, Column 9
```

#### 549. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2866, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test3() {}
        pub fun test4() {}
```

#### 550. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2867, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test3() {}
        pub fun test4() {}
        pub fun test5() {}
```

#### 551. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2868, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test4() {}
        pub fun test5() {}
```
```

#### 552. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2871, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 553. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2873, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 189, Column 9
```

#### 554. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2881, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test4() {}
        pub fun test5() {}
```

#### 555. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2882, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test4() {}
        pub fun test5() {}
        pub fun test6() {}
```

#### 556. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2883, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test5() {}
        pub fun test6() {}
```
```

#### 557. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2886, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 558. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2888, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 190, Column 9
```

#### 559. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2896, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test5() {}
        pub fun test6() {}
```

#### 560. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2897, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test5() {}
        pub fun test6() {}
        pub fun test7() {}
```

#### 561. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2898, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test6() {}
        pub fun test7() {}
```
```

#### 562. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2901, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 563. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2903, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 191, Column 9
```

#### 564. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2911, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test6() {}
        pub fun test7() {}
```

#### 565. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2912, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test6() {}
        pub fun test7() {}
        pub fun test8() {}
```

#### 566. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2913, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test7() {}
        pub fun test8() {}
```
```

#### 567. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2916, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 568. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2918, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 192, Column 9
```

#### 569. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2926, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test7() {}
        pub fun test8() {}
```

#### 570. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2927, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test7() {}
        pub fun test8() {}
        pub fun test9() {}
```

#### 571. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2928, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test8() {}
        pub fun test9() {}
```
```

#### 572. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2931, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 573. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2933, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 193, Column 9
```

#### 574. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2941, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test8() {}
        pub fun test9() {}
```

#### 575. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2942, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test8() {}
        pub fun test9() {}
        pub fun test10() {}
```

#### 576. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2943, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test9() {}
        pub fun test10() {}
```
```

#### 577. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2946, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 578. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2948, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 194, Column 9
```

#### 579. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2956, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test9() {}
        pub fun test10() {}
```

#### 580. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2957, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test9() {}
        pub fun test10() {}
        pub fun test11() {}
```

#### 581. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2958, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test10() {}
        pub fun test11() {}
```
```

#### 582. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2961, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub keyword found

```

#### 583. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2963, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 195, Column 9
```

#### 584. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2971, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test10() {}
        pub fun test11() {}
```

#### 585. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2972, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test10() {}
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```

#### 586. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2976, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub keyword found

```

#### 587. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2978, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 231, Column 9
```

#### 588. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2987, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 589. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2988, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 590. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2991, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy pub keyword found

```

#### 591. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2993, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 232, Column 11
```

#### 592. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3001, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 593. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3002, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 594. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3003, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```
```

#### 595. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3006, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy pub keyword found

```

#### 596. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3008, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 233, Column 13
```

#### 597. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3016, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 598. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3017, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```

#### 599. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3021, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy pub keyword found

```

#### 600. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3023, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 250, Column 1
```

#### 601. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3032, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 602. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3036, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy pub keyword found

```

#### 603. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3038, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 256, Column 65
```

#### 604. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3047, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      expect(pubPattern?.location.line).toBe(2);
```

#### 605. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3051, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy pub keyword found

```

#### 606. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3053, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 267, Column 36
```

#### 607. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3062, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        // This should not trigger pub detection
        let description = "This is a function"
```

#### 608. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3066, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 32. 🚨 Legacy pub keyword found

```

#### 609. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3068, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 283, Column 10
```

#### 610. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3077, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(1000).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test" }`
      ).join('\n');
```

#### 611. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3081, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 33. 🚨 Legacy pub keyword found

```

#### 612. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3083, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 290, Column 38
```

#### 613. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3092, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```

#### 614. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3093, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```
```

#### 615. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3096, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 34. 🚨 Legacy pub keyword found

```

#### 616. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3098, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 291, Column 71
```

#### 617. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3106, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```

#### 618. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3107, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
      expect(pubFunPatterns).toHaveLength(1000);
```

#### 619. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3181, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 620. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3211, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```

#### 621. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3226, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```

#### 622. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3241, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub fun test() {
  account.save()
```

#### 623. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3301, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 624. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3302, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 625. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3303, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```
```

#### 626. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3317, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 627. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3332, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 628. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3347, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 629. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3362, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 630. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3376, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 631. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3377, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 632. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3378, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```
```

#### 633. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3392, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 634. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3415, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 635. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3417, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 127, Column 9
```

#### 636. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3426, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const sampleContract = `
        pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 637. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3427, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 638. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3430, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 639. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3432, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 128, Column 11
```

#### 640. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3440, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 641. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3441, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 642. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3445, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 643. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 130, Column 11
```

#### 644. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3456, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 645. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3460, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 646. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3462, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 134, Column 11
```

#### 647. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3471, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun updateSupply(newSupply: UFix64) {
            self.totalSupply = newSupply
```

#### 648. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3475, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 649. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3477, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 155, Column 39
```

#### 650. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3486, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify syntax is modernized
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 651. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3490, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 652. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3492, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 161, Column 9
```

#### 653. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3501, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const resourceContract = `
        pub contract ResourceTest {
          pub resource Vault {
```

#### 654. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3502, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ResourceTest {
          pub resource Vault {
```
```

#### 655. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3505, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 656. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3507, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 162, Column 11
```

#### 657. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3515, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ResourceTest {
          pub resource Vault {
```

#### 658. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3516, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ResourceTest {
          pub resource Vault {
            pub var balance: UFix64
```

#### 659. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3517, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault {
            pub var balance: UFix64
```
```

#### 660. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3520, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 661. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3522, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 163, Column 13
```

#### 662. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3530, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault {
            pub var balance: UFix64
```

#### 663. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3531, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault {
            pub var balance: UFix64
            
```

#### 664. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3535, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 665. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3537, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 165, Column 13
```

#### 666. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3546, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              self.balance = self.balance + from.balance
```

#### 667. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3550, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 668. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3552, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 170, Column 13
```

#### 669. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3561, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              self.balance = self.balance - amount
```

#### 670. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3565, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 671. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3567, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 194, Column 39
```

#### 672. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3576, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 673. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3580, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 674. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3582, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 199, Column 9
```

#### 675. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3591, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const eventContract = `
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```

#### 676. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3592, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```
```

#### 677. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3595, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 678. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3597, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 200, Column 11
```

#### 679. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3605, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```

#### 680. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3606, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```

#### 681. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3607, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```
```

#### 682. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3610, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 683. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3612, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 201, Column 11
```

#### 684. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3620, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```

#### 685. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3621, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
          
```

#### 686. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3625, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 687. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3627, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 203, Column 11
```

#### 688. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3636, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64, to: Address?) {
            emit TokensDeposited(amount: amount, to: to)
```

#### 689. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3640, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 690. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3642, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 207, Column 11
```

#### 691. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3651, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(amount: UFix64, from: Address?) {
            emit TokensWithdrawn(amount: amount, from: from)
```

#### 692. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3655, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 693. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3657, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 224, Column 39
```

#### 694. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3666, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 695. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3670, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 696. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3672, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 229, Column 9
```

#### 697. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3681, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const interfaceContract = `
        pub contract InterfaceTest {
          pub resource interface Provider {
```

#### 698. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3682, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
```
```

#### 699. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3685, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 700. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3687, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 230, Column 11
```

#### 701. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3695, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
```

#### 702. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3696, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```

#### 703. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3697, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```
```

#### 704. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3700, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 705. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3702, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 231, Column 13
```

#### 706. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3710, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```

#### 707. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3711, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
          }
```

#### 708. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3715, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 709. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3717, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 234, Column 11
```

#### 710. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3726, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```

#### 711. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3727, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```
```

#### 712. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3730, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 713. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3732, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 235, Column 13
```

#### 714. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3740, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```

#### 715. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3741, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
          }
```

#### 716. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3745, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 717. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3747, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 238, Column 11
```

#### 718. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3756, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 719. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3757, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 720. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3760, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 721. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3762, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 239, Column 13
```

#### 722. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3770, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 723. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3771, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 724. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3775, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub keyword found

```

#### 725. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3777, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 241, Column 13
```

#### 726. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3786, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 727. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3790, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub keyword found

```

#### 728. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3792, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 245, Column 13
```

#### 729. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3801, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              destroy from
```

#### 730. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3805, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy pub keyword found

```

#### 731. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3807, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 269, Column 39
```

#### 732. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3816, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 733. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3820, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy pub keyword found

```

#### 734. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3822, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 289, Column 9
```

#### 735. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3831, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeContract = `
        pub contract LargeContract {
          ${Array.from({ length: 100 }, (_, i) => `
```

#### 736. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3835, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy pub keyword found

```

#### 737. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3837, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 291, Column 13
```

#### 738. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3846, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          ${Array.from({ length: 100 }, (_, i) => `
            pub var value${i}: UFix64
            
```

#### 739. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3850, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy pub keyword found

```

#### 740. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3852, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 293, Column 13
```

#### 741. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3861, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue${i}(): UFix64 {
              return self.value${i}
```

#### 742. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3865, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy pub keyword found

```

#### 743. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3867, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 297, Column 13
```

#### 744. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3876, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun setValue${i}(newValue: UFix64) {
              self.value${i} = newValue
```

#### 745. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3880, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 32. 🚨 Legacy pub keyword found

```

#### 746. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3882, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 322, Column 39
```

#### 747. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3891, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify transformation was applied
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 748. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3895, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 33. 🚨 Legacy pub keyword found

```

#### 749. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3897, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 368, Column 9
```

#### 750. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3906, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const malformedCode = `
        pub contract {
          pub var incomplete
```

#### 751. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3907, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract {
          pub var incomplete
```
```

#### 752. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3910, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 34. 🚨 Legacy pub keyword found

```

#### 753. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3912, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 369, Column 11
```

#### 754. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3920, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract {
          pub var incomplete
```

#### 755. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3921, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract {
          pub var incomplete
          pub fun missingBrace() {
```

#### 756. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3922, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var incomplete
          pub fun missingBrace() {
```
```

#### 757. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3925, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 35. 🚨 Legacy pub keyword found

```

#### 758. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3927, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 370, Column 11
```

#### 759. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3935, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var incomplete
          pub fun missingBrace() {
```

#### 760. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3936, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var incomplete
          pub fun missingBrace() {
        // Missing closing brace and other syntax errors
```

#### 761. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3940, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 36. 🚨 Legacy pub keyword found

```

#### 762. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3942, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 474, Column 11
```

#### 763. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3951, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `
          pub contract TestTemplate {
            pub var value: String
```

#### 764. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3952, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub contract TestTemplate {
            pub var value: String
```
```

#### 765. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3955, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 37. 🚨 Legacy pub keyword found

```

#### 766. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3957, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 475, Column 13
```

#### 767. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3965, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub contract TestTemplate {
            pub var value: String
```

#### 768. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3966, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub contract TestTemplate {
            pub var value: String
            
```

#### 769. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3970, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 38. 🚨 Legacy pub keyword found

```

#### 770. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3972, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 477, Column 13
```

#### 771. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3981, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 772. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3985, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 39. 🚨 Legacy pub keyword found

```

#### 773. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3987, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 503, Column 42
```

#### 774. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3996, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify code is transformed
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 775. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4011, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 776. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4012, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 777. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4026, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 778. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4041, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 779. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4049, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 780. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4051, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 28, Column 44
```

#### 781. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4060, Column 44
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Validate the template has been migrated
      expect(template.code).not.toContain('pub ')
      expect(template.code).toContain('access(all)')
```

#### 782. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4064, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 783. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4066, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 199, Column 60
```

#### 784. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4075, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyErrors = validationResult.errors.filter(error => 
        error.includes('Legacy syntax') || error.includes('pub keyword')
      )
```

#### 785. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4188, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 786. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4190, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 30, Column 14
```

#### 787. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4199, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featured: false,
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```

#### 788. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4200, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```
```

#### 789. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4203, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 790. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4205, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 31, Column 5
```

#### 791. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4213, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```

#### 792. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4214, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```

#### 793. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4215, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```
```

#### 794. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4218, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 795. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4220, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 32, Column 5
```

#### 796. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4228, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```

#### 797. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4229, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)

```

#### 798. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4233, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 799. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4235, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 34, Column 5
```

#### 800. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4244, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```

#### 801. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4245, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```
```

#### 802. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4248, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 803. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4250, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 35, Column 9
```

#### 804. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4258, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```

#### 805. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4259, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 806. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4260, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```
```

#### 807. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4263, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 808. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4265, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 36, Column 9
```

#### 809. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4273, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 810. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4274, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 811. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4275, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```
```

#### 812. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4278, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 813. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4280, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 37, Column 9
```

#### 814. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4288, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 815. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4289, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
    }
```

#### 816. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4293, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 817. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 40, Column 5
```

#### 818. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4304, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource SaleCollection: SalePublic {
        access(self) var forSale: {UInt64: UFix64}
```

#### 819. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4308, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 820. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4310, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 49, Column 9
```

#### 821. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4319, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun listForSale(tokenID: UInt64, price: UFix64) {
            self.forSale[tokenID] = price
```

#### 822. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4323, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 823. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4325, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 54, Column 9
```

#### 824. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4334, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault) {
            let price = self.forSale[tokenID]!
```

#### 825. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4338, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 826. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4340, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 66, Column 9
```

#### 827. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4349, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 828. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4353, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 829. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4355, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 70, Column 9
```

#### 830. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4364, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun getIDs(): [UInt64] {
            return self.forSale.keys
```

#### 831. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4368, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 832. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4370, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 75, Column 5
```

#### 833. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4379, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createSaleCollection(collection: &NFTCollection): @SaleCollection {
        return <- create SaleCollection(collection: collection)
```

#### 834. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4383, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 835. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4385, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 91, Column 26
```

#### 836. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4394, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform pub keywords to access(all)', () => {
    const migratedTemplate = migration.migrateNFTMarketplace(legacyTemplate)
```

#### 837. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4398, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 838. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4400, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 94, Column 27
```

#### 839. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4409, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Should not contain pub keywords
    expect(migratedTemplate.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface|event)/)
```

#### 840. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4413, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 841. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4415, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 104, Column 14
```

#### 842. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4424, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      ...legacyTemplate,
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 843. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4425, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```
```

#### 844. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4428, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 845. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4430, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 105, Column 9
```

#### 846. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4438, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 847. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4439, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 848. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4440, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```
```

#### 849. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4443, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 850. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4445, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 106, Column 11
```

#### 851. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4453, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 852. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4454, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
        }
```

#### 853. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4468, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 854. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4469, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 855. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4470, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```
```

#### 856. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4483, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 857. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4484, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 858. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4485, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```
```

#### 859. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4499, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 860. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4507, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 861. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4509, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 14, Column 9
```

#### 862. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4518, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 863. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4519, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```
```

#### 864. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4522, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 865. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4524, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 15, Column 11
```

#### 866. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4532, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 867. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4533, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 868. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4537, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 869. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4539, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 11
```

#### 870. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4548, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          }
          pub var balance: UFix64
        }
```

#### 871. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4552, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 872. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4554, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 31, Column 10
```

#### 873. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4563, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(500).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test${i}" }`
      ).join('\n');
```

#### 874. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4567, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 875. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4569, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 59, Column 9
```

#### 876. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4578, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 877. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4582, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 878. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4584, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 83, Column 29
```

#### 879. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4593, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 880. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4597, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 881. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4599, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 103, Column 9
```

#### 882. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4608, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 883. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4609, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```
```

#### 884. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4612, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 885. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4614, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 104, Column 9
```

#### 886. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4622, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 887. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4623, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```

#### 888. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4627, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 889. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4629, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 122, Column 9
```

#### 890. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4638, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 891. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4642, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 892. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4644, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 125, Column 9
```

#### 893. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4653, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64 = 0.0
      `;
```

#### 894. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4657, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 895. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4659, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 130, Column 52
```

#### 896. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4668, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```

#### 897. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4669, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```
```

#### 898. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4672, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 899. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4674, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 131, Column 52
```

#### 900. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4682, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```

#### 901. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4683, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 902. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4687, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 903. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4689, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 140, Column 9
```

#### 904. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4698, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub var balance: UFix64
```

#### 905. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4699, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var balance: UFix64
```
```

#### 906. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4702, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 907. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4704, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 141, Column 11
```

#### 908. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4712, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var balance: UFix64
```

#### 909. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4713, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var balance: UFix64
          
```

#### 910. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4717, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 911. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4719, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 143, Column 11
```

#### 912. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4728, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64) {
            self.balance = self.balance + amount
```

#### 913. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4732, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 914. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4734, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 164, Column 21
```

#### 915. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4743, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 916. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4747, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 917. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4749, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 187, Column 9
```

#### 918. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4758, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub fun complexFunction() {
          account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 919. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4762, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 920. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4764, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 198, Column 14
```

#### 921. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4773, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // The pub fun should be auto-fixed
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 922. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4777, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 923. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4779, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 214, Column 9
```

#### 924. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4788, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 925. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4792, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 926. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4794, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 253, Column 26
```

#### 927. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4803, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 928. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4807, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 929. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4809, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 283, Column 30
```

#### 930. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4818, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles malformed code gracefully', async () => {
      const malformedCode = 'pub fun { invalid syntax }';
      
```

#### 931. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4822, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 932. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4824, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 293, Column 30
```

#### 933. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4833, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles very large code inputs', async () => {
      const veryLargeCode = 'pub fun test() {}\n'.repeat(1000); // Reduced size for better performance
      
```

#### 934. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4837, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 935. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4839, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 305, Column 9
```

#### 936. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4848, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 937. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4849, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 938. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4852, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 939. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4854, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 306, Column 11
```

#### 940. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4862, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 941. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4863, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 942. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4864, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```
```

#### 943. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4867, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub keyword found

```

#### 944. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4869, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 307, Column 13
```

#### 945. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4877, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 946. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4878, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```

#### 947. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4882, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub keyword found

```

#### 948. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4884, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 326, Column 1
```

#### 949. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4893, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 950. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4922, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```

#### 951. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4952, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 952. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4967, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```

#### 953. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4982, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub fun test() {
  account.save()
```

#### 954. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4997, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 955. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4998, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 956. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4999, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```
```

#### 957. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5012, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 958. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5013, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 959. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5043, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 960. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5058, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 961. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5072, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 962. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5088, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 963. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5089, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```
```

#### 964. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5103, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 965. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5118, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 966. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5133, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 967. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5148, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 968. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5171, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 969. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5173, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 30, Column 42
```

#### 970. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5182, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify the template has been migrated
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```

#### 971. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5183, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```
```

#### 972. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5186, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 973. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5188, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 31, Column 42
```

#### 974. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5196, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```

#### 975. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5197, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```

#### 976. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5198, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```
```

#### 977. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5201, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 978. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5203, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 32, Column 42
```

#### 979. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5211, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```

#### 980. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5212, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```

#### 981. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5213, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```
```

#### 982. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5216, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 983. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5218, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 33, Column 42
```

#### 984. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5226, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```

#### 985. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5227, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```

#### 986. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5228, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```
```

#### 987. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5231, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 988. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5233, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 34, Column 42
```

#### 989. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5241, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```

#### 990. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5242, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```

#### 991. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5243, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```
```

#### 992. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5246, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 993. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5248, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 35, Column 42
```

#### 994. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5256, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```

#### 995. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5257, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
  })
```

#### 996. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5280, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 997. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5282, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 22
```

#### 998. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5291, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('transformAccessModifiers', () => {
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 999. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5292, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```
```

#### 1000. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5295, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1001. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5297, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 19, Column 22
```

#### 1002. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5305, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 1003. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5306, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1004. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5310, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1005. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5312, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 32, Column 31
```

#### 1006. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5321, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms multiple pub keywords', () => {
      const input = `
```

#### 1007. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5325, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1008. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5327, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 34, Column 9
```

#### 1009. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5336, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract {
          pub var totalSupply: UFix64
```

#### 1010. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5337, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
```
```

#### 1011. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5340, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1012. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5342, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 35, Column 11
```

#### 1013. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5350, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
```

#### 1014. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5351, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 1015. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5352, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```
```

#### 1016. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5355, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1017. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5357, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 36, Column 11
```

#### 1018. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5365, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 1019. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5366, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 1020. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5370, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 1021. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5372, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 53, Column 35
```

#### 1022. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5381, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 1023. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5385, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 1024. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5387, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 55, Column 9
```

#### 1025. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5396, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract Test {
          pub(set) var value: Int
```

#### 1026. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5400, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 1027. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5402, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 57, Column 11
```

#### 1028. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5411, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 1029. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5415, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 1030. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5417, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 82, Column 19
```

#### 1031. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5426, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub in comments and strings correctly', () => {
      const input = `
```

#### 1032. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5430, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 1033. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5432, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 84, Column 22
```

#### 1034. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5441, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        // This is a pub comment
        pub contract Test {
```

#### 1035. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5442, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This is a pub comment
        pub contract Test {
```
```

#### 1036. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5445, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 1037. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 85, Column 9
```

#### 1038. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5455, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        // This is a pub comment
        pub contract Test {
```

#### 1039. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5456, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This is a pub comment
        pub contract Test {
          pub fun test(): String {
```

#### 1040. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5457, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
```
```

#### 1041. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5460, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 1042. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5462, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 86, Column 11
```

#### 1043. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5470, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub fun test(): String {
```

#### 1044. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5471, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1045. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5472, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```
```

#### 1046. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5475, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 1047. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5477, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 87, Column 35
```

#### 1048. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5485, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1049. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5486, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
          }
```

#### 1050. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5490, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 1051. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5492, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 92, Column 22
```

#### 1052. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5501, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const expected = `
        // This is a pub comment
        access(all) contract Test {
```

#### 1053. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5505, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 1054. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5507, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 95, Column 35
```

#### 1055. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5516, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
          }
```

#### 1056. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5520, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy pub keyword found

```

#### 1057. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5522, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 109, Column 32
```

#### 1058. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5531, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles code without pub keywords', () => {
      const input = `
```

#### 1059. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5535, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy pub keyword found

```

#### 1060. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5537, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 318, Column 9
```

#### 1061. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5546, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1062. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5547, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```
```

#### 1063. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5550, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy pub keyword found

```

#### 1064. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5552, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 319, Column 11
```

#### 1065. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5560, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1066. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5561, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
          
```

#### 1067. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5565, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy pub keyword found

```

#### 1068. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5567, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 321, Column 11
```

#### 1069. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5576, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```

#### 1070. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5580, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy pub keyword found

```

#### 1071. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5582, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 325, Column 11
```

#### 1072. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5591, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(): @Vault {
            return <-account.load<@Vault>(from: /storage/vault)!
```

#### 1073. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5595, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy pub keyword found

```

#### 1074. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5597, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 333, Column 27
```

#### 1075. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5606, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should transform pub keywords
      expect(result).toContain('access(all) contract')
```

#### 1076. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5610, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub keyword found

```

#### 1077. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5612, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 349, Column 25
```

#### 1078. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5621, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates transformation statistics', () => {
      const original = `pub contract Test {
        pub var value: Int
```

#### 1079. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5622, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const original = `pub contract Test {
        pub var value: Int
```
```

#### 1080. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5625, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub keyword found

```

#### 1081. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5627, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 350, Column 9
```

#### 1082. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5635, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const original = `pub contract Test {
        pub var value: Int
```

#### 1083. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5636, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const original = `pub contract Test {
        pub var value: Int
      }`
```

#### 1084. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5681, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 1085. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5695, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub(set) var value: Int
```

#### 1086. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5697, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```
```

#### 1087. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5740, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```

#### 1088. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5816, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1089. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5817, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```
```

#### 1090. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5830, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 1091. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5831, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1092. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5845, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1093. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5860, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 1094. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5861, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 1095. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5891, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 1096. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5920, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub fun test(): String {
```

#### 1097. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5921, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1098. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5922, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```
```

#### 1099. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5937, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
```
```

#### 1100. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6079, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1101. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6081, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 174, Column 16
```

#### 1102. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6090, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 1103. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6091, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 1104. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6094, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1105. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6096, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 175, Column 11
```

#### 1106. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6104, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 1107. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6105, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 1108. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6109, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1109. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6111, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 177, Column 11
```

#### 1110. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6120, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1111. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6121, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 1112. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6124, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1113. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6126, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 178, Column 13
```

#### 1114. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6134, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1115. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6135, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 1116. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6139, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1117. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6141, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 180, Column 13
```

#### 1118. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6150, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 1119. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6154, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1120. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6156, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 189, Column 52
```

#### 1121. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6165, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(migratedTemplate.code).not.toContain('pub ')
      expect(migratedTemplate.code).toContain('access(all)')
```

#### 1122. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6169, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 1123. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6171, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 256, Column 16
```

#### 1124. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6180, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 1125. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6181, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 1126. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6184, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 1127. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6186, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 257, Column 11
```

#### 1128. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6194, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 1129. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6195, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
        }`
```

#### 1130. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6199, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 1131. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6201, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 265, Column 58
```

#### 1132. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6210, Column 58
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.errors.length).toBeGreaterThan(0)
      expect(result.errors.some(error => error.includes('pub keyword'))).toBe(true)
    })
```

#### 1133. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6225, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1134. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6226, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 1135. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6233, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1136. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6235, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 53, Column 9
```

#### 1137. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6244, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 1138. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6245, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 1139. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6248, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1140. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6250, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 54, Column 11
```

#### 1141. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6258, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 1142. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6259, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 1143. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6263, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1144. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6265, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 56, Column 11
```

#### 1145. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6274, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1146. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6278, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1147. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6280, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 87, Column 35
```

#### 1148. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6289, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectGeneratedCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 1149. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6290, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```
```

#### 1150. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6290, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```
```

#### 1151. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6293, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1152. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 88, Column 27
```

#### 1153. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6303, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 1154. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6304, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 1155. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6304, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 1156. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6308, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1157. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6310, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 88, Column 47
```

#### 1158. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6318, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 1159. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6319, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 1160. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6319, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 1161. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6323, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 1162. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6325, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 146, Column 42
```

#### 1163. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6334, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should use modern syntax
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 1164. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6338, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub keyword found

```

#### 1165. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6340, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 168, Column 9
```

#### 1166. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6349, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 1167. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6350, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 1168. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6353, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub keyword found

```

#### 1169. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6355, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 169, Column 11
```

#### 1170. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6363, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 1171. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6364, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 1172. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6368, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub keyword found

```

#### 1173. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6370, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 203, Column 9
```

#### 1174. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6379, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 1175. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6380, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 1176. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6383, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy pub keyword found

```

#### 1177. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6385, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 204, Column 11
```

#### 1178. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6393, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 1179. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6394, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 1180. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6398, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy pub keyword found

```

#### 1181. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6400, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 248, Column 35
```

#### 1182. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6409, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 1183. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6413, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy pub keyword found

```

#### 1184. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6415, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 258, Column 35
```

#### 1185. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6424, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 1186. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6428, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy pub keyword found

```

#### 1187. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6430, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 307, Column 9
```

#### 1188. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6439, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 1189. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6440, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 1190. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6443, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy pub keyword found

```

#### 1191. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6445, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 308, Column 11
```

#### 1192. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6453, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 1193. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6454, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 1194. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6458, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy pub keyword found

```

#### 1195. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6460, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 368, Column 37
```

#### 1196. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6469, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(validation.isValid).toBe(true)
        expect(code).not.toContain('pub ')
        expect(code).toContain('access(all)')
```

#### 1197. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6604, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1198. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6627, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1199. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6629, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 278, Column 24
```

#### 1200. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6638, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Add general modernization suggestions
    if (code.includes('pub ')) {
      suggestions.push('Replace all "pub" keywords with appropriate access modifiers like "access(all)"')
```

#### 1201. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6646, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1202. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6648, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 41, Column 28
```

#### 1203. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6657, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub keyword found',
      suggestedFix: 'Replace with access(all) or appropriate access modifier',
```

#### 1204. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6740, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1205. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6742, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 17, Column 29
```

#### 1206. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6751, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub to access(all)',
      category: 'access-modifier'
```

#### 1207. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6804, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1208. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6806, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 15, Column 5
```

#### 1209. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6815, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 1210. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6816, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```
```

#### 1211. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6819, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1212. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6821, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 16, Column 7
```

#### 1213. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6829, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 1214. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6830, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 1215. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6834, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1216. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6836, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 20, Column 7
```

#### 1217. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6845, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1218. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6846, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 1219. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6849, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1220. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6851, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 21, Column 9
```

#### 1221. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6859, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1222. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6860, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 1223. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6890, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1224. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6891, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 1225. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6904, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 1226. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6905, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 1227. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6928, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1228. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6930, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 10, Column 1
```

#### 1229. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6939, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1230. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6940, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```
```

#### 1231. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6943, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1232. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6945, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 11, Column 5
```

#### 1233. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6953, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1234. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6954, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```

#### 1235. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6958, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1236. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6960, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 14, Column 5
```

#### 1237. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6969, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 1238. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6973, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1239. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6975, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 5
```

#### 1240. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6984, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```

#### 1241. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6988, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1242. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6990, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 22, Column 5
```

#### 1243. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6999, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun withdraw(): @Vault {
        return <-account.load<@Vault>(from: /storage/vault)!
```

#### 1244. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7003, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1245. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7005, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 26, Column 5
```

#### 1246. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7014, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 1247. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7018, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 1248. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7020, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 64, Column 20
```

#### 1249. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7029, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('---------------------------')
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```

#### 1250. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7043, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```

#### 1251. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7058, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```

#### 1252. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7073, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```

#### 1253. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7089, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1254. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7090, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```
```

#### 1255. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7104, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 1256. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7119, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 1257. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7127, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1258. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7129, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 74, Column 31
```

#### 1259. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7138, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```

#### 1260. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7139, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```
```

#### 1261. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7142, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1262. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7144, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 75, Column 33
```

#### 1263. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7152, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```

#### 1264. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7153, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+fun/, 'access(all) fun'),
```

#### 1265. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7157, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1266. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7159, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 84, Column 31
```

#### 1267. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7168, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```

#### 1268. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7169, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```
```

#### 1269. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7172, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1270. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7174, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 85, Column 33
```

#### 1271. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7182, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```

#### 1272. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7183, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+/, 'access(all) '),
```

#### 1273. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7187, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1274. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7189, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 104, Column 31
```

#### 1275. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7198, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub struct/resource/contract" declaration found',
        suggestedFix: 'Replace "pub" with "access(all)"',
```

#### 1276. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7266, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1277. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7268, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 289, Column 39
```

#### 1278. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7277, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  private getPatternName(pattern: LegacyPattern): string {
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 1279. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7278, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```
```

#### 1280. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7278, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```
```

#### 1281. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7281, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1282. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7283, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 290, Column 39
```

#### 1283. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7291, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 1284. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7292, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1285. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7292, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1286. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7293, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1287. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7293, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1288. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7293, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1289. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7296, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1290. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7298, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 290, Column 82
```

#### 1291. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7306, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 1292. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7307, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1293. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7307, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1294. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7308, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1295. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7308, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1296. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7308, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 1297. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7311, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1298. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7313, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 291, Column 39
```

#### 1299. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7321, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1300. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7321, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1301. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7322, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1302. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7322, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1303. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7322, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1304. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7326, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1305. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7328, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 291, Column 85
```

#### 1306. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7336, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1307. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7336, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1308. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7337, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1309. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7337, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1310. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7337, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1311. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7341, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1312. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7343, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 291, Column 133
```

#### 1313. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7351, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1314. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7351, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 1315. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7352, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1316. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7352, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1317. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7352, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 1318. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7356, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub keyword found

```

#### 1319. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7358, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 390, Column 16
```

#### 1320. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7367, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 1321. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7412, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 1322. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7426, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 1323. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7465, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1324. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7467, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 18, Column 8
```

#### 1325. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7476, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform access modifiers from legacy to Cadence 1.0 syntax
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```

#### 1326. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7480, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1327. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7482, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 20, Column 22
```

#### 1328. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7491, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
   */
```

#### 1329. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7495, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1330. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7497, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 28, Column 57
```

#### 1331. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7506, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1332. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7510, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub keyword found

```

#### 1333. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7512, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 37, Column 29
```

#### 1334. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7521, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```

#### 1335. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7522, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```
```

#### 1336. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7525, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub keyword found

```

#### 1337. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7527, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 38, Column 24
```

#### 1338. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7535, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```

#### 1339. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7536, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
    const pubPattern = /\bpub\s+(?=(?:var|let|fun|resource|struct|contract|interface|event))/g
```

#### 1340. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7540, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub keyword found

```

#### 1341. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7542, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 42, Column 38
```

#### 1342. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7551, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 1343. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7565, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```

#### 1344. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7567, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```
```

#### 1345. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7581, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1346. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7595, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1347. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7769, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1348. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7771, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 150, Column 73
```

#### 1349. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7780, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 1350. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7784, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1351. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7786, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 165, Column 18
```

#### 1352. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7795, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub keywords
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 1353. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7799, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1354. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7801, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 167, Column 22
```

#### 1355. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7810, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      patterns.push('pub keyword usage')
    }
```

#### 1356. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7824, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 1357. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7878, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1358. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7880, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 248, Column 73
```

#### 1359. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7889, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 1360. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7893, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1361. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7895, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 264, Column 32
```

#### 1362. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7904, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 1363. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7904, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 1364. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7908, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub keyword found

```

#### 1365. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7910, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 264, Column 66
```

#### 1366. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7919, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 1367. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7919, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 1368. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7933, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 1369. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7942, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1370. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7944, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 165, Column 18
```

#### 1371. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7953, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub keywords (should be replaced with access modifiers)
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 1372. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7957, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub keyword found

```

#### 1373. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7959, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 167, Column 28
```

#### 1374. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7968, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      legacyPatterns.push('pub keyword usage')
    }
```

#### 1375. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8006, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy pub keyword found

```

#### 1376. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8008, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub `
- **Location**: Line 919, Column 31
```

#### 1377. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8017, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  // Check for legacy patterns in code (basic check)
  if (template.code.includes("pub ") || template.code.includes("pub(")) {
    issues.push("Template code contains legacy 'pub' keywords")
```

#### 1378. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 275, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```
```

#### 1379. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 293, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub(set) keyword found

```

#### 1380. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 295, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 105, Column 4
```

#### 1381. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 304, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
```

#### 1382. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 624, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub(set) var settableValue: String
```
```

#### 1383. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 897, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 38. 🚨 Legacy pub(set) keyword found

```

#### 1384. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 899, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 285, Column 11
```

#### 1385. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 908, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub(set) var settableValue: String
        }
```

#### 1386. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1944, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy pub(set) keyword found

```

#### 1387. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1946, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 52, Column 45
```

#### 1388. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1955, Column 45
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub(set) keyword', () => {
      const legacyCode = `
```

#### 1389. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1959, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. 🚨 Legacy pub(set) keyword found

```

#### 1390. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1961, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 55, Column 11
```

#### 1391. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1970, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub(set) var value: String
        }
```

#### 1392. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1974, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub(set) keyword found

```

#### 1393. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1976, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 142, Column 25
```

#### 1394. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1985, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```

#### 1395. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1986, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```
```

#### 1396. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1989, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub(set) keyword found

```

#### 1397. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1991, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 143, Column 59
```

#### 1398. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1999, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```

#### 1399. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2000, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPubSet)).toBe(true)
```

#### 1400. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3111, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 35. 🚨 Legacy pub(set) keyword found

```

#### 1401. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3113, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 30, Column 19
```

#### 1402. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3122, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```

#### 1403. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3123, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```
```

#### 1404. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3126, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 36. 🚨 Legacy pub(set) keyword found

```

#### 1405. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3128, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 31, Column 21
```

#### 1406. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3136, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```

#### 1407. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3137, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;

```

#### 1408. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5381, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 1409. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5397, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub(set) var value: Int
```
```

#### 1410. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5410, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 1411. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5640, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. 🚨 Legacy pub(set) keyword found

```

#### 1412. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5642, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 25, Column 22
```

#### 1413. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5651, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```

#### 1414. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5652, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```
```

#### 1415. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5655, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. 🚨 Legacy pub(set) keyword found

```

#### 1416. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5657, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 26, Column 22
```

#### 1417. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5665, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```

#### 1418. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5666, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
      const expected = 'access(all) var balance: UFix64'
```

#### 1419. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5670, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy pub(set) keyword found

```

#### 1420. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5672, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 53, Column 19
```

#### 1421. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5681, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 1422. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5685, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy pub(set) keyword found

```

#### 1423. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5687, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 56, Column 11
```

#### 1424. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5696, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 1425. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5890, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 1426. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6661, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub(set) keyword found

```

#### 1427. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6663, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 49, Column 28
```

#### 1428. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6672, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub(set) keyword found',
      suggestedFix: 'Replace with access(all) and implement setter restrictions',
```

#### 1429. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6755, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy pub(set) keyword found

```

#### 1430. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6757, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 23, Column 29
```

#### 1431. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6766, Column 29
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub(set) to access(all) with setter restrictions',
      category: 'access-modifier'
```

#### 1432. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6955, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```
```

#### 1433. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7030, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```
```

#### 1434. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7033, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub(set) keyword found

```

#### 1435. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7035, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 12, Column 5
```

#### 1436. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7044, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
    
```

#### 1437. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7048, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub(set) keyword found

```

#### 1438. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7050, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 65, Column 20
```

#### 1439. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7059, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
    console.log('✓ Interface1, Interface2 → Interface1 & Interface2')
```

#### 1440. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7477, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```
```

#### 1441. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7490, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```

#### 1442. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7506, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1443. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7507, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```
```

#### 1444. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7555, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy pub(set) keyword found

```

#### 1445. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7557, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 19, Column 8
```

#### 1446. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7566, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```

#### 1447. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7570, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy pub(set) keyword found

```

#### 1448. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7572, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 28, Column 18
```

#### 1449. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7581, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1450. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7582, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```
```

#### 1451. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7585, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. 🚨 Legacy pub(set) keyword found

```

#### 1452. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7587, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 29, Column 24
```

#### 1453. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7595, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 1454. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7596, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
    const pubSetPattern = /\bpub\(set\)\s+(?=(?:var|let|fun|resource|struct|contract|interface))/g
```

#### 1455. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7600, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy pub(set) keyword found

```

#### 1456. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7602, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 33, Column 38
```

#### 1457. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7611, Column 38
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub(set) to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 1458. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7781, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```
```

#### 1459. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7814, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub(set) keyword found

```

#### 1460. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7816, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 151, Column 30
```

#### 1461. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7825, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 1462. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7829, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy pub(set) keyword found

```

#### 1463. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7831, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 170, Column 18
```

#### 1464. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7840, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 1465. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7844, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy pub(set) keyword found

```

#### 1466. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7846, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 172, Column 22
```

#### 1467. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7855, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      patterns.push('pub(set) keyword usage')
    }
```

#### 1468. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7890, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```
```

#### 1469. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7923, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub(set) keyword found

```

#### 1470. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7925, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 249, Column 30
```

#### 1471. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7934, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 1472. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7972, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy pub(set) keyword found

```

#### 1473. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7974, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 170, Column 18
```

#### 1474. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7983, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 1475. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7987, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy pub(set) keyword found

```

#### 1476. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7989, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `pub(set) `
- **Location**: Line 172, Column 28
```

#### 1477. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7998, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      legacyPatterns.push('pub(set) keyword usage')
    }
```

#### 1478. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 105, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy account.save() found

```

#### 1479. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 107, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 141, Column 4
```

#### 1480. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 116, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Storage API Modernization
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```

#### 1481. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 130, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```

#### 1482. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 214, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy account.save() found

```

#### 1483. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 216, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 25, Column 43
```

#### 1484. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 225, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- [x] 2.3 Create storage API transformer
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```

#### 1485. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 239, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```

#### 1486. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 308, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy account.save() found

```

#### 1487. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 310, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 107, Column 4
```

#### 1488. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 319, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```

#### 1489. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 333, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```

#### 1490. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 912, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 39. 🚨 Legacy account.save() found

```

#### 1491. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 914, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 143, Column 39
```

#### 1492. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 923, Column 39
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(result).toContain('account.storage.save')
        expect(result).not.toContain('account.save(')
      }
```

#### 1493. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1427, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```
```

#### 1494. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1460, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy account.save() found

```

#### 1495. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1462, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 205, Column 15
```

#### 1496. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1471, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
              let ref = account.borrow<&TestResource>(from: /storage/test)
```

#### 1497. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2004, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy account.save() found

```

#### 1498. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2006, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 69, Column 13
```

#### 1499. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2015, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 1500. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2019, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy account.save() found

```

#### 1501. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2021, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 175, Column 13
```

#### 1502. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2030, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1503. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2034, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy account.save() found

```

#### 1504. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2036, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 207, Column 13
```

#### 1505. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2045, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 1506. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2049, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy account.save() found

```

#### 1507. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2051, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 247, Column 13
```

#### 1508. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2060, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1509. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2064, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy account.save() found

```

#### 1510. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2066, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 310, Column 13
```

#### 1511. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2075, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1512. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2718, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 1513. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2973, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```
```

#### 1514. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3018, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```
```

#### 1515. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3033, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
```
```

#### 1516. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3141, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 37. 🚨 Legacy account.save() found

```

#### 1517. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3143, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 59, Column 9
```

#### 1518. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3152, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        account.save(<-vault, to: /storage/vault)
        let vault <- account.load<@Vault>(from: /storage/vault)
```

#### 1519. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3156, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 38. 🚨 Legacy account.save() found

```

#### 1520. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3158, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 82, Column 21
```

#### 1521. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3167, Column 21
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('categorizes storage patterns correctly', () => {
      const code = `account.save(<-vault, to: /storage/vault)`;

```

#### 1522. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3171, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 39. 🚨 Legacy account.save() found

```

#### 1523. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3173, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 114, Column 9
```

#### 1524. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3182, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```

#### 1525. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3186, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 40. 🚨 Legacy account.save() found

```

#### 1526. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3188, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 169, Column 13
```

#### 1527. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3197, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 1528. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3201, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 41. 🚨 Legacy account.save() found

```

#### 1529. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3203, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 196, Column 9
```

#### 1530. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3212, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 1531. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3216, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 42. 🚨 Legacy account.save() found

```

#### 1532. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3218, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 234, Column 15
```

#### 1533. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3227, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
            }
```

#### 1534. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3231, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 43. 🚨 Legacy account.save() found

```

#### 1535. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3233, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 251, Column 3
```

#### 1536. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3242, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 1537. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3246, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 44. 🚨 Legacy account.save() found

```

#### 1538. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3248, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 260, Column 66
```

#### 1539. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3257, Column 66
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const savePattern = patterns.find(p => p.originalText === 'account.save(');
      expect(savePattern?.location.line).toBe(3);
```

#### 1540. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3286, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```

#### 1541. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3348, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 1542. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4624, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```
```

#### 1543. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4789, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 1544. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4879, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```
```

#### 1545. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4894, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
```
```

#### 1546. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4897, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. 🚨 Legacy account.save() found

```

#### 1547. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4899, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 62, Column 9
```

#### 1548. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4908, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 1549. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4912, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. 🚨 Legacy account.save() found

```

#### 1550. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4914, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 105, Column 9
```

#### 1551. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4923, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 1552. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4927, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy account.save() found

```

#### 1553. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4929, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 145, Column 13
```

#### 1554. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4938, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            self.balance = self.balance + amount
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 1555. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4942, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy account.save() found

```

#### 1556. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4944, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 215, Column 9
```

#### 1557. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4953, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 1558. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4957, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy account.save() found

```

#### 1559. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4959, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 308, Column 15
```

#### 1560. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4968, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
            }
```

#### 1561. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4972, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 32. 🚨 Legacy account.save() found

```

#### 1562. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4974, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 327, Column 3
```

#### 1563. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4983, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 1564. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5134, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 1565. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5577, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```
```

#### 1566. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5700, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. 🚨 Legacy account.save() found

```

#### 1567. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5702, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 256, Column 22
```

#### 1568. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5711, Column 22
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms account.save to account.storage.save', () => {
      const input = 'account.save(vault, to: /storage/vault)'
      const expected = 'account.storage.save(vault, to: /storage/vault)'
```

#### 1569. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5715, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. 🚨 Legacy account.save() found

```

#### 1570. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5717, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 285, Column 9
```

#### 1571. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5726, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        account.save(vault, to: /storage/vault)
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 1572. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5730, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. 🚨 Legacy account.save() found

```

#### 1573. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5732, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 322, Column 13
```

#### 1574. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5741, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
          }
```

#### 1575. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6473, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy account.save() found

```

#### 1576. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6475, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 99, Column 13
```

#### 1577. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6484, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1578. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6488, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. 🚨 Legacy account.save() found

```

#### 1579. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6490, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 172, Column 13
```

#### 1580. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6499, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1581. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6503, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. 🚨 Legacy account.save() found

```

#### 1582. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6505, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 207, Column 13
```

#### 1583. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6514, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 1584. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6518, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. 🚨 Legacy account.save() found

```

#### 1585. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6520, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 266, Column 35
```

#### 1586. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6529, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('account.capabilities')
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```

#### 1587. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6533, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. 🚨 Legacy account.save() found

```

#### 1588. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6535, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 311, Column 13
```

#### 1589. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6544, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 1590. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6558, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```

#### 1591. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6676, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy account.save() found

```

#### 1592. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6678, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 59, Column 28
```

#### 1593. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6687, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.save() found',
      suggestedFix: 'Replace with account.storage.save()',
```

#### 1594. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6770, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy account.save() found

```

#### 1595. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6772, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 39, Column 28
```

#### 1596. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6781, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'account.storage.save(',
      description: 'Update account.save() to account.storage.save()',
      category: 'storage'
```

#### 1597. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6864, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy account.save() found

```

#### 1598. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6866, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 25, Column 11
```

#### 1599. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6875, Column 11
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          self.balance = balance
          account.save(<-create Vault(balance: balance), to: /storage/vault)
        }
```

#### 1600. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6985, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```
```

#### 1601. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7063, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. 🚨 Legacy account.save() found

```

#### 1602. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7065, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 19, Column 9
```

#### 1603. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7074, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
    }
```

#### 1604. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7202, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy account.save() found

```

#### 1605. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7204, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 118, Column 30
```

#### 1606. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7213, Column 30
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.save() API found',
        suggestedFix: 'Replace with account.storage.save()',
```

#### 1607. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7371, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy account.save() found

```

#### 1608. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7373, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 396, Column 16
```

#### 1609. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7382, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('storage-api', {
      before: 'account.save(<-vault, to: /storage/vault)',
      after: 'account.storage.save(<-vault, to: /storage/vault)',
```

#### 1610. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7615, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. 🚨 Legacy account.save() found

```

#### 1611. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7617, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 103, Column 8
```

#### 1612. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7626, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform storage API calls to modern capability-based patterns
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```

#### 1613. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7630, Column 20
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. 🚨 Legacy account.save() found

```

#### 1614. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7632, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 113, Column 18
```

#### 1615. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7641, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.save() to account.storage.save()
    const savePattern = /\baccount\.save\b/g
```

#### 1616. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7655, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```

#### 1617. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8265, Column 19
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. 🚨 Legacy account.save() found

```

#### 1618. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8267, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.save(`
- **Location**: Line 104, Column 37
```

#### 1619. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8276, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use "AuthAccount" - use modern account access patterns
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```

#### 1620. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8290, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```

#### 1621. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 117, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```
```

#### 1622. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 120, Column 19
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. 🚨 Legacy account.link() found

```

#### 1623. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 122, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 142, Column 4
```

#### 1624. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 131, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```

#### 1625. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 145, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```

#### 1626. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 226, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```
```

#### 1627. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 229, Column 19
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy account.link() found

```

#### 1628. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 231, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 26, Column 16
```

#### 1629. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 240, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```

#### 1630. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 254, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```

#### 1631. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6530, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```
```

#### 1632. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6548, Column 20
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. 🚨 Legacy account.link() found

```

#### 1633. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6550, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 267, Column 35
```

#### 1634. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6559, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```

#### 1635. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6573, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```

#### 1636. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6691, Column 19
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy account.link() found

```

#### 1637. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6693, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 67, Column 28
```

#### 1638. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6702, Column 28
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.link() found',
      suggestedFix: 'Replace with account.capabilities.storage.issue() + account.capabilities.publish()',
```

#### 1639. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7217, Column 19
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy account.link() found

```

#### 1640. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7219, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 148, Column 30
```

#### 1641. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7228, Column 30
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.link() API found',
        suggestedFix: 'Replace with modern capability-based pattern',
```

#### 1642. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7627, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```
```

#### 1643. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7645, Column 20
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. 🚨 Legacy account.link() found

```

#### 1644. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7647, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 104, Column 8
```

#### 1645. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7656, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```

#### 1646. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7660, Column 20
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. 🚨 Legacy account.link() found

```

#### 1647. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7662, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 145, Column 14
```

#### 1648. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7671, Column 14
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Note: account.link() transformation is more complex and may require
    // manual intervention as it involves capability patterns
```

#### 1649. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7675, Column 20
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. 🚨 Legacy account.link() found

```

#### 1650. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7677, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 150, Column 31
```

#### 1651. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7686, Column 31
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (linkMatches) {
      this.logger.warn('Found account.link() calls that require manual migration to capability patterns', {
        count: linkMatches.length
```

#### 1652. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7700, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```

#### 1653. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8277, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```
```

#### 1654. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8280, Column 19
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. 🚨 Legacy account.link() found

```

#### 1655. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8282, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.link(`
- **Location**: Line 105, Column 13
```

#### 1656. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8291, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
- Use proper access modifiers: access(all), access(self), access(contract), access(account)
```

#### 1657. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 132, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```
```

#### 1658. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 135, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. 🚨 Legacy account.borrow() found

```

#### 1659. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 137, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 143, Column 4
```

#### 1660. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 146, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
- Capability-based access pattern updates
```

#### 1661. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 241, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```
```

#### 1662. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 244, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. 🚨 Legacy account.borrow() found

```

#### 1663. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 246, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 27, Column 13
```

#### 1664. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 255, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
  - Write unit tests for storage API transformations
```

#### 1665. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 288, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```

#### 1666. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 320, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```
```

#### 1667. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 323, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy account.borrow() found

```

#### 1668. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 325, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 108, Column 4
```

#### 1669. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 334, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```

#### 1670. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6560, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```
```

#### 1671. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6563, Column 20
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. 🚨 Legacy account.borrow() found

```

#### 1672. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6565, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 268, Column 35
```

#### 1673. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6574, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
    })
```

#### 1674. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6706, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. 🚨 Legacy account.borrow() found

```

#### 1675. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6708, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 75, Column 28
```

#### 1676. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6717, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.borrow() found',
      suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 1677. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6785, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. 🚨 Legacy account.borrow() found

```

#### 1678. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6787, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 45, Column 28
```

#### 1679. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6796, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'account.capabilities.borrow(',
      description: 'Update account.borrow() to account.capabilities.borrow()',
      category: 'storage'
```

#### 1680. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7232, Column 19
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. 🚨 Legacy account.borrow() found

```

#### 1681. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7234, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 138, Column 30
```

#### 1682. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7243, Column 30
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.borrow() API found',
        suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 1683. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7657, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```
```

#### 1684. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7690, Column 20
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. 🚨 Legacy account.borrow() found

```

#### 1685. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7692, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 105, Column 8
```

#### 1686. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7701, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
   */
```

#### 1687. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7705, Column 20
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. 🚨 Legacy account.borrow() found

```

#### 1688. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7707, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.borrow(`
- **Location**: Line 129, Column 18
```

#### 1689. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7716, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.borrow() to account.storage.borrow()
    const borrowPattern = /\baccount\.borrow\b/g
```

#### 1690. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6721, Column 19
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. ⚠️ Legacy account.load() found

```

#### 1691. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6723, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.load(`
- **Location**: Line 83, Column 28
```

#### 1692. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6732, Column 28
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'warning' as const,
      description: 'Legacy account.load() found',
      suggestedFix: 'Replace with account.storage.load()',
```

#### 1693. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7247, Column 19
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. ⚠️ Legacy account.load() found

```

#### 1694. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7249, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.load(`
- **Location**: Line 128, Column 30
```

#### 1695. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7258, Column 30
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.load() API found',
        suggestedFix: 'Replace with account.storage.load()',
```

#### 1696. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7720, Column 20
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. ⚠️ Legacy account.load() found

```

#### 1697. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7722, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `account.load(`
- **Location**: Line 121, Column 18
```

#### 1698. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7731, Column 18
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.load() to account.storage.load()
    const loadPattern = /\baccount\.load\b/g
```

#### 1699. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 152, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 136, Column 12
```

#### 1700. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 161, Column 12
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Interface Conformance Updates
- `Resource: Interface1, Interface2` → `Resource: Interface1 & Interface2`
- Multiple interface inheritance syntax modernization
```

#### 1701. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 167, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 251, Column 34
```

#### 1702. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 176, Column 34
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms interface conformance', () => {
    const input = 'resource Vault: Provider, Receiver'
    const expected = 'resource Vault: Provider & Receiver'
```

#### 1703. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1276, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1704. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1290, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1705. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1321, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 1706. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1335, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 1707. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1411, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 1708. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1425, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 1709. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1477, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 18, Column 25
```

#### 1710. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1486, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1711. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1492, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 106, Column 27
```

#### 1712. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1501, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 1713. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1507, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 203, Column 36
```

#### 1714. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1516, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 1715. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2081, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 86, Column 42
```

#### 1716. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2090, Column 42
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        access(all) resource TestResource: Interface1, Interface2 {
          // resource implementation
```

#### 1717. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2988, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 1718. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3002, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 1719. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3016, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 1720. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3183, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```
```

#### 1721. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3263, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 96, Column 23
```

#### 1722. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3272, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        resource Vault: Provider, Receiver, Balance {
          // implementation
```

#### 1723. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3278, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 115, Column 23
```

#### 1724. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3287, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
      `;
```

#### 1725. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3293, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 232, Column 29
```

#### 1726. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3302, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 1727. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3756, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1728. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3770, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1729. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4002, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 238, Column 29
```

#### 1730. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4011, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1731. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4425, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```
```

#### 1732. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4439, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 1733. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4453, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 1734. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4460, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 105, Column 34
```

#### 1735. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4469, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 1736. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4849, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 1737. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4863, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 1738. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4877, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 1739. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4989, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 306, Column 29
```

#### 1740. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4998, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 1741. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5546, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1742. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5560, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1743. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5747, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 125, Column 36
```

#### 1744. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5756, Column 36
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms comma-separated interfaces to ampersand-separated', () => {
      const input = 'resource Vault: Provider, Receiver {'
      const expected = 'resource Vault: Provider & Receiver {'
```

#### 1745. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5762, Column 17
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Fungible, Mintable`
- **Location**: Line 132, Column 34
```

#### 1746. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5771, Column 34
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms multiple interfaces with spaces', () => {
      const input = 'struct Token: Fungible, Mintable, Burnable {'
      const expected = 'struct Token: Fungible & Mintable & Burnable {'
```

#### 1747. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5777, Column 17
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: ContractInterface, AnotherInterface`
- **Location**: Line 139, Column 41
```

#### 1748. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5786, Column 41
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles contract interface conformance', () => {
      const input = 'contract MyContract: ContractInterface, AnotherInterface {'
      const expected = 'contract MyContract: ContractInterface & AnotherInterface {'
```

#### 1749. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5792, Column 17
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `:  Provider,  Receiver`
- **Location**: Line 152, Column 36
```

#### 1750. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5801, Column 36
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles interfaces with extra whitespace', () => {
      const input = 'resource Vault:  Provider,  Receiver,  Balance  {'
      const expected = 'resource Vault: Provider & Receiver & Balance {'
```

#### 1751. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5807, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 318, Column 32
```

#### 1752. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5816, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 1753. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6120, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1754. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6134, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1755. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6216, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 177, Column 29
```

#### 1756. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6225, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 1757. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6845, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1758. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6859, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1759. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6881, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 20, Column 25
```

#### 1760. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6890, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 1761. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6939, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1762. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6953, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1763. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7080, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 10, Column 29
```

#### 1764. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7089, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 1765. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7388, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Provider, Receiver`
- **Location**: Line 402, Column 30
```

#### 1766. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7397, Column 30
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('interface-conformance', {
      before: 'resource Vault: Provider, Receiver, Balance',
      after: 'resource Vault: Provider & Receiver & Balance',
```

#### 1767. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7737, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 55, Column 16
```

#### 1768. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7746, Column 16
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform interface conformance from comma-separated to ampersand-separated
   * - Resource: Interface1, Interface2 -> Resource: Interface1 & Interface2
   */
```

#### 1769. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7752, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 64, Column 37
```

#### 1770. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7761, Column 37
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Pattern to match interface conformance with comma separation
    // Matches: resource/struct Name: Interface1, Interface2, Interface3
    const interfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+?)(\s*\{)/g
```

#### 1771. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7861, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 176, Column 45
```

#### 1772. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7870, Column 45
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for comma-separated interface conformance
    // Look for patterns like "resource Name: Interface1, Interface2 {" but exclude type annotations like "&{Interface}"
    const commaInterfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+,\s*[^{]+)(\s*\{)/
```

#### 1773. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8297, Column 17
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Location**: Line 109, Column 28
```

#### 1774. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8306, Column 28
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Use modern storage API: account.storage.save(), account.capabilities.storage.issue()
- Import standard contracts: NonFungibleToken, FungibleToken, ViewResolver, MetadataViews
- Use view functions for read-only operations: access(all) view fun
```

#### 1775. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8312, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Display, Serial`
- **Location**: Line 114, Column 17
```

#### 1776. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8321, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Implement ALL views listed in getViews() in the resolveView() function
- Required views: Display, Serial, Traits, Royalties, ExternalURL, NFTCollectionData, NFTCollectionDisplay
- Never return nil for declared views - provide complete implementations
```

#### 1777. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8327, Column 17
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: StoragePath, PublicPath`
- **Location**: Line 124, Column 31
```

#### 1778. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8336, Column 31
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Remove redundant createEmptyCollection from NFT resource
- Use structured storage paths: StoragePath, PublicPath, PrivatePath

```

#### 1779. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8421, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: Interface1, Interface2`
- **Location**: Line 17, Column 114
```

#### 1780. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8430, Column 114
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
2. WHEN processing function signatures THEN the system SHALL update parameter and return type syntax to Cadence 1.0 format
3. WHEN handling resource interfaces THEN the system SHALL update interface conformance syntax from `ResourceName: Interface1, Interface2` to `ResourceName: Interface1 & Interface2`
4. WHEN processing import statements THEN the system SHALL maintain compatibility with Flow blockchain standard contracts
```

#### 1781. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8440, Column 17
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `: CalendarDayButton,
        WeekNumber`
```

#### 1782. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8450, Column 18
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        },
        DayButton: CalendarDayButton,
        WeekNumber: ({ children, ...props }) => {
```

#### 1783. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 57, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```
```

#### 1784. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 71, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 1785. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 72, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```
```

#### 1786. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 182, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 245, Column 24
```

#### 1787. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 191, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 1788. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 192, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```
```

#### 1789. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 197, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 246, Column 35
```

#### 1790. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 205, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 1791. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 206, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
    expect(transformer.transformAccessModifiers(input)).toBe(expected)
```

#### 1792. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 503, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1793. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 579, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
```
```

#### 1794. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 593, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 1795. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 929, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 192, Column 28
```

#### 1796. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 938, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 1797. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 944, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 213, Column 15
```

#### 1798. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 953, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1799. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 959, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 235, Column 28
```

#### 1800. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 968, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.modernVar
```

#### 1801. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 974, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 270, Column 15
```

#### 1802. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 983, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 1803. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 989, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 594, Column 28
```

#### 1804. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 998, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 1805. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1123, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 1806. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1137, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 1807. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1203, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 30, Column 11
```

#### 1808. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1212, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 1809. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1218, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 38, Column 19
```

#### 1810. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1227, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 1811. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1247, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 1812. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1261, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 1813. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1367, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```
```

#### 1814. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1381, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 1815. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1456, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 1816. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1522, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 14, Column 11
```

#### 1817. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1531, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 1818. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1537, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 30, Column 19
```

#### 1819. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1546, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 1820. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1552, Column 17
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun publicFunction(): S`
- **Location**: Line 197, Column 15
```

#### 1821. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1561, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 1822. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1567, Column 17
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun publicFunction(): S`
- **Location**: Line 228, Column 23
```

#### 1823. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1576, Column 23
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun publicFunction(): String {
            return "test"
```

#### 1824. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1582, Column 17
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun legacyFunction(): S`
- **Location**: Line 273, Column 15
```

#### 1825. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1591, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 1826. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1597, Column 17
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun modernFunction(): S`
- **Location**: Line 277, Column 23
```

#### 1827. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1606, Column 23
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun modernFunction(): String {
            return "modern"
```

#### 1828. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1670, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1829. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 1911, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          fun missingAccess(): String {
```
```

#### 1830. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2096, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 17, Column 23
```

#### 1831. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2105, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 1832. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2111, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 39, Column 15
```

#### 1833. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2120, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1834. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2126, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 281, Column 23
```

#### 1835. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2135, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 1836. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2141, Column 17
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun missingAccess(): S`
- **Location**: Line 349, Column 11
```

#### 1837. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2150, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          fun missingAccess(): String {
            return account.borrow<&String>(from: /storage/data)?.value ?? ""
```

#### 1838. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2424, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 1839. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2454, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 1840. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2535, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Location**: Line 71, Column 9
```

#### 1841. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2544, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 1842. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2550, Column 17
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getProposal(id: UInt64): P`
- **Location**: Line 98, Column 9
```

#### 1843. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2559, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 1844. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2627, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1845. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2687, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 1846. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2717, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1847. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2732, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 1848. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2763, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```
```

#### 1849. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2777, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 1850. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2791, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 1851. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2807, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 1852. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3181, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1853. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3308, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 14, Column 13
```

#### 1854. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3317, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1855. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3323, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 43, Column 7
```

#### 1856. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3332, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 1857. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3338, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 113, Column 13
```

#### 1858. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3347, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1859. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3353, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 138, Column 13
```

#### 1860. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3362, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 1861. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3368, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 165, Column 15
```

#### 1862. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3377, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 1863. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3383, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 183, Column 32
```

#### 1864. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3392, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 1865. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3398, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 217, Column 23
```

#### 1866. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3407, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun getValue(): String {
            return "modern"
```

#### 1867. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3456, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 1868. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 3981, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 1869. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4017, Column 17
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getTotalSupply(): U`
- **Location**: Line 130, Column 15
```

#### 1870. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4026, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 1871. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4032, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 477, Column 17
```

#### 1872. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4041, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 1873. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4081, Column 17
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getRequiredConfirmations(): U`
- **Location**: Line 174, Column 31
```

#### 1874. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4090, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getOwners(): [Address]')
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```

#### 1875. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4091, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```
```

#### 1876. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4096, Column 17
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getTransactionCount(): U`
- **Location**: Line 175, Column 31
```

#### 1877. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4104, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```

#### 1878. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4105, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```

#### 1879. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4106, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```
```

#### 1880. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4111, Column 17
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun isOwner(address: Address): B`
- **Location**: Line 176, Column 31
```

#### 1881. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4119, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```

#### 1882. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4120, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```

#### 1883. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4121, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```
```

#### 1884. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4126, Column 17
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Location**: Line 177, Column 31
```

#### 1885. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4134, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```

#### 1886. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4135, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```

#### 1887. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4136, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```
```

#### 1888. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4141, Column 17
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Location**: Line 178, Column 31
```

#### 1889. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4149, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```

#### 1890. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4150, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')

```

#### 1891. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4156, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Location**: Line 295, Column 31
```

#### 1892. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4165, Column 31
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Original core functions should still exist with same signatures
      expect(code).toContain('fun submitTransaction(to: Address, amount: UFix64): UInt64')
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
```

#### 1893. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4171, Column 17
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Location**: Line 297, Column 31
```

#### 1894. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4180, Column 31
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
      expect(code).toContain('fun getTransaction(txId: UInt64): Transaction?')

```

#### 1895. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4260, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```
```

#### 1896. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4274, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 1897. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4288, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 1898. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4349, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 1899. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4475, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 36, Column 13
```

#### 1900. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4484, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 1901. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4490, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 66, Column 13
```

#### 1902. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4499, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 1903. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4519, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```
```

#### 1904. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4533, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 1905. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4578, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1906. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4593, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 1907. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4594, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```
```

#### 1908. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4608, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 1909. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4622, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 1910. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4638, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1911. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4743, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 1912. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4788, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1913. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4803, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 1914. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 4952, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1915. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5004, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 15, Column 15
```

#### 1916. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5013, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 1917. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5019, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 43, Column 28
```

#### 1918. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5028, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) view fun getValue(): String {
            return "modern"
```

#### 1919. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5034, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 59, Column 13
```

#### 1920. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5043, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1921. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5049, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun test(): S`
- **Location**: Line 83, Column 33
```

#### 1922. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5058, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 1923. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5059, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```
```

#### 1924. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5064, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun test(): S`
- **Location**: Line 84, Column 43
```

#### 1925. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5072, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 1926. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5073, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';

```

#### 1927. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5079, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 103, Column 13
```

#### 1928. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5088, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 1929. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5094, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 122, Column 13
```

#### 1930. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5103, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 1931. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5109, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun test(): S`
- **Location**: Line 164, Column 25
```

#### 1932. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5118, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 1933. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5124, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 214, Column 13
```

#### 1934. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5133, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 1935. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5139, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 253, Column 30
```

#### 1936. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5148, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 1937. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5154, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 262, Column 9
```

#### 1938. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5163, Column 9
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This might be a function
        fun getValue(): String { return "test" }
      `;
```

#### 1939. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5263, Column 17
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun calculateReward(user: Address): U`
- **Location**: Line 73, Column 38
```

#### 1940. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5272, Column 38
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify core staking logic is preserved
    expect(template.code).toContain('fun calculateReward(user: Address): UFix64')
    expect(template.code).toContain('timeStaked = getCurrentBlock().timestamp - stakeInfo.stakedAt')
```

#### 1941. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5292, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```
```

#### 1942. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5306, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1943. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5307, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```
```

#### 1944. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5352, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```
```

#### 1945. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5366, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 1946. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5411, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 1947. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5457, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
```
```

#### 1948. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5471, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1949. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5485, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1950. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5515, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
```

#### 1951. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5697, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```
```

#### 1952. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5822, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 19, Column 26
```

#### 1953. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5831, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1954. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5832, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```
```

#### 1955. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5837, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 20, Column 37
```

#### 1956. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5845, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 1957. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5846, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
      const result = transformer.transformAccessModifiers(input)
```

#### 1958. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5852, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSupply(): U`
- **Location**: Line 36, Column 15
```

#### 1959. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5861, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 1960. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5867, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSupply(): U`
- **Location**: Line 44, Column 23
```

#### 1961. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5876, Column 23
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var totalSupply: UFix64
          access(all) fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 1962. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5882, Column 17
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): I`
- **Location**: Line 57, Column 15
```

#### 1963. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5891, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 1964. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5897, Column 17
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): I`
- **Location**: Line 63, Column 23
```

#### 1965. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5906, Column 23
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var value: Int
          access(all) fun getValue(): Int
        }
```

#### 1966. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5912, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun test(): S`
- **Location**: Line 86, Column 15
```

#### 1967. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5921, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 1968. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5927, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun test(): S`
- **Location**: Line 94, Column 23
```

#### 1969. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5936, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          access(all) fun test(): String {
            return "This contains pub in string"
```

#### 1970. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5942, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 187, Column 34
```

#### 1971. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5951, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to getter functions', () => {
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```

#### 1972. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5952, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```
```

#### 1973. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5957, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 188, Column 42
```

#### 1974. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5965, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```

#### 1975. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5966, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 1976. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5972, Column 17
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun readBalance(): U`
- **Location**: Line 194, Column 34
```

#### 1977. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5981, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to read functions', () => {
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```

#### 1978. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5982, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```
```

#### 1979. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5987, Column 17
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun readBalance(): U`
- **Location**: Line 195, Column 42
```

#### 1980. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5995, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```

#### 1981. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 5996, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 1982. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6002, Column 17
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun checkPermission(): B`
- **Location**: Line 201, Column 34
```

#### 1983. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6011, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to check functions', () => {
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```

#### 1984. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6012, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```
```

#### 1985. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6017, Column 17
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun checkPermission(): B`
- **Location**: Line 202, Column 42
```

#### 1986. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6025, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```

#### 1987. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6026, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 1988. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6032, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 208, Column 39
```

#### 1989. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6041, Column 39
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('preserves existing view modifier', () => {
      const input = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 1990. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6047, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 233, Column 21
```

#### 1991. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6056, Column 21
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        access(all) fun getValue(): String {
          return self.value
```

#### 1992. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6062, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getBalance(): U`
- **Location**: Line 241, Column 21
```

#### 1993. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6071, Column 21
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        
        access(all) fun getBalance(): UFix64 {
          return self.balance
```

#### 1994. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6274, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1995. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6580, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 34, Column 23
```

#### 1996. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6589, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 1997. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6595, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 56, Column 15
```

#### 1998. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6604, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 1999. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6610, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 278, Column 23
```

#### 2000. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6619, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 2001. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6816, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```
```

#### 2002. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6830, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 2003. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6896, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 16, Column 11
```

#### 2004. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6905, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 2005. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6911, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 34, Column 19
```

#### 2006. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6920, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract ModernContract {
      access(all) fun getValue(): String {
        return "modern"
```

#### 2007. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 6969, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 2008. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7014, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 2009. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7095, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSupply(): U`
- **Location**: Line 14, Column 9
```

#### 2010. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7104, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 2011. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7110, Column 17
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun checkBalance(): U`
- **Location**: Line 26, Column 9
```

#### 2012. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7119, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 2013. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7367, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 2014. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7368, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```
```

#### 2015. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7403, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 390, Column 20
```

#### 2016. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7412, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 2017. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7413, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```
```

#### 2018. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7418, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 391, Column 27
```

#### 2019. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7426, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 2020. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7427, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
      description: 'Replace "pub" with explicit access control'
```

#### 2021. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7433, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getBalance(): U`
- **Location**: Line 408, Column 28
```

#### 2022. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7442, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('function-signature', {
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```

#### 2023. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7443, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```
```

#### 2024. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7448, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getBalance(): U`
- **Location**: Line 409, Column 32
```

#### 2025. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7456, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```

#### 2026. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 7457, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
      description: 'Add view modifier for read-only functions'
```

#### 2027. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8023, Column 17
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun resolveView(_ view: Type): A`
- **Location**: Line 86, Column 21
```

#### 2028. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8032, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 2029. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8038, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 368, Column 26
```

#### 2030. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8047, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 2031. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8053, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Location**: Line 370, Column 26
```

#### 2032. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8062, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 2033. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8068, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 471, Column 26
```

#### 2034. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8077, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 2035. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8083, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Location**: Line 479, Column 26
```

#### 2036. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8092, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 2037. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8098, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Location**: Line 579, Column 17
```

#### 2038. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8107, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 2039. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8113, Column 17
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getProposal(id: UInt64): P`
- **Location**: Line 606, Column 22
```

#### 2040. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8122, Column 22
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 2041. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8128, Column 17
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun calculateReward(user: Address): U`
- **Location**: Line 699, Column 22
```

#### 2042. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8137, Column 22
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun calculateReward(user: Address): UFix64 {
        if self.stakes[user] == nil {
```

#### 2043. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8143, Column 17
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Location**: Line 710, Column 22
```

#### 2044. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8152, Column 22
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getStakeInfo(user: Address): StakeInfo? {
        return self.stakes[user]
```

#### 2045. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8158, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Location**: Line 776, Column 17
```

#### 2046. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8167, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) fun submitTransaction(to: Address, amount: UFix64): UInt64 {
        let tx = Transaction(id: self.transactionCount, to: to, amount: amount)
```

#### 2047. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8173, Column 17
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Location**: Line 810, Column 22
```

#### 2048. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8182, Column 22
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getTransaction(txId: UInt64): Transaction? {
        return self.transactions[txId]
```

#### 2049. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8188, Column 17
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getRequiredConfirmations(): U`
- **Location**: Line 818, Column 22
```

#### 2050. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8197, Column 22
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getRequiredConfirmations(): UInt64 {
        return self.required
```

#### 2051. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8203, Column 17
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getTransactionCount(): U`
- **Location**: Line 822, Column 22
```

#### 2052. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8212, Column 22
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getTransactionCount(): UInt64 {
        return self.transactionCount
```

#### 2053. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8218, Column 17
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun isOwner(address: Address): B`
- **Location**: Line 826, Column 22
```

#### 2054. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8227, Column 22
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun isOwner(address: Address): Bool {
        return self.owners.contains(address)
```

#### 2055. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8233, Column 17
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Location**: Line 830, Column 22
```

#### 2056. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8242, Column 22
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getConfirmationCount(txId: UInt64): UInt64 {
        if let tx = self.transactions[txId] {
```

#### 2057. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8248, Column 17
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Location**: Line 837, Column 22
```

#### 2058. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8257, Column 22
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun hasConfirmed(txId: UInt64, owner: Address): Bool {
        if let confirmationMap = self.confirmations[txId] {
```

#### 2059. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8342, Column 17
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun resolveView(_ view: Type): A`
- **Location**: Line 291, Column 21
```

#### 2060. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8351, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        
        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 2061. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8357, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValue(): S`
- **Location**: Line 569, Column 22
```

#### 2062. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8366, Column 22
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Public read-only functions
    access(all) view fun getValue(): String {
        return self.value
```

#### 2063. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8372, Column 17
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getCounter(): U`
- **Location**: Line 573, Column 22
```

#### 2064. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8381, Column 22
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    access(all) view fun getCounter(): UInt64 {
        return self.counter
```

#### 2065. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8387, Column 17
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getValueLength(): I`
- **Location**: Line 589, Column 22
```

#### 2066. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8396, Column 22
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Public utility functions
    access(all) view fun getValueLength(): Int {
        return self.value.length
```

#### 2067. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8402, Column 17
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun isCounterEven(): B`
- **Location**: Line 593, Column 22
```

#### 2068. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8411, Column 22
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    access(all) view fun isCounterEven(): Bool {
        return self.counter % 2 == 0
```

#### 2069. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8460, Column 17
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun main(address: Address): U`
- **Location**: Line 16, Column 13
```

#### 2070. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8469, Column 13
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 2071. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8475, Column 17
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun main(address: Address): U`
- **Location**: Line 183, Column 42
```

#### 2072. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8484, Column 42
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 2073. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8490, Column 17
- **Pattern**: `fun main(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun main(): U`
- **Location**: Line 197, Column 42
```

#### 2074. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8499, Column 42
- **Pattern**: `fun main(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(): UInt64 {
    return getCurrentBlock().height
```

#### 2075. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8505, Column 17
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun main(address: Address): S`
- **Location**: Line 210, Column 42
```

#### 2076. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8514, Column 42
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): String {
    let account = getAccount(address)
```

#### 2077. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8554, Column 17
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun hello(): S`
- **Location**: Line 37, Column 22
```

#### 2078. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8563, Column 22
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun hello(): String {
        return self.greeting
```

#### 2079. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8573, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 42, Column 26
```

#### 2080. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8582, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 2081. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8588, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Location**: Line 44, Column 26
```

#### 2082. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8597, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 2083. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8603, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Location**: Line 145, Column 26
```

#### 2084. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8612, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 2085. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8618, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Location**: Line 153, Column 26
```

#### 2086. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8627, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 2087. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2156, Column 17
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Location**: Line 375, Column 9
```

#### 2088. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 2165, Column 9
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithHardcodedAddress = `
        import NonFungibleToken from 0x1d7e57aa55817448
        
```

#### 2089. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8520, Column 17
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Location**: Line 14, Column 1
```

#### 2090. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8529, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const [script, setScript] = useState(`// Example: Query account balance
import FungibleToken from 0x9a0766d93b6608b7

```

#### 2091. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8535, Column 17
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Location**: Line 21, Column 1
```

#### 2092. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-by-file.md
- **Location**: Line 8544, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const [transaction, setTransaction] = useState(`// Example: Transfer tokens
import FungibleToken from 0x9a0766d93b6608b7

```

#### 2093. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 43, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 1. Legacy pub keyword found

```

#### 2094. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 47, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 244, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2095. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 55, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
describe('SyntaxTransformer', () => {
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 2096. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 56, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```
```

#### 2097. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 59, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 2. Legacy pub keyword found

```

#### 2098. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 63, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 245, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2099. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 70, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 2100. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 71, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 2101. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 75, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 3. Legacy pub keyword found

```

#### 2102. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 79, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 264, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2103. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 87, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = migrator.migrateTemplate(originalTemplate)
    expect(result.code).not.toContain('pub ')
    expect(result.code).toContain('access(all)')
```

#### 2104. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 91, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 4. Legacy pub keyword found

```

#### 2105. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 95, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 276, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2106. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 103, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const result = await vibeSDK.generateCode({ prompt })
    expect(result).not.toContain('pub ')
    expect(result).toContain('access(all)')
```

#### 2107. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 203, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 11. Legacy pub keyword found

```

#### 2108. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 207, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2109. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 215, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
|---------|---------------|----------|
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```

#### 2110. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 219, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 12. Legacy pub keyword found

```

#### 2111. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 223, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 109, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2112. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 231, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |

```

#### 2113. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 246, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```

#### 2114. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 280, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```
```

#### 2115. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 283, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 16. Legacy pub keyword found

```

#### 2116. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 287, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 25, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2117. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 295, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2118. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 299, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 17. Legacy pub keyword found

```

#### 2119. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 303, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 44, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2120. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 311, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2121. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 315, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 18. Legacy pub keyword found

```

#### 2122. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 319, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 60, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2123. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 327, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2124. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 331, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 19. Legacy pub keyword found

```

#### 2125. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 335, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 76, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2126. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 343, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2127. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 347, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 20. Legacy pub keyword found

```

#### 2128. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 351, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 92, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2129. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 359, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2130. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 363, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. Legacy pub keyword found

```

#### 2131. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 367, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 108, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2132. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 375, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax anywhere in complex contract
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 2133. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 379, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 22. Legacy pub keyword found

```

#### 2134. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 383, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 159, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2135. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 391, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should also use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 2136. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 395, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 23. Legacy pub keyword found

```

#### 2137. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 399, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 169, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2138. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 407, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Default mock should use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 2139. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 411, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. Legacy pub keyword found

```

#### 2140. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 415, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 210, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2141. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 423, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 2142. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 424, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 2143. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 427, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 25. Legacy pub keyword found

```

#### 2144. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 431, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 211, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2145. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 438, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 2146. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 439, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          
```

#### 2147. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 443, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. Legacy pub keyword found

```

#### 2148. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 213, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2149. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 455, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 2150. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 459, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 27. Legacy pub keyword found

```

#### 2151. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 463, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 232, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2152. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 471, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub var legacyVar: String
          access(all) var modernVar: String
```

#### 2153. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 475, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 28. Legacy pub keyword found

```

#### 2154. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 479, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 254, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2155. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 487, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 2156. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 487, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 2157. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 491, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 29. Legacy pub keyword found

```

#### 2158. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 495, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 254, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2159. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 503, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 2160. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 503, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 2161. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 507, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 30. Legacy pub keyword found

```

#### 2162. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 511, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 268, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2163. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 519, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 2164. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 520, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 2165. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 523, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 31. Legacy pub keyword found

```

#### 2166. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 527, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 269, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2167. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 534, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 2168. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 535, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub fun getValue(): String
```

#### 2169. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 536, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
```
```

#### 2170. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 539, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 32. Legacy pub keyword found

```

#### 2171. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 543, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 270, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2172. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 550, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub fun getValue(): String
```

#### 2173. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 551, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 2174. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 555, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 33. Legacy pub keyword found

```

#### 2175. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 559, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 283, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2176. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 567, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 2177. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 568, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 2178. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 571, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 34. Legacy pub keyword found

```

#### 2179. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 575, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 284, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2180. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 582, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 2181. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 583, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub(set) var settableValue: String
```

#### 2182. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 587, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 35. Legacy pub keyword found

```

#### 2183. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 591, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 329, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2184. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 599, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Each template should use modern syntax
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 2185. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 603, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 36. Legacy pub keyword found

```

#### 2186. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 607, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 341, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2187. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 615, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      nftTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2188. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 619, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 37. Legacy pub keyword found

```

#### 2189. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 623, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 351, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2190. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 631, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      tokenTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2191. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 635, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 38. Legacy pub keyword found

```

#### 2192. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 639, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 360, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2193. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 647, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      marketplaceTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2194. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 651, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 39. Legacy pub keyword found

```

#### 2195. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 655, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 369, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2196. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 663, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      daoTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2197. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 667, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 40. Legacy pub keyword found

```

#### 2198. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 671, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 378, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2199. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 679, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      defiTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2200. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 683, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 41. Legacy pub keyword found

```

#### 2201. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 687, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 387, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2202. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 695, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      utilityTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2203. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 699, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 42. Legacy pub keyword found

```

#### 2204. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 703, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 398, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2205. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 711, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featuredTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2206. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 715, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 43. Legacy pub keyword found

```

#### 2207. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 719, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 407, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2208. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 727, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(nftTemplate).toBeDefined()
      expect(nftTemplate!.code).not.toContain('pub ')
      expect(nftTemplate!.code).toContain('access(all)')
```

#### 2209. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 731, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 44. Legacy pub keyword found

```

#### 2210. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 735, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 417, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2211. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 743, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      searchResults.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 2212. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 747, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 45. Legacy pub keyword found

```

#### 2213. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 751, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 424, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2214. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 759, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      templates.forEach(template => {
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```

#### 2215. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 760, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```
```

#### 2216. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 763, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 46. Legacy pub keyword found

```

#### 2217. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 767, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 425, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2218. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 774, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```

#### 2219. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 775, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 2220. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 779, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 47. Legacy pub keyword found

```

#### 2221. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 783, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 460, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2222. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 791, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result).toContain('access(all)')
      expect(result).not.toContain('pub ')
      
```

#### 2223. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 795, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 48. Legacy pub keyword found

```

#### 2224. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 799, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 508, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2225. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 807, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 2226. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 807, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 2227. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 811, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 49. Legacy pub keyword found

```

#### 2228. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 815, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 508, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2229. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 823, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 2230. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 823, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 2231. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 827, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 50. Legacy pub keyword found

```

#### 2232. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 831, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 553, Column 41
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2233. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 839, Column 41
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Streamed code should use modern syntax
        expect(fullCode).not.toContain('pub ')
        expect(fullCode).toContain('access(all)')
```

#### 2234. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 843, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 51. Legacy pub keyword found

```

#### 2235. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 847, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 628, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2236. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 855, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Refined code should still use modern syntax
      expect(refinedCode).not.toContain('pub ')
      expect(refinedCode).toContain('access(all)')
```

#### 2237. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 859, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 52. Legacy pub keyword found

```

#### 2238. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 863, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 698, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2239. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 871, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.length).toBeGreaterThan(0)
      expect(result).not.toContain('pub ')
    })
```

#### 2240. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 886, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub(set) var settableValue: String
```

#### 2241. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 907, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 55. Legacy pub keyword found

```

#### 2242. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 911, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 12, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2243. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 919, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  return Promise.resolve({
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```

#### 2244. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 920, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```
```

#### 2245. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 923, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 56. Legacy pub keyword found

```

#### 2246. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 927, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 13, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2247. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 934, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```

#### 2248. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 935, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
    json: () => Promise.resolve({
```

#### 2249. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 939, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 57. Legacy pub keyword found

```

#### 2250. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 943, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 15, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2251. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 951, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    json: () => Promise.resolve({
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```

#### 2252. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 952, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```
```

#### 2253. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 955, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 58. Legacy pub keyword found

```

#### 2254. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 959, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 16, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2255. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 966, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```

#### 2256. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 967, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
      validation: {
```

#### 2257. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 971, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 59. Legacy pub keyword found

```

#### 2258. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 975, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2259. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 983, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      validation: {
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```

#### 2260. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 984, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```
```

#### 2261. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 987, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 60. Legacy pub keyword found

```

#### 2262. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 991, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2263. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 998, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```

#### 2264. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 999, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```

#### 2265. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1000, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```
```

#### 2266. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1003, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 61. Legacy pub keyword found

```

#### 2267. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1007, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 20, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2268. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1014, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```

#### 2269. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1015, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
        ] : []
```

#### 2270. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1019, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 62. Legacy pub keyword found

```

#### 2271. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1023, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 29, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2272. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1031, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 2273. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1032, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 2274. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1035, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 63. Legacy pub keyword found

```

#### 2275. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1039, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2276. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1046, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 2277. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1047, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 2278. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1051, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 64. Legacy pub keyword found

```

#### 2279. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1055, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 50, Column 43
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2280. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1063, Column 43
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        body: JSON.stringify({
          prompt: 'Create a contract with pub functions',
          context: legacyCode
```

#### 2281. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1067, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 65. Legacy pub keyword found

```

#### 2282. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1071, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 188, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2283. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1079, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          code: modernCode,
          refinementRequest: 'Add pub functions' // This would generate legacy syntax
        })
```

#### 2284. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1083, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 66. Legacy pub keyword found

```

#### 2285. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1087, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 243, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2286. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1095, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const streamData = [
        { type: 'code_chunk', chunk: 'pub contract Test {' },
        { type: 'validation_warning', message: 'Legacy syntax patterns detected' },
```

#### 2287. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1099, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 67. Legacy pub keyword found

```

#### 2288. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1103, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 283, Column 62
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2289. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1111, Column 62
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const testCases = [
        { endpoint: '/api/generate', body: { prompt: 'create pub contract' } },
        { endpoint: '/api/explain', body: { code: legacyCode } },
```

#### 2290. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1115, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 68. Legacy pub keyword found

```

#### 2291. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1119, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 13, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2292. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1127, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 2293. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1128, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 2294. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1131, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 69. Legacy pub keyword found

```

#### 2295. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1135, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2296. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1142, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 2297. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1143, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 2298. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1147, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 70. Legacy pub keyword found

```

#### 2299. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1151, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2300. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1159, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 2301. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1160, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 2302. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1163, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 71. Legacy pub keyword found

```

#### 2303. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1167, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2304. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1174, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 2305. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1175, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 2306. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1179, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 72. Legacy pub keyword found

```

#### 2307. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1183, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 98, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2308. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1191, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toBeDefined()
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.modernizedCode).toContain('access(all)')
```

#### 2309. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1195, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 73. Legacy pub keyword found

```

#### 2310. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1199, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 106, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2311. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1207, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 2312. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1208, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```
```

#### 2313. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1211, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 74. Legacy pub keyword found

```

#### 2314. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1215, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 107, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2315. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1222, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 2316. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1223, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
        }
```

#### 2317. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1227, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 75. Legacy pub keyword found

```

#### 2318. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1231, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 124, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2319. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1239, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toContain('access(all)')
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.transformationsApplied.length).toBeGreaterThan(0)
```

#### 2320. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1243, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 76. Legacy pub keyword found

```

#### 2321. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1247, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 196, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2322. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1255, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexLegacyCode = `
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 2323. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1256, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```
```

#### 2324. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1259, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 77. Legacy pub keyword found

```

#### 2325. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1263, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 197, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2326. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1270, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 2327. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1271, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 2328. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1275, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 78. Legacy pub keyword found

```

#### 2329. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1279, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 201, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2330. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1287, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub var publicVariable: String
          
```

#### 2331. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1291, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 79. Legacy pub keyword found

```

#### 2332. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 203, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2333. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1303, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 2334. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1304, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```
```

#### 2335. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1307, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 80. Legacy pub keyword found

```

#### 2336. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1311, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 204, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2337. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1318, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 2338. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1319, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```

#### 2339. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1323, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 81. Legacy pub keyword found

```

#### 2340. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1327, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 261, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2341. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1335, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle malformed code gracefully', async () => {
      const malformedCode = 'pub contract { invalid syntax }'
      
```

#### 2342. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1339, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 82. Legacy pub keyword found

```

#### 2343. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1343, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 273, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2344. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1351, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 2345. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1366, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```

#### 2346. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1371, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 84. Legacy pub keyword found

```

#### 2347. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1375, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 45
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2348. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1383, Column 45
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub keyword', () => {
      const legacyCode = `
```

#### 2349. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1387, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 85. Legacy pub keyword found

```

#### 2350. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1391, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2351. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1399, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 2352. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1400, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 2353. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1403, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 86. Legacy pub keyword found

```

#### 2354. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1407, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2355. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1414, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 2356. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1415, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 2357. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1419, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 87. Legacy pub keyword found

```

#### 2358. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1423, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 39, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2359. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1431, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 2360. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1435, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 88. Legacy pub keyword found

```

#### 2361. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1439, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 137, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2362. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1447, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('containsLegacyPubKeywords', () => {
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 2363. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1448, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```
```

#### 2364. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1448, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```
```

#### 2365. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1451, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 89. Legacy pub keyword found

```

#### 2366. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1455, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 138, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2367. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1462, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 2368. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1463, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 2369. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1463, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 2370. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1467, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 90. Legacy pub keyword found

```

#### 2371. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1471, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 138, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2372. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1478, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 2373. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1479, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 2374. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1479, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 2375. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1483, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 91. Legacy pub keyword found

```

#### 2376. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1487, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 152, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2377. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1495, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should not detect pub in comments or strings', () => {
      const codeWithPubInString = `
```

#### 2378. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1499, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 92. Legacy pub keyword found

```

#### 2379. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1503, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 155, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2380. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1511, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```

#### 2381. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1512, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```
```

#### 2382. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1515, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 93. Legacy pub keyword found

```

#### 2383. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1519, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 156, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2384. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1526, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```

#### 2385. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1527, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
        }
```

#### 2386. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1531, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 94. Legacy pub keyword found

```

#### 2387. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1535, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 164, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2388. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1543, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```

#### 2389. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1544, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```
```

#### 2390. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1547, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 95. Legacy pub keyword found

```

#### 2391. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1551, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2392. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1558, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```

#### 2393. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1559, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
      const result = validator.shouldRejectCode(legacyCode)
```

#### 2394. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1563, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 96. Legacy pub keyword found

```

#### 2395. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1567, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 203, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2396. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1575, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 2397. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1576, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 2398. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1579, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 97. Legacy pub keyword found

```

#### 2399. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1583, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 204, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2400. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1590, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 2401. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1591, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 2402. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1595, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 98. Legacy pub keyword found

```

#### 2403. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1599, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 243, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2404. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1607, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 2405. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1608, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 2406. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1611, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 99. Legacy pub keyword found

```

#### 2407. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1615, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 244, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2408. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1622, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 2409. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1623, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 2410. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1627, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 100. Legacy pub keyword found

```

#### 2411. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1631, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 306, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2412. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1639, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 2413. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1640, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 2414. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1643, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 101. Legacy pub keyword found

```

#### 2415. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1647, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 307, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2416. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1654, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 2417. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1655, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 2418. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1659, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 102. Legacy pub keyword found

```

#### 2419. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1663, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 347, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2420. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1671, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithMultipleIssues = `
        pub contract TestContract {
          pub var value: String
```

#### 2421. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1672, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
```
```

#### 2422. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1675, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 103. Legacy pub keyword found

```

#### 2423. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1679, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 348, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2424. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1686, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var value: String
```

#### 2425. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1687, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          fun missingAccess(): String {
```

#### 2426. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1691, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 104. Legacy pub keyword found

```

#### 2427. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1695, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 418, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2428. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1703, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract {
          pub var incomplete
          fun missingBraces() {
```

#### 2429. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1707, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 105. Legacy pub keyword found

```

#### 2430. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1711, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 429, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2431. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1719, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle very long lines', () => {
      const longLine = 'pub var ' + 'a'.repeat(10000) + ': String'
      const result = validator.validateCode(longLine)
```

#### 2432. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1867, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 115. Legacy pub keyword found

```

#### 2433. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1871, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 27, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2434. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1879, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    featured: false,
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```

#### 2435. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1880, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```
```

#### 2436. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1883, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 116. Legacy pub keyword found

```

#### 2437. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1887, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 28, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2438. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1894, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```

#### 2439. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1895, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64

```

#### 2440. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1899, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 117. Legacy pub keyword found

```

#### 2441. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1903, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2442. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1911, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```

#### 2443. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1912, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```
```

#### 2444. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1915, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 118. Legacy pub keyword found

```

#### 2445. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1919, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 31, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2446. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1926, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```

#### 2447. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1927, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```

#### 2448. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1928, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```
```

#### 2449. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1931, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 119. Legacy pub keyword found

```

#### 2450. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1935, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 32, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2451. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1942, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```

#### 2452. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1943, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)

```

#### 2453. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1947, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 120. Legacy pub keyword found

```

#### 2454. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1951, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2455. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1959, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub struct Proposal {
        pub let id: UInt64
```

#### 2456. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1960, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub struct Proposal {
        pub let id: UInt64
```
```

#### 2457. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1963, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 121. Legacy pub keyword found

```

#### 2458. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1967, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 35, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2459. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1974, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub struct Proposal {
        pub let id: UInt64
```

#### 2460. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1975, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub struct Proposal {
        pub let id: UInt64
        pub let title: String
```

#### 2461. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1976, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let id: UInt64
        pub let title: String
```
```

#### 2462. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1979, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 122. Legacy pub keyword found

```

#### 2463. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1983, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2464. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1990, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let id: UInt64
        pub let title: String
```

#### 2465. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1991, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let id: UInt64
        pub let title: String
        pub let description: String
```

#### 2466. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1992, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let title: String
        pub let description: String
```
```

#### 2467. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1995, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 123. Legacy pub keyword found

```

#### 2468. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1999, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2469. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2006, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let title: String
        pub let description: String
```

#### 2470. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2007, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let title: String
        pub let description: String
        pub let creator: Address
```

#### 2471. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2008, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let description: String
        pub let creator: Address
```
```

#### 2472. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2011, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 124. Legacy pub keyword found

```

#### 2473. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2015, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 38, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2474. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2022, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let description: String
        pub let creator: Address
```

#### 2475. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2023, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let description: String
        pub let creator: Address
        pub var votesFor: UInt64
```

#### 2476. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2024, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
```
```

#### 2477. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2027, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 125. Legacy pub keyword found

```

#### 2478. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2031, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 39, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2479. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2038, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
```

#### 2480. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2039, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```

#### 2481. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2040, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```
```

#### 2482. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2043, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 126. Legacy pub keyword found

```

#### 2483. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2047, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 40, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2484. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2054, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```

#### 2485. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2055, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
        pub var executed: Bool
```

#### 2486. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2056, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
```
```

#### 2487. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2059, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 127. Legacy pub keyword found

```

#### 2488. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2063, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 41, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2489. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2070, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
```

#### 2490. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2071, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
        pub let createdAt: UFix64
```

#### 2491. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2072, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64
```
```

#### 2492. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2075, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 128. Legacy pub keyword found

```

#### 2493. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2079, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 42, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2494. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2086, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64
```

#### 2495. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2087, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64

```

#### 2496. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2091, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 129. Legacy pub keyword found

```

#### 2497. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2095, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 55, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2498. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2103, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun addVote(support: Bool) {
            if support {
```

#### 2499. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2107, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 130. Legacy pub keyword found

```

#### 2500. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2111, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 63, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2501. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2119, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun execute() {
            self.executed = true
```

#### 2502. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2123, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 131. Legacy pub keyword found

```

#### 2503. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2127, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 71, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2504. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2135, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 2505. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2139, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 132. Legacy pub keyword found

```

#### 2506. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2143, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 85, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2507. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2151, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun vote(proposalId: UInt64, voter: Address, support: Bool) {
        pre {
```

#### 2508. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2155, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 133. Legacy pub keyword found

```

#### 2509. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2159, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 98, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2510. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2167, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 2511. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2171, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 134. Legacy pub keyword found

```

#### 2512. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2175, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 102, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2513. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2183, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getAllProposals(): [Proposal] {
        return self.proposals.values
```

#### 2514. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2187, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 135. Legacy pub keyword found

```

#### 2515. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2191, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 121, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2516. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2199, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Syntax Transformation', () => {
    it('should transform all pub keywords to access(all)', () => {
      const transformedCode = transformer.transformAccessModifiers(originalDAOTemplate.code)
```

#### 2517. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2203, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 136. Legacy pub keyword found

```

#### 2518. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2207, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 124, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2519. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2215, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should not contain any pub keywords followed by Cadence keywords
      expect(transformedCode).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 2520. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2219, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 137. Legacy pub keyword found

```

#### 2521. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2223, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 223, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2522. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2231, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(validationResult.errors.length).toBeGreaterThan(0)
      expect(validationResult.errors.some(error => error.includes('pub keyword usage'))).toBe(true)
    })
```

#### 2523. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2235, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 138. Legacy pub keyword found

```

#### 2524. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2239, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 232, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2525. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2247, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const hasLegacySyntaxErrors = validationResult.errors.some(error => 
        error.includes('pub keyword usage') || 
        error.includes('Legacy syntax found')
```

#### 2526. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2251, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 139. Legacy pub keyword found

```

#### 2527. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2255, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2528. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2263, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform all pub keywords to access(all)', () => {
    const template = getTemplateById('fungible-token')
```

#### 2529. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2267, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 140. Legacy pub keyword found

```

#### 2530. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2271, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 43, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2531. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2279, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check that no pub keywords remain
    expect(template.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 2532. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2283, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 141. Legacy pub keyword found

```

#### 2533. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2287, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 12, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2534. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2295, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Access Modifier Detection', () => {
    test('detects pub keyword in various contexts', () => {
      const code = `
```

#### 2535. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2299, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 142. Legacy pub keyword found

```

#### 2536. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2303, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2537. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2311, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 2538. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2315, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 143. Legacy pub keyword found

```

#### 2539. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2319, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 17, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2540. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2327, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64
        pub let name: String
```

#### 2541. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2328, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        pub let name: String
```
```

#### 2542. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2331, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 144. Legacy pub keyword found

```

#### 2543. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2335, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2544. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2342, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var balance: UFix64
        pub let name: String
```

#### 2545. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2343, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        pub let name: String
      `;
```

#### 2546. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2347, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 145. Legacy pub keyword found

```

#### 2547. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2351, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 25, Column 49
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2548. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2359, Column 49
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(pubPatterns).toHaveLength(3);
      expect(pubPatterns[0].originalText).toBe('pub fun ');
      expect(pubPatterns[0].modernReplacement).toBe('access(all) fun ');
```

#### 2549. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2363, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 146. Legacy pub keyword found

```

#### 2550. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2367, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 43, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2551. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2375, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 2552. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2379, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 147. Legacy pub keyword found

```

#### 2553. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2383, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 49, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2554. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2391, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const patterns = detector.detectAllLegacyPatterns(code);
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      
```

#### 2555. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2395, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 148. Legacy pub keyword found

```

#### 2556. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2399, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 113, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2557. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2407, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 2558. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2411, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 149. Legacy pub keyword found

```

#### 2559. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2415, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 138, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2560. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2423, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 2561. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2427, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 150. Legacy pub keyword found

```

#### 2562. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2431, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 140, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2563. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2439, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.link<&Vault>(/public/vault, target: /storage/vault)
        pub var balance: UFix64
      `;
```

#### 2564. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2443, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 151. Legacy pub keyword found

```

#### 2565. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 164, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2566. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2455, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 2567. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2456, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```
```

#### 2568. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2459, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 152. Legacy pub keyword found

```

#### 2569. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2463, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2570. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2470, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 2571. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2471, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 2572. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2472, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```
```

#### 2573. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2475, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 153. Legacy pub keyword found

```

#### 2574. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2479, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 166, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2575. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2486, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 2576. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2487, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
          
```

#### 2577. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2491, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 154. Legacy pub keyword found

```

#### 2578. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2495, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 183, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2579. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2503, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 2580. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2507, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 155. Legacy pub keyword found

```

#### 2581. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2511, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 185, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2582. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2519, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const highRiskCode = `
        pub fun test1() {}
        pub fun test2() {}
```

#### 2583. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2520, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test1() {}
        pub fun test2() {}
```
```

#### 2584. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2523, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 156. Legacy pub keyword found

```

#### 2585. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2527, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 186, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2586. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2534, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test1() {}
        pub fun test2() {}
```

#### 2587. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2535, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test1() {}
        pub fun test2() {}
        pub fun test3() {}
```

#### 2588. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2536, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test2() {}
        pub fun test3() {}
```
```

#### 2589. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2539, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 157. Legacy pub keyword found

```

#### 2590. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2543, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 187, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2591. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2550, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test2() {}
        pub fun test3() {}
```

#### 2592. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2551, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test2() {}
        pub fun test3() {}
        pub fun test4() {}
```

#### 2593. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2552, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test3() {}
        pub fun test4() {}
```
```

#### 2594. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2555, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 158. Legacy pub keyword found

```

#### 2595. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2559, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 188, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2596. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2566, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test3() {}
        pub fun test4() {}
```

#### 2597. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2567, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test3() {}
        pub fun test4() {}
        pub fun test5() {}
```

#### 2598. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2568, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test4() {}
        pub fun test5() {}
```
```

#### 2599. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2571, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 159. Legacy pub keyword found

```

#### 2600. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2575, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 189, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2601. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2582, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test4() {}
        pub fun test5() {}
```

#### 2602. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2583, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test4() {}
        pub fun test5() {}
        pub fun test6() {}
```

#### 2603. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2584, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test5() {}
        pub fun test6() {}
```
```

#### 2604. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2587, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 160. Legacy pub keyword found

```

#### 2605. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2591, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 190, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2606. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2598, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test5() {}
        pub fun test6() {}
```

#### 2607. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2599, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test5() {}
        pub fun test6() {}
        pub fun test7() {}
```

#### 2608. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2600, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test6() {}
        pub fun test7() {}
```
```

#### 2609. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2603, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 161. Legacy pub keyword found

```

#### 2610. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2607, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 191, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2611. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2614, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test6() {}
        pub fun test7() {}
```

#### 2612. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2615, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test6() {}
        pub fun test7() {}
        pub fun test8() {}
```

#### 2613. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2616, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test7() {}
        pub fun test8() {}
```
```

#### 2614. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2619, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 162. Legacy pub keyword found

```

#### 2615. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2623, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 192, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2616. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2630, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test7() {}
        pub fun test8() {}
```

#### 2617. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2631, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test7() {}
        pub fun test8() {}
        pub fun test9() {}
```

#### 2618. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2632, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test8() {}
        pub fun test9() {}
```
```

#### 2619. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2635, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 163. Legacy pub keyword found

```

#### 2620. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2639, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 193, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2621. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2646, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test8() {}
        pub fun test9() {}
```

#### 2622. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2647, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test8() {}
        pub fun test9() {}
        pub fun test10() {}
```

#### 2623. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2648, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test9() {}
        pub fun test10() {}
```
```

#### 2624. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2651, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 164. Legacy pub keyword found

```

#### 2625. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2655, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 194, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2626. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2662, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test9() {}
        pub fun test10() {}
```

#### 2627. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2663, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test9() {}
        pub fun test10() {}
        pub fun test11() {}
```

#### 2628. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2664, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test10() {}
        pub fun test11() {}
```
```

#### 2629. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2667, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 165. Legacy pub keyword found

```

#### 2630. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2671, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 195, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2631. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2678, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test10() {}
        pub fun test11() {}
```

#### 2632. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2679, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test10() {}
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```

#### 2633. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2683, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 166. Legacy pub keyword found

```

#### 2634. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2687, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 231, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2635. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2695, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 2636. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2696, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 2637. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2699, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 167. Legacy pub keyword found

```

#### 2638. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2703, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 232, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2639. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2710, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 2640. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2711, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 2641. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2712, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```
```

#### 2642. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2715, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 168. Legacy pub keyword found

```

#### 2643. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2719, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 233, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2644. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2726, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 2645. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2727, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```

#### 2646. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2731, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 169. Legacy pub keyword found

```

#### 2647. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2735, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 250, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2648. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2743, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 2649. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2747, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 170. Legacy pub keyword found

```

#### 2650. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2751, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 256, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2651. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2759, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      expect(pubPattern?.location.line).toBe(2);
```

#### 2652. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2763, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 171. Legacy pub keyword found

```

#### 2653. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2767, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 267, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2654. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2775, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        // This should not trigger pub detection
        let description = "This is a function"
```

#### 2655. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2779, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 172. Legacy pub keyword found

```

#### 2656. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2783, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 283, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2657. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2791, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(1000).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test" }`
      ).join('\n');
```

#### 2658. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2795, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 173. Legacy pub keyword found

```

#### 2659. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2799, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 290, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2660. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2807, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```

#### 2661. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2808, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```
```

#### 2662. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2811, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 174. Legacy pub keyword found

```

#### 2663. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2815, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2664. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2822, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```

#### 2665. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2823, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
      expect(pubFunPatterns).toHaveLength(1000);
```

#### 2666. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2902, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 2667. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2934, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```

#### 2668. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2950, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```

#### 2669. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2966, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub fun test() {
  account.save()
```

#### 2670. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2987, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 185. Legacy pub keyword found

```

#### 2671. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2991, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 127, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2672. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2999, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const sampleContract = `
        pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 2673. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3000, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 2674. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3003, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 186. Legacy pub keyword found

```

#### 2675. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3007, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 128, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2676. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3014, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 2677. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3015, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 2678. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3019, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 187. Legacy pub keyword found

```

#### 2679. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3023, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 130, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2680. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3031, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 2681. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3035, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 188. Legacy pub keyword found

```

#### 2682. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3039, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 134, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2683. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3047, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun updateSupply(newSupply: UFix64) {
            self.totalSupply = newSupply
```

#### 2684. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3051, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 189. Legacy pub keyword found

```

#### 2685. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3055, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 155, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2686. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3063, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify syntax is modernized
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 2687. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3067, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 190. Legacy pub keyword found

```

#### 2688. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3071, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 161, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2689. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3079, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const resourceContract = `
        pub contract ResourceTest {
          pub resource Vault {
```

#### 2690. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3080, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ResourceTest {
          pub resource Vault {
```
```

#### 2691. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3083, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 191. Legacy pub keyword found

```

#### 2692. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3087, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 162, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2693. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3094, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ResourceTest {
          pub resource Vault {
```

#### 2694. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3095, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ResourceTest {
          pub resource Vault {
            pub var balance: UFix64
```

#### 2695. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3096, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault {
            pub var balance: UFix64
```
```

#### 2696. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3099, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 192. Legacy pub keyword found

```

#### 2697. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3103, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 163, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2698. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3110, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault {
            pub var balance: UFix64
```

#### 2699. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3111, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault {
            pub var balance: UFix64
            
```

#### 2700. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3115, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 193. Legacy pub keyword found

```

#### 2701. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3119, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2702. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3127, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              self.balance = self.balance + from.balance
```

#### 2703. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3131, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 194. Legacy pub keyword found

```

#### 2704. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3135, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 170, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2705. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3143, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              self.balance = self.balance - amount
```

#### 2706. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3147, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 195. Legacy pub keyword found

```

#### 2707. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3151, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 194, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2708. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3159, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 2709. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3163, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 196. Legacy pub keyword found

```

#### 2710. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3167, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 199, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2711. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3175, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const eventContract = `
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```

#### 2712. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3176, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```
```

#### 2713. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3179, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 197. Legacy pub keyword found

```

#### 2714. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3183, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 200, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2715. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3190, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```

#### 2716. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3191, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```

#### 2717. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3192, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```
```

#### 2718. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3195, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 198. Legacy pub keyword found

```

#### 2719. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3199, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 201, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2720. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3206, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```

#### 2721. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3207, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
          
```

#### 2722. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3211, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 199. Legacy pub keyword found

```

#### 2723. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3215, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 203, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2724. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3223, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64, to: Address?) {
            emit TokensDeposited(amount: amount, to: to)
```

#### 2725. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3227, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 200. Legacy pub keyword found

```

#### 2726. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3231, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 207, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2727. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3239, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(amount: UFix64, from: Address?) {
            emit TokensWithdrawn(amount: amount, from: from)
```

#### 2728. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3243, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 201. Legacy pub keyword found

```

#### 2729. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3247, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 224, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2730. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3255, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 2731. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3259, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 202. Legacy pub keyword found

```

#### 2732. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3263, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 229, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2733. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3271, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const interfaceContract = `
        pub contract InterfaceTest {
          pub resource interface Provider {
```

#### 2734. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3272, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
```
```

#### 2735. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3275, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 203. Legacy pub keyword found

```

#### 2736. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3279, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 230, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2737. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3286, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
```

#### 2738. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3287, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```

#### 2739. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3288, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```
```

#### 2740. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3291, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 204. Legacy pub keyword found

```

#### 2741. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 231, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2742. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3302, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```

#### 2743. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3303, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
          }
```

#### 2744. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3307, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 205. Legacy pub keyword found

```

#### 2745. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3311, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 234, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2746. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3319, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```

#### 2747. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3320, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```
```

#### 2748. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3323, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 206. Legacy pub keyword found

```

#### 2749. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3327, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 235, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2750. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3334, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```

#### 2751. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3335, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
          }
```

#### 2752. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3339, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 207. Legacy pub keyword found

```

#### 2753. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3343, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 238, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2754. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3351, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 2755. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3352, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 2756. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3355, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 208. Legacy pub keyword found

```

#### 2757. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3359, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 239, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2758. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3366, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 2759. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3367, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 2760. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3371, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 209. Legacy pub keyword found

```

#### 2761. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3375, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 241, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2762. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3383, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 2763. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3387, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 210. Legacy pub keyword found

```

#### 2764. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3391, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 245, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2765. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3399, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              destroy from
```

#### 2766. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3403, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 211. Legacy pub keyword found

```

#### 2767. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3407, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 269, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2768. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3415, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 2769. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3419, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 212. Legacy pub keyword found

```

#### 2770. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3423, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 289, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2771. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3431, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeContract = `
        pub contract LargeContract {
          ${Array.from({ length: 100 }, (_, i) => `
```

#### 2772. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3435, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 213. Legacy pub keyword found

```

#### 2773. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3439, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2774. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3447, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          ${Array.from({ length: 100 }, (_, i) => `
            pub var value${i}: UFix64
            
```

#### 2775. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3451, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 214. Legacy pub keyword found

```

#### 2776. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3455, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 293, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2777. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3463, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue${i}(): UFix64 {
              return self.value${i}
```

#### 2778. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3467, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 215. Legacy pub keyword found

```

#### 2779. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3471, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 297, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2780. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3479, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun setValue${i}(newValue: UFix64) {
              self.value${i} = newValue
```

#### 2781. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3483, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 216. Legacy pub keyword found

```

#### 2782. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3487, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 322, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2783. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3495, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify transformation was applied
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 2784. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3499, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 217. Legacy pub keyword found

```

#### 2785. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3503, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 368, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2786. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3511, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const malformedCode = `
        pub contract {
          pub var incomplete
```

#### 2787. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3512, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract {
          pub var incomplete
```
```

#### 2788. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3515, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 218. Legacy pub keyword found

```

#### 2789. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3519, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 369, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2790. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3526, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract {
          pub var incomplete
```

#### 2791. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3527, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract {
          pub var incomplete
          pub fun missingBrace() {
```

#### 2792. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3528, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var incomplete
          pub fun missingBrace() {
```
```

#### 2793. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3531, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 219. Legacy pub keyword found

```

#### 2794. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3535, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 370, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2795. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3542, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var incomplete
          pub fun missingBrace() {
```

#### 2796. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3543, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var incomplete
          pub fun missingBrace() {
        // Missing closing brace and other syntax errors
```

#### 2797. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3547, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 220. Legacy pub keyword found

```

#### 2798. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3551, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 474, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2799. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3559, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `
          pub contract TestTemplate {
            pub var value: String
```

#### 2800. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3560, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub contract TestTemplate {
            pub var value: String
```
```

#### 2801. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3563, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 221. Legacy pub keyword found

```

#### 2802. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3567, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 475, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2803. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3574, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub contract TestTemplate {
            pub var value: String
```

#### 2804. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3575, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub contract TestTemplate {
            pub var value: String
            
```

#### 2805. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3579, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 222. Legacy pub keyword found

```

#### 2806. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3583, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 477, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2807. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3591, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 2808. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3595, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 223. Legacy pub keyword found

```

#### 2809. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3599, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 503, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2810. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3607, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify code is transformed
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 2811. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3611, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 224. Legacy pub keyword found

```

#### 2812. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3615, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 28, Column 44
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2813. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3623, Column 44
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Validate the template has been migrated
      expect(template.code).not.toContain('pub ')
      expect(template.code).toContain('access(all)')
```

#### 2814. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3627, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 225. Legacy pub keyword found

```

#### 2815. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3631, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 199, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2816. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3639, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyErrors = validationResult.errors.filter(error => 
        error.includes('Legacy syntax') || error.includes('pub keyword')
      )
```

#### 2817. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3643, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 226. Legacy pub keyword found

```

#### 2818. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3647, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2819. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3655, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featured: false,
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```

#### 2820. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3656, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```
```

#### 2821. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3659, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 227. Legacy pub keyword found

```

#### 2822. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3663, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 31, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2823. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3670, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```

#### 2824. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3671, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```

#### 2825. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3672, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```
```

#### 2826. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3675, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 228. Legacy pub keyword found

```

#### 2827. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3679, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 32, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2828. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3686, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```

#### 2829. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3687, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)

```

#### 2830. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3691, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 229. Legacy pub keyword found

```

#### 2831. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3695, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2832. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3703, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```

#### 2833. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3704, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```
```

#### 2834. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3707, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 230. Legacy pub keyword found

```

#### 2835. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3711, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 35, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2836. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3718, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```

#### 2837. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3719, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 2838. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3720, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```
```

#### 2839. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3723, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 231. Legacy pub keyword found

```

#### 2840. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3727, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2841. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3734, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 2842. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3735, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 2843. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3736, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```
```

#### 2844. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3739, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 232. Legacy pub keyword found

```

#### 2845. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3743, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2846. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3750, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 2847. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3751, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
    }
```

#### 2848. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3755, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 233. Legacy pub keyword found

```

#### 2849. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3759, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 40, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2850. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3767, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource SaleCollection: SalePublic {
        access(self) var forSale: {UInt64: UFix64}
```

#### 2851. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3771, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 234. Legacy pub keyword found

```

#### 2852. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3775, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 49, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2853. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3783, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun listForSale(tokenID: UInt64, price: UFix64) {
            self.forSale[tokenID] = price
```

#### 2854. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3787, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 235. Legacy pub keyword found

```

#### 2855. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3791, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 54, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2856. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3799, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault) {
            let price = self.forSale[tokenID]!
```

#### 2857. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3803, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 236. Legacy pub keyword found

```

#### 2858. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3807, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 66, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2859. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3815, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 2860. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3819, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 237. Legacy pub keyword found

```

#### 2861. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3823, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 70, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2862. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3831, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun getIDs(): [UInt64] {
            return self.forSale.keys
```

#### 2863. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3835, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 238. Legacy pub keyword found

```

#### 2864. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3839, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 75, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2865. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3847, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createSaleCollection(collection: &NFTCollection): @SaleCollection {
        return <- create SaleCollection(collection: collection)
```

#### 2866. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3851, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 239. Legacy pub keyword found

```

#### 2867. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3855, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 91, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2868. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3863, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform pub keywords to access(all)', () => {
    const migratedTemplate = migration.migrateNFTMarketplace(legacyTemplate)
```

#### 2869. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3867, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 240. Legacy pub keyword found

```

#### 2870. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3871, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 94, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2871. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3879, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Should not contain pub keywords
    expect(migratedTemplate.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface|event)/)
```

#### 2872. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3883, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 241. Legacy pub keyword found

```

#### 2873. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3887, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2874. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3895, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      ...legacyTemplate,
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 2875. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3896, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```
```

#### 2876. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3899, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 242. Legacy pub keyword found

```

#### 2877. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3903, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2878. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3910, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 2879. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3911, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 2880. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3912, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```
```

#### 2881. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3915, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 243. Legacy pub keyword found

```

#### 2882. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3919, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 106, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2883. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3926, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 2884. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3927, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
        }
```

#### 2885. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3931, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 244. Legacy pub keyword found

```

#### 2886. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3935, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2887. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3943, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 2888. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3944, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```
```

#### 2889. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3947, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 245. Legacy pub keyword found

```

#### 2890. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3951, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 15, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2891. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3958, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 2892. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3959, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 2893. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3963, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 246. Legacy pub keyword found

```

#### 2894. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3967, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2895. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3975, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          }
          pub var balance: UFix64
        }
```

#### 2896. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3979, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 247. Legacy pub keyword found

```

#### 2897. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3983, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 31, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2898. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3991, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(500).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test${i}" }`
      ).join('\n');
```

#### 2899. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3995, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 248. Legacy pub keyword found

```

#### 2900. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3999, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 59, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2901. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4007, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 2902. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4011, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 249. Legacy pub keyword found

```

#### 2903. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4015, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 83, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2904. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4023, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 2905. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4027, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 250. Legacy pub keyword found

```

#### 2906. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4031, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 103, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2907. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4039, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 2908. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4040, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```
```

#### 2909. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4043, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 251. Legacy pub keyword found

```

#### 2910. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4047, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2911. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4054, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 2912. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4055, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```

#### 2913. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4059, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 252. Legacy pub keyword found

```

#### 2914. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4063, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 122, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2915. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4071, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 2916. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4075, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 253. Legacy pub keyword found

```

#### 2917. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4079, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 125, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2918. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4087, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64 = 0.0
      `;
```

#### 2919. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4091, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 254. Legacy pub keyword found

```

#### 2920. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4095, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 130, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2921. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4103, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```

#### 2922. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4104, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```
```

#### 2923. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4107, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 255. Legacy pub keyword found

```

#### 2924. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4111, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 131, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2925. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4118, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```

#### 2926. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4119, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 2927. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4123, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 256. Legacy pub keyword found

```

#### 2928. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4127, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 140, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2929. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4135, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub var balance: UFix64
```

#### 2930. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4136, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var balance: UFix64
```
```

#### 2931. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4139, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 257. Legacy pub keyword found

```

#### 2932. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4143, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 141, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2933. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4150, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub var balance: UFix64
```

#### 2934. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4151, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var balance: UFix64
          
```

#### 2935. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4155, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 258. Legacy pub keyword found

```

#### 2936. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4159, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 143, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2937. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4167, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64) {
            self.balance = self.balance + amount
```

#### 2938. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4171, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 259. Legacy pub keyword found

```

#### 2939. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4175, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 164, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2940. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4183, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 2941. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4187, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 260. Legacy pub keyword found

```

#### 2942. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4191, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 187, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2943. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4199, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub fun complexFunction() {
          account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 2944. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4203, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 261. Legacy pub keyword found

```

#### 2945. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4207, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 198, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2946. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4215, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // The pub fun should be auto-fixed
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 2947. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4219, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 262. Legacy pub keyword found

```

#### 2948. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4223, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 214, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2949. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4231, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 2950. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4235, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 263. Legacy pub keyword found

```

#### 2951. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4239, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 253, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2952. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4247, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 2953. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4251, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 264. Legacy pub keyword found

```

#### 2954. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4255, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 283, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2955. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4263, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles malformed code gracefully', async () => {
      const malformedCode = 'pub fun { invalid syntax }';
      
```

#### 2956. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4267, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 265. Legacy pub keyword found

```

#### 2957. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4271, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 293, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2958. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4279, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles very large code inputs', async () => {
      const veryLargeCode = 'pub fun test() {}\n'.repeat(1000); // Reduced size for better performance
      
```

#### 2959. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4283, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 266. Legacy pub keyword found

```

#### 2960. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4287, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 305, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2961. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4295, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 2962. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4296, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 2963. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4299, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 267. Legacy pub keyword found

```

#### 2964. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4303, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 306, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2965. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4310, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 2966. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4311, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 2967. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4312, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```
```

#### 2968. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4315, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 268. Legacy pub keyword found

```

#### 2969. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4319, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 307, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2970. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4326, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 2971. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4327, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```

#### 2972. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4331, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 269. Legacy pub keyword found

```

#### 2973. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4335, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 326, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2974. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4343, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 2975. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4374, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```

#### 2976. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4406, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 2977. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4422, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```

#### 2978. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4438, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub fun test() {
  account.save()
```

#### 2979. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4443, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 276. Legacy pub keyword found

```

#### 2980. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4447, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2981. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4455, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify the template has been migrated
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```

#### 2982. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4456, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```
```

#### 2983. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4459, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 277. Legacy pub keyword found

```

#### 2984. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4463, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 31, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2985. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4470, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```

#### 2986. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4471, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```

#### 2987. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4472, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```
```

#### 2988. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4475, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 278. Legacy pub keyword found

```

#### 2989. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4479, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 32, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2990. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4486, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```

#### 2991. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4487, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```

#### 2992. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4488, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```
```

#### 2993. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4491, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 279. Legacy pub keyword found

```

#### 2994. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4495, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 33, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 2995. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4502, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```

#### 2996. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4503, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```

#### 2997. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4504, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```
```

#### 2998. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4507, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 280. Legacy pub keyword found

```

#### 2999. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4511, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3000. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4518, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```

#### 3001. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4519, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```

#### 3002. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4520, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```
```

#### 3003. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4523, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 281. Legacy pub keyword found

```

#### 3004. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4527, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 35, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3005. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4534, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```

#### 3006. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4535, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
  })
```

#### 3007. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4539, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 282. Legacy pub keyword found

```

#### 3008. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4543, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3009. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4551, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('transformAccessModifiers', () => {
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 3010. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4552, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```
```

#### 3011. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4555, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 283. Legacy pub keyword found

```

#### 3012. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4559, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3013. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4566, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 3014. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4567, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 3015. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4571, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 284. Legacy pub keyword found

```

#### 3016. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4575, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 32, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3017. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4583, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms multiple pub keywords', () => {
      const input = `
```

#### 3018. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4587, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 285. Legacy pub keyword found

```

#### 3019. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4591, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3020. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4599, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract {
          pub var totalSupply: UFix64
```

#### 3021. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4600, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
```
```

#### 3022. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4603, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 286. Legacy pub keyword found

```

#### 3023. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4607, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 35, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3024. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4614, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
```

#### 3025. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4615, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 3026. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4616, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```
```

#### 3027. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4619, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 287. Legacy pub keyword found

```

#### 3028. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4623, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3029. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4630, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 3030. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4631, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 3031. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4635, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 288. Legacy pub keyword found

```

#### 3032. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4639, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 53, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3033. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4647, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 3034. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4651, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 289. Legacy pub keyword found

```

#### 3035. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4655, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 55, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3036. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4663, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract Test {
          pub(set) var value: Int
```

#### 3037. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4667, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 290. Legacy pub keyword found

```

#### 3038. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4671, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 57, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3039. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4679, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 3040. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4683, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 291. Legacy pub keyword found

```

#### 3041. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4687, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 82, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3042. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4695, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub in comments and strings correctly', () => {
      const input = `
```

#### 3043. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4699, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 292. Legacy pub keyword found

```

#### 3044. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4703, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 84, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3045. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4711, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        // This is a pub comment
        pub contract Test {
```

#### 3046. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4712, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This is a pub comment
        pub contract Test {
```
```

#### 3047. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4715, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 293. Legacy pub keyword found

```

#### 3048. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4719, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 85, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3049. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4726, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        // This is a pub comment
        pub contract Test {
```

#### 3050. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4727, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This is a pub comment
        pub contract Test {
          pub fun test(): String {
```

#### 3051. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4728, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
```
```

#### 3052. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4731, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 294. Legacy pub keyword found

```

#### 3053. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4735, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 86, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3054. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4742, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub fun test(): String {
```

#### 3055. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4743, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 3056. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4744, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```
```

#### 3057. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4747, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 295. Legacy pub keyword found

```

#### 3058. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4751, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 87, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3059. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4758, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```

#### 3060. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4759, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
          }
```

#### 3061. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4763, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 296. Legacy pub keyword found

```

#### 3062. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4767, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 92, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3063. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4775, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const expected = `
        // This is a pub comment
        access(all) contract Test {
```

#### 3064. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4779, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 297. Legacy pub keyword found

```

#### 3065. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4783, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 95, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3066. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4791, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
          }
```

#### 3067. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4795, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 298. Legacy pub keyword found

```

#### 3068. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4799, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 109, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3069. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4807, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles code without pub keywords', () => {
      const input = `
```

#### 3070. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4811, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 299. Legacy pub keyword found

```

#### 3071. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4815, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 318, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3072. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4823, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3073. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4824, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```
```

#### 3074. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4827, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 300. Legacy pub keyword found

```

#### 3075. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4831, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 319, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3076. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4838, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3077. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4839, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
          
```

#### 3078. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4843, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 301. Legacy pub keyword found

```

#### 3079. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4847, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 321, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3080. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4855, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```

#### 3081. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4859, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 302. Legacy pub keyword found

```

#### 3082. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4863, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 325, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3083. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4871, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(): @Vault {
            return <-account.load<@Vault>(from: /storage/vault)!
```

#### 3084. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4875, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 303. Legacy pub keyword found

```

#### 3085. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4879, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 333, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3086. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4887, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should transform pub keywords
      expect(result).toContain('access(all) contract')
```

#### 3087. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4891, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 304. Legacy pub keyword found

```

#### 3088. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4895, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 349, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3089. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4903, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates transformation statistics', () => {
      const original = `pub contract Test {
        pub var value: Int
```

#### 3090. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4904, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const original = `pub contract Test {
        pub var value: Int
```
```

#### 3091. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4907, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 305. Legacy pub keyword found

```

#### 3092. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4911, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 350, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3093. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4918, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const original = `pub contract Test {
        pub var value: Int
```

#### 3094. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4919, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const original = `pub contract Test {
        pub var value: Int
      }`
```

#### 3095. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4967, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 3096. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4982, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub(set) var value: Int
```

#### 3097. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4984, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```
```

#### 3098. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5030, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```

#### 3099. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5035, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 313. Legacy pub keyword found

```

#### 3100. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5039, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 174, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3101. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5047, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 3102. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5048, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 3103. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5051, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 314. Legacy pub keyword found

```

#### 3104. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5055, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 175, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3105. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5062, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 3106. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5063, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 3107. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5067, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 315. Legacy pub keyword found

```

#### 3108. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5071, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 177, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3109. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5079, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3110. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5080, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 3111. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5083, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 316. Legacy pub keyword found

```

#### 3112. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5087, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 178, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3113. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5094, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3114. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5095, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 3115. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5099, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 317. Legacy pub keyword found

```

#### 3116. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5103, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 180, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3117. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5111, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 3118. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5115, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 318. Legacy pub keyword found

```

#### 3119. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5119, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 189, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3120. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5127, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(migratedTemplate.code).not.toContain('pub ')
      expect(migratedTemplate.code).toContain('access(all)')
```

#### 3121. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5131, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 319. Legacy pub keyword found

```

#### 3122. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5135, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 256, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3123. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5143, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 3124. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5144, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```
```

#### 3125. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5147, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 320. Legacy pub keyword found

```

#### 3126. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5151, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 257, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3127. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5158, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 3128. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5159, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
        }`
```

#### 3129. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5163, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 321. Legacy pub keyword found

```

#### 3130. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5167, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 265, Column 58
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3131. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5175, Column 58
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.errors.length).toBeGreaterThan(0)
      expect(result.errors.some(error => error.includes('pub keyword'))).toBe(true)
    })
```

#### 3132. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5179, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 322. Legacy pub keyword found

```

#### 3133. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5183, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 53, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3134. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5191, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 3135. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5192, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 3136. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5195, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 323. Legacy pub keyword found

```

#### 3137. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5199, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 54, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3138. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5206, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 3139. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5207, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 3140. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5211, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 324. Legacy pub keyword found

```

#### 3141. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5215, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 56, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3142. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5223, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3143. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5227, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 325. Legacy pub keyword found

```

#### 3144. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5231, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 87, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3145. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5239, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectGeneratedCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 3146. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5240, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```
```

#### 3147. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5240, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```
```

#### 3148. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5243, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 326. Legacy pub keyword found

```

#### 3149. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5247, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 88, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3150. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5254, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 3151. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5255, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 3152. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5255, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 3153. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5259, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 327. Legacy pub keyword found

```

#### 3154. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5263, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 88, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3155. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5270, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 3156. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5271, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 3157. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5271, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 3158. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5275, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 328. Legacy pub keyword found

```

#### 3159. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5279, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 146, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3160. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5287, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should use modern syntax
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 3161. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5291, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 329. Legacy pub keyword found

```

#### 3162. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5295, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 168, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3163. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5303, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 3164. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5304, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 3165. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5307, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 330. Legacy pub keyword found

```

#### 3166. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5311, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 169, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3167. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5318, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 3168. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5319, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 3169. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5323, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 331. Legacy pub keyword found

```

#### 3170. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5327, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 203, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3171. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5335, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 3172. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5336, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 3173. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5339, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 332. Legacy pub keyword found

```

#### 3174. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5343, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 204, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3175. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5350, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 3176. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5351, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 3177. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5355, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 333. Legacy pub keyword found

```

#### 3178. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5359, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 248, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3179. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5367, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 3180. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5371, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 334. Legacy pub keyword found

```

#### 3181. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5375, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 258, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3182. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5383, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 3183. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5387, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 335. Legacy pub keyword found

```

#### 3184. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5391, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 307, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3185. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5399, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 3186. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5400, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
```
```

#### 3187. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5403, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 336. Legacy pub keyword found

```

#### 3188. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5407, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 308, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3189. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5414, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract LegacyContract {
          pub var value: String
```

#### 3190. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5415, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 3191. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5419, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 337. Legacy pub keyword found

```

#### 3192. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5423, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 368, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3193. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5431, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(validation.isValid).toBe(true)
        expect(code).not.toContain('pub ')
        expect(code).toContain('access(all)')
```

#### 3194. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5547, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 345. Legacy pub keyword found

```

#### 3195. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5551, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 278, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3196. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5559, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Add general modernization suggestions
    if (code.includes('pub ')) {
      suggestions.push('Replace all "pub" keywords with appropriate access modifiers like "access(all)"')
```

#### 3197. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5563, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 346. Legacy pub keyword found

```

#### 3198. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5567, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 41, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3199. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5575, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub keyword found',
      suggestedFix: 'Replace with access(all) or appropriate access modifier',
```

#### 3200. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5643, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 351. Legacy pub keyword found

```

#### 3201. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5647, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 17, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3202. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5655, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub to access(all)',
      category: 'access-modifier'
```

#### 3203. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5707, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 355. Legacy pub keyword found

```

#### 3204. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5711, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 15, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3205. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5719, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 3206. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5720, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```
```

#### 3207. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5723, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 356. Legacy pub keyword found

```

#### 3208. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5727, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 16, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3209. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5734, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 3210. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5735, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 3211. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5739, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 357. Legacy pub keyword found

```

#### 3212. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5743, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 20, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3213. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5751, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3214. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5752, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 3215. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5755, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 358. Legacy pub keyword found

```

#### 3216. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5759, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 21, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3217. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5766, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3218. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5767, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 3219. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5787, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 360. Legacy pub keyword found

```

#### 3220. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5791, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 10, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3221. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5799, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3222. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5800, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```
```

#### 3223. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5803, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 361. Legacy pub keyword found

```

#### 3224. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5807, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 11, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3225. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5814, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3226. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5815, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```

#### 3227. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5819, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 362. Legacy pub keyword found

```

#### 3228. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5823, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3229. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5831, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 3230. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5835, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 363. Legacy pub keyword found

```

#### 3231. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5839, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3232. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5847, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```

#### 3233. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5851, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 364. Legacy pub keyword found

```

#### 3234. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5855, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 22, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3235. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5863, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun withdraw(): @Vault {
        return <-account.load<@Vault>(from: /storage/vault)!
```

#### 3236. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5867, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 365. Legacy pub keyword found

```

#### 3237. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5871, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 26, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3238. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5879, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 3239. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5883, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 366. Legacy pub keyword found

```

#### 3240. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5887, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 64, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3241. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5895, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('---------------------------')
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```

#### 3242. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5910, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```

#### 3243. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5926, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```

#### 3244. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5942, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```

#### 3245. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5947, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 370. Legacy pub keyword found

```

#### 3246. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5951, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 74, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3247. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5959, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```

#### 3248. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5960, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```
```

#### 3249. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5963, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 371. Legacy pub keyword found

```

#### 3250. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5967, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 75, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3251. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5974, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```

#### 3252. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5975, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+fun/, 'access(all) fun'),
```

#### 3253. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5979, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 372. Legacy pub keyword found

```

#### 3254. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5983, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 84, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3255. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5991, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```

#### 3256. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5992, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```
```

#### 3257. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5995, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 373. Legacy pub keyword found

```

#### 3258. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5999, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 85, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3259. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6006, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```

#### 3260. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6007, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+/, 'access(all) '),
```

#### 3261. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6011, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 374. Legacy pub keyword found

```

#### 3262. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6015, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3263. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6023, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub struct/resource/contract" declaration found',
        suggestedFix: 'Replace "pub" with "access(all)"',
```

#### 3264. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6075, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 378. Legacy pub keyword found

```

#### 3265. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6079, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 289, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3266. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6087, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  private getPatternName(pattern: LegacyPattern): string {
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 3267. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6088, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```
```

#### 3268. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6088, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```
```

#### 3269. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6091, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 379. Legacy pub keyword found

```

#### 3270. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6095, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 290, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3271. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6102, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 3272. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6103, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3273. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6103, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3274. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6104, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3275. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6104, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3276. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6104, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3277. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6107, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 380. Legacy pub keyword found

```

#### 3278. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6111, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 290, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3279. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6118, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
```

#### 3280. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6119, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3281. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6119, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub fun')) return 'pub-function';
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3282. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6120, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3283. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6120, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3284. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6120, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```
```

#### 3285. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6123, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 381. Legacy pub keyword found

```

#### 3286. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6127, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3287. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6134, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3288. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6134, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3289. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6135, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3290. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6135, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3291. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6135, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3292. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6139, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 382. Legacy pub keyword found

```

#### 3293. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6143, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3294. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6150, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3295. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6150, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3296. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6151, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3297. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6151, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3298. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6151, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3299. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6155, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 383. Legacy pub keyword found

```

#### 3300. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6159, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3301. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6166, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3302. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6166, Column 82
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
```

#### 3303. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6167, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3304. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6167, Column 85
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3305. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6167, Column 133
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (pattern.description.includes('pub var') || pattern.description.includes('pub let')) return 'pub-variable';
    if (pattern.description.includes('pub struct') || pattern.description.includes('pub resource') || pattern.description.includes('pub contract')) return 'pub-struct-resource';
    if (pattern.description.includes('account.save')) return 'account-save';
```

#### 3306. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6171, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 384. Legacy pub keyword found

```

#### 3307. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6175, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 390, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3308. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6183, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 3309. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6203, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 386. Legacy pub keyword found

```

#### 3310. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6207, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3311. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6215, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform access modifiers from legacy to Cadence 1.0 syntax
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```

#### 3312. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6219, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 387. Legacy pub keyword found

```

#### 3313. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6223, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 20, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3314. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6231, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
   */
```

#### 3315. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6235, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 388. Legacy pub keyword found

```

#### 3316. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6239, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 28, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3317. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6247, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3318. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6251, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 389. Legacy pub keyword found

```

#### 3319. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6255, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3320. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6263, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```

#### 3321. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6264, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```
```

#### 3322. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6267, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 390. Legacy pub keyword found

```

#### 3323. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6271, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 38, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3324. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6278, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
```

#### 3325. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6279, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform standalone pub keywords
    // Only match when pub is followed by whitespace and then a Cadence keyword
    const pubPattern = /\bpub\s+(?=(?:var|let|fun|resource|struct|contract|interface|event))/g
```

#### 3326. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6283, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 391. Legacy pub keyword found

```

#### 3327. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6287, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 42, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3328. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6295, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 3329. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6310, Column 8
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```

#### 3330. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6312, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```
```

#### 3331. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6327, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3332. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6342, Column 57
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3333. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6475, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 403. Legacy pub keyword found

```

#### 3334. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6479, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 150, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3335. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6487, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 3336. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6491, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 404. Legacy pub keyword found

```

#### 3337. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6495, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3338. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6503, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub keywords
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 3339. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6507, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 405. Legacy pub keyword found

```

#### 3340. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6511, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 167, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3341. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6519, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      patterns.push('pub keyword usage')
    }
```

#### 3342. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6534, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 3343. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6571, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 409. Legacy pub keyword found

```

#### 3344. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6575, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 248, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3345. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6583, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const legacyPatterns = [
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 3346. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6587, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 410. Legacy pub keyword found

```

#### 3347. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6591, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 264, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3348. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6599, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 3349. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6599, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 3350. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6603, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 411. Legacy pub keyword found

```

#### 3351. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6607, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 264, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3352. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6615, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 3353. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6615, Column 66
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for access modifier transformations
    if (originalCode.includes('pub ') && !migratedCode.includes('pub ')) {
      transformations.push('access-modifier-transformation')
```

#### 3354. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6630, Column 73
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```

#### 3355. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6635, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 413. Legacy pub keyword found

```

#### 3356. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6639, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3357. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6647, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub keywords (should be replaced with access modifiers)
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
```

#### 3358. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6651, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 414. Legacy pub keyword found

```

#### 3359. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6655, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 167, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3360. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6663, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/.test(code)) {
      legacyPatterns.push('pub keyword usage')
    }
```

#### 3361. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6699, Column 18
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 417. Legacy pub keyword found

```

#### 3362. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6703, Column 17
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 919, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
```

#### 3363. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6711, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  // Check for legacy patterns in code (basic check)
  if (template.code.includes("pub ") || template.code.includes("pub(")) {
    issues.push("Template code contains legacy 'pub' keywords")
```

#### 3364. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6826, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3365. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6827, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 3366. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6842, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 3367. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6843, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```
```

#### 3368. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6858, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 3369. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6859, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```
```

#### 3370. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6921, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 3371. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6922, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 3372. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6923, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```
```

#### 3373. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6938, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3374. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6939, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 3375. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6953, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 3376. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6954, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 3377. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6955, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```
```

#### 3378. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6969, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 3379. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6970, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 3380. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6971, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```
```

#### 3381. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7050, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3382. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7051, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```
```

#### 3383. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7066, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3384. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7067, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```
```

#### 3385. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7098, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3386. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7099, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```
```

#### 3387. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7114, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3388. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7115, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```
```

#### 3389. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7275, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```

#### 3390. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7276, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3391. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7291, Column 20
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3392. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7436, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3393. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7467, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          pub fun getValue(): String
```

#### 3394. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7468, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 3395. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7499, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 3396. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7500, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3397. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7531, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 3398. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7532, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3399. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7563, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 3400. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7564, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 3401. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7596, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 3402. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7644, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3403. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7675, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var value: String
          fun missingAccess(): String {
```

#### 3404. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7708, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 3405. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7724, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 3406. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7740, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3407. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7756, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 3408. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7772, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3409. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7788, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 3410. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7803, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 3411. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7804, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 3412. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7805, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```
```

#### 3413. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7820, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 3414. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7852, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 3415. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7868, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 3416. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7995, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 3417. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7996, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 3418. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7997, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```
```

#### 3419. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8012, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 3420. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8027, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 3421. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8028, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 3422. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8060, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3423. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8076, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 3424. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8091, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 3425. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8108, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 3426. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8109, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```
```

#### 3427. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8124, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3428. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8140, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 3429. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8156, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3430. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8172, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 3431. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8219, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 3432. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8220, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 3433. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8235, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 3434. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8251, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 3435. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8252, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 3436. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8284, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 3437. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8315, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract Test {
          pub fun test(): String {
```

#### 3438. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8316, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 3439. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8317, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```
```

#### 3440. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8333, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
```
```

#### 3441. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8508, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3442. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8539, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 3443. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8540, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 3444. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8572, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 3445. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8588, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 3446. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8668, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 3447. Legacy pub keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8683, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 3448. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 216, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```
```

#### 3449. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 235, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 13. Legacy pub(set) keyword found

```

#### 3450. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 239, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3451. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 247, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
```

#### 3452. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 584, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub(set) var settableValue: String
```
```

#### 3453. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 875, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 53. Legacy pub(set) keyword found

```

#### 3454. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 879, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 285, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3455. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 887, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub(set) var settableValue: String
        }
```

#### 3456. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1723, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 106. Legacy pub(set) keyword found

```

#### 3457. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1727, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 52, Column 45
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3458. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1735, Column 45
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub(set) keyword', () => {
      const legacyCode = `
```

#### 3459. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1739, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 107. Legacy pub(set) keyword found

```

#### 3460. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1743, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 55, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3461. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1751, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub(set) var value: String
        }
```

#### 3462. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1755, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 108. Legacy pub(set) keyword found

```

#### 3463. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1759, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 142, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3464. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1767, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```

#### 3465. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1768, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```
```

#### 3466. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1771, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 109. Legacy pub(set) keyword found

```

#### 3467. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1775, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 143, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3468. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1782, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```

#### 3469. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1783, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPubSet)).toBe(true)
```

#### 3470. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2827, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 175. Legacy pub(set) keyword found

```

#### 3471. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2831, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3472. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2839, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```

#### 3473. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2840, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```
```

#### 3474. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2843, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 176. Legacy pub(set) keyword found

```

#### 3475. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2847, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 31, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3476. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2854, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```

#### 3477. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2855, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;

```

#### 3478. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4647, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 3479. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4664, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub(set) var value: Int
```
```

#### 3480. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4678, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 3481. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4923, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 306. Legacy pub(set) keyword found

```

#### 3482. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4927, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 25, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3483. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4935, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```

#### 3484. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4936, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```
```

#### 3485. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4939, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 307. Legacy pub(set) keyword found

```

#### 3486. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4943, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 26, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3487. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4950, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```

#### 3488. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4951, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
      const expected = 'access(all) var balance: UFix64'
```

#### 3489. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4955, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 308. Legacy pub(set) keyword found

```

#### 3490. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4959, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 53, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3491. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4967, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 3492. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4971, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 309. Legacy pub(set) keyword found

```

#### 3493. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4975, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 56, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3494. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4983, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 3495. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5579, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 347. Legacy pub(set) keyword found

```

#### 3496. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5583, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 49, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3497. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5591, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub(set) keyword found',
      suggestedFix: 'Replace with access(all) and implement setter restrictions',
```

#### 3498. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5659, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 352. Legacy pub(set) keyword found

```

#### 3499. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5663, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 23, Column 29
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3500. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5671, Column 29
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'access(all) ',
      description: 'Convert pub(set) to access(all) with setter restrictions',
      category: 'access-modifier'
```

#### 3501. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5816, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
```
```

#### 3502. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5896, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
```
```

#### 3503. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5899, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 367. Legacy pub(set) keyword found

```

#### 3504. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5903, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 12, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3505. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5911, Column 5
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub var totalSupply: UFix64
    pub(set) var owner: Address
    
```

#### 3506. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5915, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 368. Legacy pub(set) keyword found

```

#### 3507. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5919, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 65, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3508. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5927, Column 20
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    console.log('✓ pub → access(all)')
    console.log('✓ pub(set) → access(all)')
    console.log('✓ Interface1, Interface2 → Interface1 & Interface2')
```

#### 3509. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6216, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
```
```

#### 3510. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6230, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```

#### 3511. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6247, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3512. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6248, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```
```

#### 3513. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6299, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 392. Legacy pub(set) keyword found

```

#### 3514. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6303, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3515. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6311, Column 8
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - pub -> access(all)
   * - pub(set) -> access(all) with appropriate setter patterns
   * Only transforms pub keywords that are actual Cadence keywords, not in comments or strings
```

#### 3516. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6315, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 393. Legacy pub(set) keyword found

```

#### 3517. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6319, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 28, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3518. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6327, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3519. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6328, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```
```

#### 3520. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6331, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 394. Legacy pub(set) keyword found

```

#### 3521. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6335, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 29, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3522. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6342, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
```

#### 3523. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6343, Column 24
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Transform pub(set) first to avoid conflicts with pub transformation
    // Only match when pub(set) is followed by whitespace and then a Cadence keyword
    const pubSetPattern = /\bpub\(set\)\s+(?=(?:var|let|fun|resource|struct|contract|interface))/g
```

#### 3524. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6347, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 395. Legacy pub(set) keyword found

```

#### 3525. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6351, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 33, Column 38
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3526. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6359, Column 38
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      transformationCount++
      this.logger.debug('Transformed pub(set) to access(all)', { original: match.trim() })
      return 'access(all) '
```

#### 3527. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6488, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```
```

#### 3528. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6523, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 406. Legacy pub(set) keyword found

```

#### 3529. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6527, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 151, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3530. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6535, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 3531. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6539, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 407. Legacy pub(set) keyword found

```

#### 3532. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6543, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 170, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3533. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6551, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 3534. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6555, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 408. Legacy pub(set) keyword found

```

#### 3535. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6559, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 172, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3536. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6567, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      patterns.push('pub(set) keyword usage')
    }
```

#### 3537. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6584, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
```
```

#### 3538. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6619, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 412. Legacy pub(set) keyword found

```

#### 3539. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6623, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 249, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3540. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6631, Column 30
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      /\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/,  // pub keyword
      /\bpub\(set\)\s+/,  // pub(set) keyword
      /:\s*[^{]+,\s*[^{]+\s*\{/,  // comma-separated interface conformance
```

#### 3541. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6667, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 415. Legacy pub(set) keyword found

```

#### 3542. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6671, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 170, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3543. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6679, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check for pub(set) keywords
    if (/\bpub\(set\)\s+/.test(code)) {
```

#### 3544. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6683, Column 18
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 416. Legacy pub(set) keyword found

```

#### 3545. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6687, Column 17
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 172, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
```

#### 3546. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6695, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (/\bpub\(set\)\s+/.test(code)) {
      legacyPatterns.push('pub(set) keyword usage')
    }
```

#### 3547. Legacy pub(set) keyword found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8283, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 3548. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 107, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 5. Legacy account.save() found

```

#### 3549. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 111, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 141, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3550. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 119, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Storage API Modernization
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```

#### 3551. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 134, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```

#### 3552. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 155, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 8. Legacy account.save() found

```

#### 3553. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 159, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 25, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3554. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 167, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- [x] 2.3 Create storage API transformer
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```

#### 3555. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 182, Column 43
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```

#### 3556. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 251, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 14. Legacy account.save() found

```

#### 3557. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 255, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 107, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3558. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 263, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```

#### 3559. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 278, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```

#### 3560. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 891, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 54. Legacy account.save() found

```

#### 3561. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 895, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 143, Column 39
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3562. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 903, Column 39
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(result).toContain('account.storage.save')
        expect(result).not.toContain('account.save(')
      }
```

#### 3563. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1320, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```
```

#### 3564. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1355, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 83. Legacy account.save() found

```

#### 3565. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1359, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 205, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3566. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1367, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
              let ref = account.borrow<&TestResource>(from: /storage/test)
```

#### 3567. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1787, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 110. Legacy account.save() found

```

#### 3568. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1791, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 69, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3569. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1799, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 3570. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1803, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 111. Legacy account.save() found

```

#### 3571. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1807, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 175, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3572. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1815, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3573. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1819, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 112. Legacy account.save() found

```

#### 3574. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1823, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 207, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3575. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1831, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 3576. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1835, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 113. Legacy account.save() found

```

#### 3577. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1839, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 247, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3578. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1847, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3579. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1851, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 114. Legacy account.save() found

```

#### 3580. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1855, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 310, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3581. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1863, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3582. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2408, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 3583. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2680, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```
```

#### 3584. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2728, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```
```

#### 3585. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2744, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
```
```

#### 3586. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2859, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 177. Legacy account.save() found

```

#### 3587. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2863, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 59, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3588. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2871, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        account.save(<-vault, to: /storage/vault)
        let vault <- account.load<@Vault>(from: /storage/vault)
```

#### 3589. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2875, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 178. Legacy account.save() found

```

#### 3590. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2879, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 82, Column 21
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3591. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2887, Column 21
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('categorizes storage patterns correctly', () => {
      const code = `account.save(<-vault, to: /storage/vault)`;

```

#### 3592. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2891, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 179. Legacy account.save() found

```

#### 3593. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2895, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 114, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3594. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2903, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```

#### 3595. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2907, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 180. Legacy account.save() found

```

#### 3596. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2911, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 169, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3597. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2919, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 3598. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2923, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 181. Legacy account.save() found

```

#### 3599. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2927, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 196, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3600. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2935, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 3601. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2939, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 182. Legacy account.save() found

```

#### 3602. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2943, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 234, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3603. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2951, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
            }
```

#### 3604. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2955, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 183. Legacy account.save() found

```

#### 3605. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2959, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 251, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3606. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2967, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 3607. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2971, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 184. Legacy account.save() found

```

#### 3608. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2975, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 260, Column 66
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3609. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2983, Column 66
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const savePattern = patterns.find(p => p.originalText === 'account.save(');
      expect(savePattern?.location.line).toBe(3);
```

#### 3610. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4056, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```
```

#### 3611. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4232, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 3612. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4328, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```
```

#### 3613. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4344, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
```
```

#### 3614. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4347, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 270. Legacy account.save() found

```

#### 3615. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4351, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 62, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3616. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4359, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 3617. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4363, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 271. Legacy account.save() found

```

#### 3618. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4367, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3619. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4375, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 3620. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4379, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 272. Legacy account.save() found

```

#### 3621. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4383, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 145, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3622. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4391, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            self.balance = self.balance + amount
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 3623. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4395, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 273. Legacy account.save() found

```

#### 3624. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4399, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 215, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3625. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4407, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 3626. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4411, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 274. Legacy account.save() found

```

#### 3627. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4415, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 308, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3628. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4423, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
            }
```

#### 3629. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4427, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 275. Legacy account.save() found

```

#### 3630. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4431, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 327, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3631. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4439, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 3632. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4856, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```
```

#### 3633. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4987, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 310. Legacy account.save() found

```

#### 3634. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4991, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 256, Column 22
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3635. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4999, Column 22
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms account.save to account.storage.save', () => {
      const input = 'account.save(vault, to: /storage/vault)'
      const expected = 'account.storage.save(vault, to: /storage/vault)'
```

#### 3636. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5003, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 311. Legacy account.save() found

```

#### 3637. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5007, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 285, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3638. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5015, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        account.save(vault, to: /storage/vault)
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 3639. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5019, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 312. Legacy account.save() found

```

#### 3640. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5023, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 322, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3641. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5031, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
          }
```

#### 3642. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5435, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 338. Legacy account.save() found

```

#### 3643. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5439, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 99, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3644. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5447, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3645. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5451, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 339. Legacy account.save() found

```

#### 3646. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5455, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 172, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3647. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5463, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3648. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5467, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 340. Legacy account.save() found

```

#### 3649. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5471, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 207, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3650. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5479, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 3651. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5483, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 341. Legacy account.save() found

```

#### 3652. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5487, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 266, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3653. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5495, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('account.capabilities')
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```

#### 3654. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5499, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 342. Legacy account.save() found

```

#### 3655. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5503, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 311, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3656. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5511, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 3657. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5526, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```

#### 3658. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5595, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 348. Legacy account.save() found

```

#### 3659. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5599, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 59, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3660. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5607, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.save() found',
      suggestedFix: 'Replace with account.storage.save()',
```

#### 3661. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5675, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 353. Legacy account.save() found

```

#### 3662. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5679, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 39, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3663. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5687, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'account.storage.save(',
      description: 'Update account.save() to account.storage.save()',
      category: 'storage'
```

#### 3664. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5771, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 359. Legacy account.save() found

```

#### 3665. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5775, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 25, Column 11
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3666. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5783, Column 11
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          self.balance = balance
          account.save(<-create Vault(balance: balance), to: /storage/vault)
        }
```

#### 3667. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5848, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
```
```

#### 3668. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5931, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 369. Legacy account.save() found

```

#### 3669. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5935, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3670. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5943, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub fun deposit(vault: @Vault) {
        account.save(<-vault, to: /storage/vault)
    }
```

#### 3671. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6027, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 375. Legacy account.save() found

```

#### 3672. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6031, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 118, Column 30
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3673. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6039, Column 30
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.save() API found',
        suggestedFix: 'Replace with account.storage.save()',
```

#### 3674. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6187, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 385. Legacy account.save() found

```

#### 3675. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6191, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 396, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3676. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6199, Column 16
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('storage-api', {
      before: 'account.save(<-vault, to: /storage/vault)',
      after: 'account.storage.save(<-vault, to: /storage/vault)',
```

#### 3677. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6363, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 396. Legacy account.save() found

```

#### 3678. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6367, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 103, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3679. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6375, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform storage API calls to modern capability-based patterns
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```

#### 3680. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6379, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 397. Legacy account.save() found

```

#### 3681. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6383, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 113, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3682. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6391, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.save() to account.storage.save()
    const savePattern = /\baccount\.save\b/g
```

#### 3683. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6406, Column 8
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```

#### 3684. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6715, Column 18
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 418. Legacy account.save() found

```

#### 3685. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6719, Column 17
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
```

#### 3686. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6727, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use "AuthAccount" - use modern account access patterns
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```

#### 3687. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6742, Column 37
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```

#### 3688. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6905, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```

#### 3689. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7773, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 3690. Legacy account.save() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8157, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```
```

#### 3691. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 120, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
```
```

#### 3692. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 123, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 6. Legacy account.link() found

```

#### 3693. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 127, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 142, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3694. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 135, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.save()` → `account.storage.save()`
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```

#### 3695. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 150, Column 4
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```

#### 3696. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 168, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
```
```

#### 3697. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 171, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 9. Legacy account.link() found

```

#### 3698. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 175, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 26, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3699. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 183, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Implement transformation from legacy `account.save()` to `account.storage.save()`
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```

#### 3700. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 198, Column 16
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```

#### 3701. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5496, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```
```

#### 3702. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5515, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 343. Legacy account.link() found

```

#### 3703. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5519, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 267, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3704. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5527, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```

#### 3705. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5542, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```

#### 3706. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5611, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 349. Legacy account.link() found

```

#### 3707. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5615, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 67, Column 28
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3708. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5623, Column 28
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.link() found',
      suggestedFix: 'Replace with account.capabilities.storage.issue() + account.capabilities.publish()',
```

#### 3709. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6043, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 376. Legacy account.link() found

```

#### 3710. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6047, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 148, Column 30
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3711. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6055, Column 30
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.link() API found',
        suggestedFix: 'Replace with modern capability-based pattern',
```

#### 3712. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6376, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
```
```

#### 3713. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6395, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 398. Legacy account.link() found

```

#### 3714. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6399, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 104, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3715. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6407, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.save() -> account.storage.save()
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```

#### 3716. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6411, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 399. Legacy account.link() found

```

#### 3717. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6415, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 145, Column 14
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3718. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6423, Column 14
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Note: account.link() transformation is more complex and may require
    // manual intervention as it involves capability patterns
```

#### 3719. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6427, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 400. Legacy account.link() found

```

#### 3720. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6431, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 150, Column 31
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3721. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6439, Column 31
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    if (linkMatches) {
      this.logger.warn('Found account.link() calls that require manual migration to capability patterns', {
        count: linkMatches.length
```

#### 3722. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6454, Column 8
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```

#### 3723. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6728, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
```
```

#### 3724. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6731, Column 18
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 419. Legacy account.link() found

```

#### 3725. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6735, Column 17
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
```

#### 3726. Legacy account.link() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6743, Column 13
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- NEVER use legacy storage API like account.save() - use account.storage.save()
- NEVER use account.link() - use account.capabilities.storage.issue()
- Use proper access modifiers: access(all), access(self), access(contract), access(account)
```

#### 3727. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 136, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
```
```

#### 3728. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 139, Column 16
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 7. Legacy account.borrow() found

```

#### 3729. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 143, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 143, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3730. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 151, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- `account.link()` → `account.capabilities.storage.issue()` + `account.capabilities.publish()`
- `account.borrow()` → `account.capabilities.borrow()`
- Capability-based access pattern updates
```

#### 3731. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 184, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
```
```

#### 3732. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 187, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 10. Legacy account.borrow() found

```

#### 3733. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 191, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 27, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3734. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 199, Column 13
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  - Transform `account.link()` to modern capability-based patterns
  - Update `account.borrow()` to `account.capabilities.borrow()`
  - Write unit tests for storage API transformations
```

#### 3735. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 230, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```

#### 3736. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 264, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```
```

#### 3737. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 267, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 15. Legacy account.borrow() found

```

#### 3738. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 271, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 108, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3739. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 279, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```

#### 3740. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5528, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```
```

#### 3741. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5531, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 344. Legacy account.borrow() found

```

#### 3742. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5535, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 268, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3743. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5543, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
    })
```

#### 3744. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5627, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 350. Legacy account.borrow() found

```

#### 3745. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5631, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 75, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3746. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5639, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.borrow() found',
      suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 3747. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5691, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 354. Legacy account.borrow() found

```

#### 3748. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5695, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 45, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3749. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5703, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      replacement: 'account.capabilities.borrow(',
      description: 'Update account.borrow() to account.capabilities.borrow()',
      category: 'storage'
```

#### 3750. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6059, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 377. Legacy account.borrow() found

```

#### 3751. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6063, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 138, Column 30
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3752. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6071, Column 30
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.borrow() API found',
        suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 3753. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6408, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
```
```

#### 3754. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6443, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 401. Legacy account.borrow() found

```

#### 3755. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6447, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3756. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6455, Column 8
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * - account.link() -> account.capabilities.storage.issue() + account.capabilities.publish()
   * - account.borrow() -> account.capabilities.borrow()
   */
```

#### 3757. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6459, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 402. Legacy account.borrow() found

```

#### 3758. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6463, Column 17
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 129, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
```

#### 3759. Legacy account.borrow() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6471, Column 18
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.borrow() to account.storage.borrow()
    const borrowPattern = /\baccount\.borrow\b/g
```

#### 3760. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7070, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 21. Legacy account.load() found

```

#### 3761. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7074, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 83, Column 28
- **Pattern**: `account.load(`
- **Type**: storage-api
```

#### 3762. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7082, Column 28
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'warning' as const,
      description: 'Legacy account.load() found',
      suggestedFix: 'Replace with account.storage.load()',
```

#### 3763. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7118, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 24. Legacy account.load() found

```

#### 3764. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7122, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 128, Column 30
- **Pattern**: `account.load(`
- **Type**: storage-api
```

#### 3765. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7130, Column 30
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.load() API found',
        suggestedFix: 'Replace with account.storage.load()',
```

#### 3766. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7150, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

#### 26. Legacy account.load() found

```

#### 3767. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7154, Column 17
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 121, Column 18
- **Pattern**: `account.load(`
- **Type**: storage-api
```

#### 3768. Legacy account.load() found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7162, Column 18
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Transform account.load() to account.storage.load()
    const loadPattern = /\baccount\.load\b/g
```

#### 3769. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1159, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3770. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1174, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3771. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1207, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 3772. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1222, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 3773. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1303, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 3774. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1318, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 3775. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2696, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 3776. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2711, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 3777. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2726, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 3778. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2904, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```
```

#### 3779. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3351, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3780. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3366, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3781. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3896, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```
```

#### 3782. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3911, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 3783. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3926, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 3784. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4296, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```
```

#### 3785. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4311, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 3786. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4326, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 3787. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4823, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3788. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4838, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3789. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5079, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3790. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5094, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3791. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5751, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3792. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5766, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3793. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5799, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3794. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5814, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3795. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6753, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 136, Column 12
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3796. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6761, Column 12
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
#### Interface Conformance Updates
- `Resource: Interface1, Interface2` → `Resource: Interface1 & Interface2`
- Multiple interface inheritance syntax modernization
```

#### 3797. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6769, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 251, Column 34
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3798. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6777, Column 34
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms interface conformance', () => {
    const input = 'resource Vault: Provider, Receiver'
    const expected = 'resource Vault: Provider & Receiver'
```

#### 3799. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6785, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 17, Column 114
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3800. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6793, Column 114
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
2. WHEN processing function signatures THEN the system SHALL update parameter and return type syntax to Cadence 1.0 format
3. WHEN handling resource interfaces THEN the system SHALL update interface conformance syntax from `ResourceName: Interface1, Interface2` to `ResourceName: Interface1 & Interface2`
4. WHEN processing import statements THEN the system SHALL maintain compatibility with Flow blockchain standard contracts
```

#### 3801. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6801, Column 17
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 158, Column 18
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
```

#### 3802. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6810, Column 18
- **Pattern**: `: CalendarDayButton,
        WeekNumber`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        },
        DayButton: CalendarDayButton,
        WeekNumber: ({ children, ...props }) => {
```

#### 3803. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6818, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 18, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3804. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6826, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3805. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6834, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 106, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3806. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6842, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 3807. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6850, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 203, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3808. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6858, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 3809. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6866, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 86, Column 42
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3810. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6874, Column 42
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        access(all) resource TestResource: Interface1, Interface2 {
          // resource implementation
```

#### 3811. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6882, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 96, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3812. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6890, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        resource Vault: Provider, Receiver, Balance {
          // implementation
```

#### 3813. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6898, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 115, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3814. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6906, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
      `;
```

#### 3815. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6914, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 232, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3816. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6922, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 3817. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6930, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 238, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3818. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6938, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3819. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6946, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 105, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3820. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6954, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 3821. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6962, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 306, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3822. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6970, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 3823. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6978, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 125, Column 36
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3824. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6986, Column 36
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms comma-separated interfaces to ampersand-separated', () => {
      const input = 'resource Vault: Provider, Receiver {'
      const expected = 'resource Vault: Provider & Receiver {'
```

#### 3825. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6994, Column 17
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 132, Column 34
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
```

#### 3826. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7002, Column 34
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms multiple interfaces with spaces', () => {
      const input = 'struct Token: Fungible, Mintable, Burnable {'
      const expected = 'struct Token: Fungible & Mintable & Burnable {'
```

#### 3827. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7010, Column 17
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 139, Column 41
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
```

#### 3828. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7018, Column 41
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles contract interface conformance', () => {
      const input = 'contract MyContract: ContractInterface, AnotherInterface {'
      const expected = 'contract MyContract: ContractInterface & AnotherInterface {'
```

#### 3829. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7026, Column 17
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 152, Column 36
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
```

#### 3830. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7034, Column 36
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles interfaces with extra whitespace', () => {
      const input = 'resource Vault:  Provider,  Receiver,  Balance  {'
      const expected = 'resource Vault: Provider & Receiver & Balance {'
```

#### 3831. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7042, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 318, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3832. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7050, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 3833. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7058, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 177, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3834. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7066, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 3835. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7090, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 20, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3836. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7098, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 3837. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7106, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 10, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3838. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7114, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 3839. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7138, Column 17
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 402, Column 30
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
```

#### 3840. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7146, Column 30
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('interface-conformance', {
      before: 'resource Vault: Provider, Receiver, Balance',
      after: 'resource Vault: Provider & Receiver & Balance',
```

#### 3841. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7170, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 55, Column 16
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3842. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7178, Column 16
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
   * Transform interface conformance from comma-separated to ampersand-separated
   * - Resource: Interface1, Interface2 -> Resource: Interface1 & Interface2
   */
```

#### 3843. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7186, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 64, Column 37
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3844. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7194, Column 37
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Pattern to match interface conformance with comma separation
    // Matches: resource/struct Name: Interface1, Interface2, Interface3
    const interfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+?)(\s*\{)/g
```

#### 3845. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7202, Column 17
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 176, Column 45
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
```

#### 3846. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7210, Column 45
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Check for comma-separated interface conformance
    // Look for patterns like "resource Name: Interface1, Interface2 {" but exclude type annotations like "&{Interface}"
    const commaInterfacePattern = /(\b(?:resource|struct|contract)\s+\w+\s*:\s*)([^{]+,\s*[^{]+)(\s*\{)/
```

#### 3847. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7218, Column 17
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 109, Column 28
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
```

#### 3848. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7226, Column 28
- **Pattern**: `: NonFungibleToken, FungibleToken`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Use modern storage API: account.storage.save(), account.capabilities.storage.issue()
- Import standard contracts: NonFungibleToken, FungibleToken, ViewResolver, MetadataViews
- Use view functions for read-only operations: access(all) view fun
```

#### 3849. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7234, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 114, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
```

#### 3850. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7242, Column 17
- **Pattern**: `: Display, Serial`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Implement ALL views listed in getViews() in the resolveView() function
- Required views: Display, Serial, Traits, Royalties, ExternalURL, NFTCollectionData, NFTCollectionDisplay
- Never return nil for declared views - provide complete implementations
```

#### 3851. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7250, Column 17
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 124, Column 31
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
```

#### 3852. Legacy interface conformance syntax found (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7258, Column 31
- **Pattern**: `: StoragePath, PublicPath`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- Remove redundant createEmptyCollection from NFT resource
- Use structured storage paths: StoragePath, PublicPath, PrivatePath

```

#### 3853. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 56, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
```
```

#### 3854. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 71, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3855. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 72, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```
```

#### 3856. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 455, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3857. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 536, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
```
```

#### 3858. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 551, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 3859. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1032, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 3860. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1047, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3861. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1128, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
```
```

#### 3862. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1143, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3863. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1256, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```
```

#### 3864. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1271, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 3865. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1351, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 3866. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1431, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3867. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 1688, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          fun missingAccess(): String {
```
```

#### 3868. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2135, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 3869. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2167, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 3870. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2311, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3871. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2375, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 3872. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2407, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3873. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2423, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 3874. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2456, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```
```

#### 3875. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2471, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 3876. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2486, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 3877. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2503, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 3878. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 2902, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3879. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3031, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 3880. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3591, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 3881. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3720, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```
```

#### 3882. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3735, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 3883. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3750, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 3884. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3815, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 3885. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3944, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
```
```

#### 3886. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 3959, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 3887. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4007, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3888. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4023, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 3889. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4024, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```
```

#### 3890. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4039, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 3891. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4054, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 3892. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4071, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3893. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4183, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 3894. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4231, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3895. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4247, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 3896. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4406, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3897. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4552, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```
```

#### 3898. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4567, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 3899. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4568, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```
```

#### 3900. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4616, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```
```

#### 3901. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4631, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 3902. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4679, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 3903. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4728, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
```
```

#### 3904. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4743, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 3905. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4758, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          pub fun test(): String {
            return "This contains pub in string"
```

#### 3906. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4790, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
```

#### 3907. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 4984, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
```
```

#### 3908. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5223, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3909. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5720, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
```
```

#### 3910. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5735, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 3911. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5831, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 3912. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 5879, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 3913. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6183, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 3914. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 6184, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```
```

#### 3915. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7268, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 245, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3916. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7276, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  test('transforms pub to access(all)', () => {
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3917. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7277, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```
```

#### 3918. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7284, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 246, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3919. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7291, Column 24
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
```

#### 3920. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7292, Column 35
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    const input = 'pub fun getValue(): String'
    const expected = 'access(all) fun getValue(): String'
    expect(transformer.transformAccessModifiers(input)).toBe(expected)
```

#### 3921. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7300, Column 17
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 16, Column 13
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
```

#### 3922. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7308, Column 13
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 3923. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7316, Column 17
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 183, Column 42
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
```

#### 3924. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7324, Column 42
- **Pattern**: `fun main(address: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): UFix64 {
    let account = getAccount(address)
```

#### 3925. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7332, Column 17
- **Pattern**: `fun main(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 197, Column 42
- **Pattern**: `fun main(): U`
- **Type**: function-signature
```

#### 3926. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7340, Column 42
- **Pattern**: `fun main(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(): UInt64 {
    return getCurrentBlock().height
```

#### 3927. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7348, Column 17
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 210, Column 42
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
```

#### 3928. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7356, Column 42
- **Pattern**: `fun main(address: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
                onClick={() =>
                  setScript(`access(all) fun main(address: Address): String {
    let account = getAccount(address)
```

#### 3929. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7396, Column 17
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 37, Column 22
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
```

#### 3930. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7404, Column 22
- **Pattern**: `fun hello(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun hello(): String {
        return self.greeting
```

#### 3931. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7412, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 192, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3932. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7420, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 3933. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7428, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 213, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3934. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7436, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3935. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7444, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 235, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3936. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7452, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.modernVar
```

#### 3937. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7460, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 270, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3938. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7468, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 3939. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7476, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 594, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3940. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7484, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 3941. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7492, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3942. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7500, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3943. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7508, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 38, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3944. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7516, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 3945. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7524, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3946. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7532, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 3947. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7540, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 30, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3948. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7548, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 3949. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7556, Column 17
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 197, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
```

#### 3950. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7564, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 3951. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7572, Column 17
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 228, Column 23
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
```

#### 3952. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7580, Column 23
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun publicFunction(): String {
            return "test"
```

#### 3953. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7588, Column 17
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 273, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
```

#### 3954. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7596, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 3955. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7604, Column 17
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 277, Column 23
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
```

#### 3956. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7612, Column 23
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun modernFunction(): String {
            return "modern"
```

#### 3957. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7620, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 17, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3958. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7628, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 3959. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7636, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 39, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3960. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7644, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 3961. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7652, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 281, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3962. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7660, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 3963. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7668, Column 17
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 349, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
```

#### 3964. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7676, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          fun missingAccess(): String {
            return account.borrow<&String>(from: /storage/data)?.value ?? ""
```

#### 3965. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7700, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 71, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
```

#### 3966. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7708, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 3967. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7716, Column 17
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 98, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
```

#### 3968. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7724, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 3969. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7732, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3970. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7740, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 3971. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7748, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 43, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3972. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7756, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 3973. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7764, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 113, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3974. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7772, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 3975. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7780, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 138, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3976. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7788, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 3977. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7796, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 165, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3978. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7804, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 3979. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7812, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 183, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3980. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7820, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 3981. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7828, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 217, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3982. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7836, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun getValue(): String {
            return "modern"
```

#### 3983. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7844, Column 17
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 130, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
```

#### 3984. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7852, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 3985. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7860, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 477, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 3986. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7868, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 3987. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7876, Column 17
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 174, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
```

#### 3988. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7884, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getOwners(): [Address]')
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```

#### 3989. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7885, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```
```

#### 3990. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7892, Column 17
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 175, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
```

#### 3991. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7899, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```

#### 3992. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7900, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```

#### 3993. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7901, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```
```

#### 3994. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7908, Column 17
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 176, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
```

#### 3995. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7915, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```

#### 3996. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7916, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```

#### 3997. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7917, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```
```

#### 3998. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7924, Column 17
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 177, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
```

#### 3999. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7931, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```

#### 4000. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7932, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```

#### 4001. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7933, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```
```

#### 4002. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7940, Column 17
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 178, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
```

#### 4003. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7947, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```

#### 4004. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7948, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')

```

#### 4005. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7956, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 295, Column 31
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
```

#### 4006. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7964, Column 31
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Original core functions should still exist with same signatures
      expect(code).toContain('fun submitTransaction(to: Address, amount: UFix64): UInt64')
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
```

#### 4007. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7972, Column 17
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 297, Column 31
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
```

#### 4008. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7980, Column 31
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
      expect(code).toContain('fun getTransaction(txId: UInt64): Transaction?')

```

#### 4009. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7988, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4010. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7996, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 4011. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8004, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 66, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4012. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8012, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4013. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8020, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 15, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4014. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8028, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 4015. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8036, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 43, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4016. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8044, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) view fun getValue(): String {
            return "modern"
```

#### 4017. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8052, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 59, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4018. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8060, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4019. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8068, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 83, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
```

#### 4020. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8076, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 4021. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8077, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```
```

#### 4022. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8084, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 84, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
```

#### 4023. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8091, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 4024. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8092, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';

```

#### 4025. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8100, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 103, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4026. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8108, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 4027. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8116, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 122, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4028. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8124, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4029. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8132, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 164, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
```

#### 4030. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8140, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 4031. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8148, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 214, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4032. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8156, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 4033. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8164, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 253, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4034. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8172, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 4035. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8180, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 262, Column 9
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4036. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8188, Column 9
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This might be a function
        fun getValue(): String { return "test" }
      `;
```

#### 4037. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8196, Column 17
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 73, Column 38
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
```

#### 4038. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8204, Column 38
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify core staking logic is preserved
    expect(template.code).toContain('fun calculateReward(user: Address): UFix64')
    expect(template.code).toContain('timeStaked = getCurrentBlock().timestamp - stakeInfo.stakedAt')
```

#### 4039. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8212, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 19, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4040. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8220, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 4041. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8221, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```
```

#### 4042. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8228, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 20, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4043. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8235, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 4044. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8236, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
      const result = transformer.transformAccessModifiers(input)
```

#### 4045. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8244, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 36, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
```

#### 4046. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8252, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 4047. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8260, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 44, Column 23
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
```

#### 4048. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8268, Column 23
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var totalSupply: UFix64
          access(all) fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 4049. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8276, Column 17
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 57, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
```

#### 4050. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8284, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 4051. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8292, Column 17
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 63, Column 23
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
```

#### 4052. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8300, Column 23
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var value: Int
          access(all) fun getValue(): Int
        }
```

#### 4053. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8308, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 86, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
```

#### 4054. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8316, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 4055. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8324, Column 17
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 94, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
```

#### 4056. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8332, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          access(all) fun test(): String {
            return "This contains pub in string"
```

#### 4057. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8340, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 187, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4058. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8348, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to getter functions', () => {
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```

#### 4059. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8349, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```
```

#### 4060. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8356, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 188, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4061. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8363, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```

#### 4062. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8364, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4063. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8372, Column 17
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 194, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
```

#### 4064. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8380, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to read functions', () => {
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```

#### 4065. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8381, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```
```

#### 4066. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8388, Column 17
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 195, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
```

#### 4067. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8395, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```

#### 4068. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8396, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4069. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8404, Column 17
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 201, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
```

#### 4070. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8412, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to check functions', () => {
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```

#### 4071. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8413, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```
```

#### 4072. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8420, Column 17
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 202, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
```

#### 4073. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8427, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```

#### 4074. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8428, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4075. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8436, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 208, Column 39
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4076. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8444, Column 39
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('preserves existing view modifier', () => {
      const input = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4077. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8452, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 233, Column 21
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4078. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8460, Column 21
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        access(all) fun getValue(): String {
          return self.value
```

#### 4079. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8468, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 241, Column 21
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
```

#### 4080. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8476, Column 21
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        
        access(all) fun getBalance(): UFix64 {
          return self.balance
```

#### 4081. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8484, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4082. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8492, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4083. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8500, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 56, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4084. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8508, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4085. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8516, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 278, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4086. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8524, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4087. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8532, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 16, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4088. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8540, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 4089. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8548, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 34, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4090. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8556, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract ModernContract {
      access(all) fun getValue(): String {
        return "modern"
```

#### 4091. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8564, Column 17
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
```

#### 4092. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8572, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 4093. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8580, Column 17
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 26, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
```

#### 4094. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8588, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 4095. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8596, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 42, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4096. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8604, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 4097. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8612, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 44, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
```

#### 4098. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8620, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 4099. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8628, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 145, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4100. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8636, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4101. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8644, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 153, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
```

#### 4102. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8652, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 4103. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8660, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 390, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4104. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8668, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 4105. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8669, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```
```

#### 4106. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8676, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 391, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4107. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8683, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 4108. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8684, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
      description: 'Replace "pub" with explicit access control'
```

#### 4109. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8692, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 408, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
```

#### 4110. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8700, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    this.exampleDatabase.set('function-signature', {
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```

#### 4111. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8701, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```
```

#### 4112. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8708, Column 17
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 409, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
```

#### 4113. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8715, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```

#### 4114. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8716, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
      description: 'Add view modifier for read-only functions'
```

#### 4115. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8724, Column 17
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 86, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
```

#### 4116. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8732, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 4117. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8740, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 368, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4118. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8748, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 4119. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8756, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 370, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
```

#### 4120. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8764, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 4121. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8772, Column 17
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 471, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
```

#### 4122. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8780, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4123. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8788, Column 17
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 479, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
```

#### 4124. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8796, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 4125. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8804, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 579, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
```

#### 4126. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8812, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 4127. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8820, Column 17
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 606, Column 22
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
```

#### 4128. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8828, Column 22
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 4129. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8836, Column 17
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 699, Column 22
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
```

#### 4130. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8844, Column 22
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun calculateReward(user: Address): UFix64 {
        if self.stakes[user] == nil {
```

#### 4131. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8852, Column 17
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 710, Column 22
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
```

#### 4132. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8860, Column 22
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getStakeInfo(user: Address): StakeInfo? {
        return self.stakes[user]
```

#### 4133. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8868, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 776, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
```

#### 4134. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8876, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) fun submitTransaction(to: Address, amount: UFix64): UInt64 {
        let tx = Transaction(id: self.transactionCount, to: to, amount: amount)
```

#### 4135. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8884, Column 17
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 810, Column 22
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
```

#### 4136. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8892, Column 22
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getTransaction(txId: UInt64): Transaction? {
        return self.transactions[txId]
```

#### 4137. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8900, Column 17
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 818, Column 22
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
```

#### 4138. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8908, Column 22
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getRequiredConfirmations(): UInt64 {
        return self.required
```

#### 4139. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8916, Column 17
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 822, Column 22
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
```

#### 4140. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8924, Column 22
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getTransactionCount(): UInt64 {
        return self.transactionCount
```

#### 4141. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8932, Column 17
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 826, Column 22
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
```

#### 4142. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8940, Column 22
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun isOwner(address: Address): Bool {
        return self.owners.contains(address)
```

#### 4143. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8948, Column 17
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 830, Column 22
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
```

#### 4144. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8956, Column 22
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun getConfirmationCount(txId: UInt64): UInt64 {
        if let tx = self.transactions[txId] {
```

#### 4145. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8964, Column 17
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 837, Column 22
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
```

#### 4146. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8972, Column 22
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    access(all) view fun hasConfirmed(txId: UInt64, owner: Address): Bool {
        if let confirmationMap = self.confirmations[txId] {
```

#### 4147. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8980, Column 17
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 291, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
```

#### 4148. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8988, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        
        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 4149. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 8996, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 569, Column 22
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
```

#### 4150. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9004, Column 22
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Public read-only functions
    access(all) view fun getValue(): String {
        return self.value
```

#### 4151. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9012, Column 17
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 573, Column 22
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
```

#### 4152. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9020, Column 22
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    access(all) view fun getCounter(): UInt64 {
        return self.counter
```

#### 4153. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9028, Column 17
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 589, Column 22
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
```

#### 4154. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9036, Column 22
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Public utility functions
    access(all) view fun getValueLength(): Int {
        return self.value.length
```

#### 4155. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9044, Column 17
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 593, Column 22
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
```

#### 4156. Potential legacy function signature (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 9052, Column 22
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    
    access(all) view fun isCounterEven(): Bool {
        return self.counter % 2 == 0
```

#### 4157. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7364, Column 17
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 14, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
```

#### 4158. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7372, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const [script, setScript] = useState(`// Example: Query account balance
import FungibleToken from 0x9a0766d93b6608b7

```

#### 4159. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7380, Column 17
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 21, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
```

#### 4160. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7388, Column 1
- **Pattern**: `import FungibleToken from 0x9a0766d93b6608b7`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const [transaction, setTransaction] = useState(`// Example: Transfer tokens
import FungibleToken from 0x9a0766d93b6608b7

```

#### 4161. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7684, Column 17
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
- **Location**: Line 375, Column 9
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
```

#### 4162. Import statement with hardcoded address (in documentation context)

- **File**: legacy-patterns-report.md
- **Location**: Line 7692, Column 9
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithHardcodedAddress = `
        import NonFungibleToken from 0x1d7e57aa55817448
        
```

#### 4163. Legacy pub keyword found (in documentation context)

- **File**: lib/migration/README.md
- **Location**: Line 104, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
|---------|---------------|----------|
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
```

#### 4164. Legacy pub keyword found (in documentation context)

- **File**: lib/migration/README.md
- **Location**: Line 109, Column 4
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |

```

#### 4165. Legacy pub(set) keyword found (in documentation context)

- **File**: lib/migration/README.md
- **Location**: Line 105, Column 4
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `pub ` | `access(all) ` | access-modifier |
| `pub(set) ` | `access(all) ` | access-modifier |
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
```

#### 4166. Legacy account.save() found (in documentation context)

- **File**: lib/migration/README.md
- **Location**: Line 107, Column 4
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `Interface1, Interface2` | `Interface1 & Interface2` | interface |
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
```

#### 4167. Legacy account.borrow() found (in documentation context)

- **File**: lib/migration/README.md
- **Location**: Line 108, Column 4
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
| `account.save(` | `account.storage.save(` | storage |
| `account.borrow(` | `account.capabilities.borrow(` | storage |
| `pub fun ` | `access(all) fun ` | function |
```

#### 4168. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 25, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4169. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 44, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4170. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 60, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4171. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 76, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4172. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 92, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4173. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 108, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify no legacy syntax anywhere in complex contract
      expect(result).not.toContain('pub ')
      expect(result).not.toContain('pub(')
```

#### 4174. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 159, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should also use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 4175. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 169, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Default mock should use modern syntax
      expect(result).not.toContain('pub ')
      expect(result).toContain('access(all)')
```

#### 4176. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 210, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 4177. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 211, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          
```

#### 4178. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 213, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4179. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 232, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub var legacyVar: String
          access(all) var modernVar: String
```

#### 4180. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 254, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 4181. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 254, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should correctly identify code for rejection', () => {
      const legacyCode = `pub contract Test { pub var value: String }`
      const modernCode = `access(all) contract Test { access(all) var value: String }`
```

#### 4182. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 268, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 4183. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 269, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub fun getValue(): String
```

#### 4184. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 270, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 4185. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 283, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract TestContract {
          pub var value: String
```

#### 4186. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 284, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          pub(set) var settableValue: String
```

#### 4187. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 329, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Each template should use modern syntax
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 4188. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 341, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      nftTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4189. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 351, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      tokenTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4190. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 360, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      marketplaceTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4191. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 369, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      daoTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4192. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 378, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      defiTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4193. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 387, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      utilityTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4194. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 398, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featuredTemplates.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4195. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 407, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(nftTemplate).toBeDefined()
      expect(nftTemplate!.code).not.toContain('pub ')
      expect(nftTemplate!.code).toContain('access(all)')
```

#### 4196. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 417, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      searchResults.forEach(template => {
        expect(template.code).not.toContain('pub ')
        expect(template.code).toContain('access(all)')
```

#### 4197. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 424, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      templates.forEach(template => {
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
```

#### 4198. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 425, Column 46
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // At minimum, templates should not contain legacy pub keywords
        expect(template.code).not.toContain('pub ')
        expect(template.code).not.toContain('pub(')
```

#### 4199. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 460, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result).toContain('access(all)')
      expect(result).not.toContain('pub ')
      
```

#### 4200. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 508, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 4201. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 508, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // This test simulates what would happen if AI generated legacy code
      const legacyCode = 'pub contract Test { pub var value: String }'
      
```

#### 4202. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 553, Column 41
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // Streamed code should use modern syntax
        expect(fullCode).not.toContain('pub ')
        expect(fullCode).toContain('access(all)')
```

#### 4203. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 628, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Refined code should still use modern syntax
      expect(refinedCode).not.toContain('pub ')
      expect(refinedCode).toContain('access(all)')
```

#### 4204. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 698, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.length).toBeGreaterThan(0)
      expect(result).not.toContain('pub ')
    })
```

#### 4205. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 285, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub(set) var settableValue: String
        }
```

#### 4206. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 143, Column 39
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(result).toContain('account.storage.save')
        expect(result).not.toContain('account.save(')
      }
```

#### 4207. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 192, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 4208. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 213, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4209. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 235, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.modernVar
```

#### 4210. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 270, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          pub fun getValue(): String
        }
```

#### 4211. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/ai-generation-e2e.test.ts
- **Location**: Line 594, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) view fun getValue(): String {
            return self.value
```

#### 4212. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 12, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  return Promise.resolve({
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
```

#### 4213. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 13, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    ok: options.body.includes('pub ') ? false : true,
    status: options.body.includes('pub ') ? 422 : 200,
    json: () => Promise.resolve({
```

#### 4214. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 15, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    json: () => Promise.resolve({
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
```

#### 4215. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 16, Column 40
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      error: options.body.includes('pub ') ? 'Legacy syntax detected' : undefined,
      rejected: options.body.includes('pub '),
      validation: {
```

#### 4216. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 18, Column 51
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      validation: {
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
```

#### 4217. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 19, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        hasLegacyPatterns: options.body.includes('pub '),
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
```

#### 4218. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 20, Column 74
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        patterns: options.body.includes('pub ') ? [
          { type: 'access-modifier', severity: 'critical', description: 'pub keyword detected' }
        ] : []
```

#### 4219. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 29, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 4220. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 30, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 4221. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 50, Column 43
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        body: JSON.stringify({
          prompt: 'Create a contract with pub functions',
          context: legacyCode
```

#### 4222. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 188, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          code: modernCode,
          refinementRequest: 'Add pub functions' // This would generate legacy syntax
        })
```

#### 4223. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 243, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const streamData = [
        { type: 'code_chunk', chunk: 'pub contract Test {' },
        { type: 'validation_warning', message: 'Legacy syntax patterns detected' },
```

#### 4224. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 283, Column 62
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const testCases = [
        { endpoint: '/api/generate', body: { prompt: 'create pub contract' } },
        { endpoint: '/api/explain', body: { code: legacyCode } },
```

#### 4225. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 30, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 4226. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-strict-validation.test.ts
- **Location**: Line 38, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 4227. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 13, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract TestContract {
      pub fun getValue(): String {
```

#### 4228. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 14, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 4229. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 18, Column 7
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 4230. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 19, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
        
```

#### 4231. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 98, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toBeDefined()
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.modernizedCode).toContain('access(all)')
```

#### 4232. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 106, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 4233. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 107, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
        }
```

#### 4234. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 124, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).toContain('access(all)')
      expect(result.modernizedCode).not.toContain('pub ')
      expect(result.transformationsApplied.length).toBeGreaterThan(0)
```

#### 4235. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 196, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexLegacyCode = `
        pub contract ComplexContract {
          pub fun publicFunction(): String {
```

#### 4236. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 197, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 4237. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 201, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub var publicVariable: String
          
```

#### 4238. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 203, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 4239. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 204, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
```

#### 4240. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 261, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle malformed code gracefully', async () => {
      const malformedCode = 'pub contract { invalid syntax }'
      
```

#### 4241. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 273, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 4242. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 205, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun resourceFunction() {
              account.save(<-create TestResource(), to: /storage/test)
              let ref = account.borrow<&TestResource>(from: /storage/test)
```

#### 4243. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 18, Column 25
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      pub resource Vault: Provider, Receiver {
        pub var balance: UFix64
```

#### 4244. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 106, Column 27
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyInterfaceCode = `
        pub resource Vault: Provider, Receiver, Balance {
          pub var balance: UFix64
```

#### 4245. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 203, Column 36
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource TestResource: Interface1, Interface2 {
            pub fun resourceFunction() {
```

#### 4246. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 14, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub contract TestContract {
      pub fun getValue(): String {
        return "test"
```

#### 4247. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 30, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    access(all) contract TestContract {
      access(all) fun getValue(): String {
        return "test"
```

#### 4248. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 197, Column 15
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ComplexContract {
          pub fun publicFunction(): String {
            return "test"
```

#### 4249. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 228, Column 23
- **Pattern**: `fun publicFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun publicFunction(): String {
            return "test"
```

#### 4250. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 273, Column 15
- **Pattern**: `fun legacyFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract MixedContract {
          pub fun legacyFunction(): String {
            return "test"
```

#### 4251. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/api-validation-integration.test.ts
- **Location**: Line 277, Column 23
- **Pattern**: `fun modernFunction(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun modernFunction(): String {
            return "modern"
```

#### 4252. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 34, Column 45
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub keyword', () => {
      const legacyCode = `
```

#### 4253. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4254. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 37, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4255. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 39, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4256. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 137, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('containsLegacyPubKeywords', () => {
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
```

#### 4257. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 138, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 4258. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 138, Column 48
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub keywords', () => {
      const codeWithPub = 'pub contract Test { pub var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPub)).toBe(true)
```

#### 4259. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 152, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should not detect pub in comments or strings', () => {
      const codeWithPubInString = `
```

#### 4260. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 155, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
```

#### 4261. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 156, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          // This is a comment about pub keyword
          access(all) var description: String = "This contract was pub before"
        }
```

#### 4262. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 164, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
```

#### 4263. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 165, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test {}'
      const result = validator.shouldRejectCode(legacyCode)
```

#### 4264. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 203, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4265. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 204, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4266. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 243, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4267. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 244, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4268. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 306, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4269. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 307, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4270. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 347, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithMultipleIssues = `
        pub contract TestContract {
          pub var value: String
```

#### 4271. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 348, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var value: String
          fun missingAccess(): String {
```

#### 4272. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 418, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract {
          pub var incomplete
          fun missingBraces() {
```

#### 4273. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 429, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should handle very long lines', () => {
      const longLine = 'pub var ' + 'a'.repeat(10000) + ': String'
      const result = validator.validateCode(longLine)
```

#### 4274. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 52, Column 45
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should fail validation for legacy pub(set) keyword', () => {
      const legacyCode = `
```

#### 4275. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 55, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract TestContract {
          pub(set) var value: String
        }
```

#### 4276. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 142, Column 25
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
```

#### 4277. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 143, Column 59
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should detect pub(set) keywords', () => {
      const codeWithPubSet = 'access(all) contract Test { pub(set) var value: String }'
      expect(validator.containsLegacyPubKeywords(codeWithPubSet)).toBe(true)
```

#### 4278. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 69, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 4279. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 175, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4280. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 207, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 4281. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 247, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4282. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 310, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4283. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 86, Column 42
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        access(all) resource TestResource: Interface1, Interface2 {
          // resource implementation
```

#### 4284. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 17, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4285. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 39, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4286. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 281, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4287. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 349, Column 11
- **Pattern**: `fun missingAccess(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var value: String
          fun missingAccess(): String {
            return account.borrow<&String>(from: /storage/data)?.value ?? ""
```

#### 4288. Import statement with hardcoded address (in test context)

- **File**: lib/migration/__tests__/code-validator.test.ts
- **Location**: Line 375, Column 9
- **Pattern**: `import NonFungibleToken from 0x1d7e57aa55817448`
- **Type**: import-statement
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const codeWithHardcodedAddress = `
        import NonFungibleToken from 0x1d7e57aa55817448
        
```

#### 4289. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 27, Column 12
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    featured: false,
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64
```

#### 4290. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 28, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    code: `pub contract DAOVoting {
    pub var proposalCount: UInt64

```

#### 4291. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 30, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
```

#### 4292. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 31, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ProposalCreated(id: UInt64, title: String, creator: Address?)
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)
```

#### 4293. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 32, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event VoteCast(proposalId: UInt64, voter: Address?, support: Bool)
    pub event ProposalExecuted(id: UInt64)

```

#### 4294. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 34, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub struct Proposal {
        pub let id: UInt64
```

#### 4295. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 35, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub struct Proposal {
        pub let id: UInt64
        pub let title: String
```

#### 4296. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let id: UInt64
        pub let title: String
        pub let description: String
```

#### 4297. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 37, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let title: String
        pub let description: String
        pub let creator: Address
```

#### 4298. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 38, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let description: String
        pub let creator: Address
        pub var votesFor: UInt64
```

#### 4299. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 39, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub let creator: Address
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
```

#### 4300. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 40, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesFor: UInt64
        pub var votesAgainst: UInt64
        pub var executed: Bool
```

#### 4301. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 41, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var votesAgainst: UInt64
        pub var executed: Bool
        pub let createdAt: UFix64
```

#### 4302. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 42, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var executed: Bool
        pub let createdAt: UFix64

```

#### 4303. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 55, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun addVote(support: Bool) {
            if support {
```

#### 4304. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 63, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun execute() {
            self.executed = true
```

#### 4305. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 71, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 4306. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 85, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun vote(proposalId: UInt64, voter: Address, support: Bool) {
        pre {
```

#### 4307. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 98, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 4308. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 102, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getAllProposals(): [Proposal] {
        return self.proposals.values
```

#### 4309. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 121, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Syntax Transformation', () => {
    it('should transform all pub keywords to access(all)', () => {
      const transformedCode = transformer.transformAccessModifiers(originalDAOTemplate.code)
```

#### 4310. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 124, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should not contain any pub keywords followed by Cadence keywords
      expect(transformedCode).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 4311. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 223, Column 68
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(validationResult.errors.length).toBeGreaterThan(0)
      expect(validationResult.errors.some(error => error.includes('pub keyword usage'))).toBe(true)
    })
```

#### 4312. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 232, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const hasLegacySyntaxErrors = validationResult.errors.some(error => 
        error.includes('pub keyword usage') || 
        error.includes('Legacy syntax found')
```

#### 4313. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 71, Column 9
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 4314. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/dao-voting-migration.test.ts
- **Location**: Line 98, Column 9
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 4315. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/fungible-token-migration.test.ts
- **Location**: Line 37, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform all pub keywords to access(all)', () => {
    const template = getTemplateById('fungible-token')
```

#### 4316. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/fungible-token-migration.test.ts
- **Location**: Line 43, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Check that no pub keywords remain
    expect(template.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface)/)
```

#### 4317. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 12, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('Access Modifier Detection', () => {
    test('detects pub keyword in various contexts', () => {
      const code = `
```

#### 4318. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 14, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4319. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 17, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64
        pub let name: String
```

#### 4320. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 18, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        pub let name: String
      `;
```

#### 4321. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 25, Column 49
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(pubPatterns).toHaveLength(3);
      expect(pubPatterns[0].originalText).toBe('pub fun ');
      expect(pubPatterns[0].modernReplacement).toBe('access(all) fun ');
```

#### 4322. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 43, Column 3
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 4323. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 49, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const patterns = detector.detectAllLegacyPatterns(code);
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      
```

#### 4324. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 113, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 4325. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 138, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 4326. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 140, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.link<&Vault>(/public/vault, target: /storage/vault)
        pub var balance: UFix64
      `;
```

#### 4327. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 164, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
```

#### 4328. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 165, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 4329. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 166, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
          
```

#### 4330. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 183, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 4331. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 185, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const highRiskCode = `
        pub fun test1() {}
        pub fun test2() {}
```

#### 4332. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 186, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test1() {}
        pub fun test2() {}
        pub fun test3() {}
```

#### 4333. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 187, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test2() {}
        pub fun test3() {}
        pub fun test4() {}
```

#### 4334. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 188, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test3() {}
        pub fun test4() {}
        pub fun test5() {}
```

#### 4335. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 189, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test4() {}
        pub fun test5() {}
        pub fun test6() {}
```

#### 4336. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 190, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test5() {}
        pub fun test6() {}
        pub fun test7() {}
```

#### 4337. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 191, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test6() {}
        pub fun test7() {}
        pub fun test8() {}
```

#### 4338. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 192, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test7() {}
        pub fun test8() {}
        pub fun test9() {}
```

#### 4339. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 193, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test8() {}
        pub fun test9() {}
        pub fun test10() {}
```

#### 4340. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 194, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test9() {}
        pub fun test10() {}
        pub fun test11() {}
```

#### 4341. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 195, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test10() {}
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
```

#### 4342. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 231, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 4343. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 232, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 4344. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 233, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
```

#### 4345. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 250, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 4346. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 256, Column 65
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const pubPattern = patterns.find(p => p.originalText === 'pub fun ');
      expect(pubPattern?.location.line).toBe(2);
```

#### 4347. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 267, Column 36
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        // This should not trigger pub detection
        let description = "This is a function"
```

#### 4348. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 283, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(1000).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test" }`
      ).join('\n');
```

#### 4349. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 290, Column 38
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
```

#### 4350. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 291, Column 71
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Should detect at least 1000 pub fun patterns
      const pubFunPatterns = patterns.filter(p => p.originalText === 'pub fun ');
      expect(pubFunPatterns).toHaveLength(1000);
```

#### 4351. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 30, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;
```

#### 4352. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 31, Column 21
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('detects pub(set) modifier', () => {
      const code = `pub(set) var balance: UFix64 = 0.0`;

```

#### 4353. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 59, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        account.save(<-vault, to: /storage/vault)
        let vault <- account.load<@Vault>(from: /storage/vault)
```

#### 4354. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 82, Column 21
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('categorizes storage patterns correctly', () => {
      const code = `account.save(<-vault, to: /storage/vault)`;

```

#### 4355. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 114, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
```

#### 4356. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 169, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 4357. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 196, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun test11() {}
        account.save(<-vault, to: /storage/vault)
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 4358. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 234, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit(from: @FungibleToken.Vault) {
              account.save(<-from, to: /storage/temp)
            }
```

#### 4359. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 251, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 4360. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 260, Column 66
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      const savePattern = patterns.find(p => p.originalText === 'account.save(');
      expect(savePattern?.location.line).toBe(3);
```

#### 4361. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 96, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        resource Vault: Provider, Receiver, Balance {
          // implementation
```

#### 4362. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 115, Column 23
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        account.save(<-vault, to: /storage/vault)
        resource Vault: Provider, Receiver {}
      `;
```

#### 4363. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 232, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit(from: @FungibleToken.Vault) {
```

#### 4364. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 14, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4365. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 43, Column 7
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `contract Test {
  pub fun getValue(): String {
    return "test"
```

#### 4366. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 113, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 4367. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 138, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 4368. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 165, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String { return "test" }
          pub var balance: UFix64
```

#### 4369. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 183, Column 32
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates risk level correctly', () => {
      const lowRiskCode = `pub fun getValue(): String { return "test" }`;
      const highRiskCode = `
```

#### 4370. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/legacy-pattern-detector.test.ts
- **Location**: Line 217, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) fun getValue(): String {
            return "modern"
```

#### 4371. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 127, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const sampleContract = `
        pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 4372. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 128, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 4373. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 130, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 4374. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 134, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun updateSupply(newSupply: UFix64) {
            self.totalSupply = newSupply
```

#### 4375. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 155, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify syntax is modernized
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 4376. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 161, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const resourceContract = `
        pub contract ResourceTest {
          pub resource Vault {
```

#### 4377. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 162, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract ResourceTest {
          pub resource Vault {
            pub var balance: UFix64
```

#### 4378. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 163, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault {
            pub var balance: UFix64
            
```

#### 4379. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 165, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              self.balance = self.balance + from.balance
```

#### 4380. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 170, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              self.balance = self.balance - amount
```

#### 4381. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 194, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 4382. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 199, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const eventContract = `
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
```

#### 4383. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 200, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract EventTest {
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
```

#### 4384. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 201, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub event TokensDeposited(amount: UFix64, to: Address?)
          pub event TokensWithdrawn(amount: UFix64, from: Address?)
          
```

#### 4385. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 203, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64, to: Address?) {
            emit TokensDeposited(amount: amount, to: to)
```

#### 4386. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 207, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(amount: UFix64, from: Address?) {
            emit TokensWithdrawn(amount: amount, from: from)
```

#### 4387. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 224, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 4388. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 229, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const interfaceContract = `
        pub contract InterfaceTest {
          pub resource interface Provider {
```

#### 4389. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 230, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract InterfaceTest {
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
```

#### 4390. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 231, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Provider {
            pub fun withdraw(amount: UFix64): @Vault
          }
```

#### 4391. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 234, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
```

#### 4392. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 235, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource interface Receiver {
            pub fun deposit(from: @Vault)
          }
```

#### 4393. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 238, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 4394. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 239, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 4395. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 241, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 4396. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 245, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun deposit(from: @Vault) {
              destroy from
```

#### 4397. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 269, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(migrated).toContain('access(all)')
      expect(migrated).not.toContain('pub ')
    })
```

#### 4398. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 289, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeContract = `
        pub contract LargeContract {
          ${Array.from({ length: 100 }, (_, i) => `
```

#### 4399. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 291, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          ${Array.from({ length: 100 }, (_, i) => `
            pub var value${i}: UFix64
            
```

#### 4400. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 293, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue${i}(): UFix64 {
              return self.value${i}
```

#### 4401. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 297, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun setValue${i}(newValue: UFix64) {
              self.value${i} = newValue
```

#### 4402. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 322, Column 39
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify transformation was applied
      expect(migrated).not.toContain('pub ')
      expect(migrated).toContain('access(all)')
```

#### 4403. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 368, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const malformedCode = `
        pub contract {
          pub var incomplete
```

#### 4404. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 369, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract {
          pub var incomplete
          pub fun missingBrace() {
```

#### 4405. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 370, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var incomplete
          pub fun missingBrace() {
        // Missing closing brace and other syntax errors
```

#### 4406. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 474, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `
          pub contract TestTemplate {
            pub var value: String
```

#### 4407. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 475, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub contract TestTemplate {
            pub var value: String
            
```

#### 4408. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 477, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 4409. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 503, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Verify code is transformed
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 4410. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 238, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 4411. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 130, Column 15
- **Pattern**: `fun getTotalSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getTotalSupply(): UFix64 {
            return self.totalSupply
```

#### 4412. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/migration-pipeline-integration.test.ts
- **Location**: Line 477, Column 17
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun getValue(): String {
              return self.value
```

#### 4413. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 28, Column 44
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Validate the template has been migrated
      expect(template.code).not.toContain('pub ')
      expect(template.code).toContain('access(all)')
```

#### 4414. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 199, Column 60
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyErrors = validationResult.errors.filter(error => 
        error.includes('Legacy syntax') || error.includes('pub keyword')
      )
```

#### 4415. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 174, Column 31
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getOwners(): [Address]')
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
```

#### 4416. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 175, Column 31
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getRequiredConfirmations(): UInt64')
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
```

#### 4417. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 176, Column 31
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getTransactionCount(): UInt64')
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
```

#### 4418. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 177, Column 31
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun isOwner(address: Address): Bool')
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')
```

#### 4419. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 178, Column 31
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun getConfirmationCount(txId: UInt64): UInt64')
      expect(code).toContain('fun hasConfirmed(txId: UInt64, owner: Address): Bool')

```

#### 4420. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 295, Column 31
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Original core functions should still exist with same signatures
      expect(code).toContain('fun submitTransaction(to: Address, amount: UFix64): UInt64')
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
```

#### 4421. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/multi-sig-wallet-migration.test.ts
- **Location**: Line 297, Column 31
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('fun confirmTransaction(txId: UInt64, owner: Address)')
      expect(code).toContain('fun getTransaction(txId: UInt64): Transaction?')

```

#### 4422. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 30, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      featured: false,
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
```

#### 4423. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 31, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract NFTMarketplace {
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)
```

#### 4424. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 32, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub event ForSale(id: UInt64, price: UFix64, owner: Address?)
    pub event TokenPurchased(id: UInt64, price: UFix64, seller: Address?, buyer: Address?)

```

#### 4425. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 34, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
```

#### 4426. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 35, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    pub resource interface SalePublic {
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
```

#### 4427. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 36, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 4428. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 37, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
    }
```

#### 4429. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 40, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub resource SaleCollection: SalePublic {
        access(self) var forSale: {UInt64: UFix64}
```

#### 4430. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 49, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun listForSale(tokenID: UInt64, price: UFix64) {
            self.forSale[tokenID] = price
```

#### 4431. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 54, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault) {
            let price = self.forSale[tokenID]!
```

#### 4432. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 66, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4433. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 70, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun getIDs(): [UInt64] {
            return self.forSale.keys
```

#### 4434. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 75, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    pub fun createSaleCollection(collection: &NFTCollection): @SaleCollection {
        return <- create SaleCollection(collection: collection)
```

#### 4435. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 91, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

  test('should transform pub keywords to access(all)', () => {
    const migratedTemplate = migration.migrateNFTMarketplace(legacyTemplate)
```

#### 4436. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 94, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    // Should not contain pub keywords
    expect(migratedTemplate.code).not.toMatch(/\bpub\s+(?:var|let|fun|resource|struct|contract|interface|event)/)
```

#### 4437. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 104, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      ...legacyTemplate,
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
```

#### 4438. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 105, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 4439. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 106, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
        }
```

#### 4440. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 105, Column 34
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      code: `pub contract Test {
        pub resource TestResource: Interface1, Interface2 {
          pub fun test() {}
```

#### 4441. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 36, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun purchase(tokenID: UInt64, recipient: &AnyResource{}, buyTokens: @FungibleToken.Vault)
        pub fun idPrice(tokenID: UInt64): UFix64?
        pub fun getIDs(): [UInt64]
```

#### 4442. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/nft-marketplace-migration.test.ts
- **Location**: Line 66, Column 13
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

        pub fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4443. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 14, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub fun getValue(): String {
```

#### 4444. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 15, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 4445. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 18, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          }
          pub var balance: UFix64
        }
```

#### 4446. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 31, Column 10
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const largeCode = Array(500).fill(0).map((_, i) => 
        `pub fun test${i}(): String { return "test${i}" }`
      ).join('\n');
```

#### 4447. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 59, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4448. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 83, Column 29
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 4449. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 103, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 4450. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 104, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
```

#### 4451. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 122, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4452. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 125, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        pub var balance: UFix64 = 0.0
      `;
```

#### 4453. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 130, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
```

#### 4454. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 131, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.modernizedCode).not.toContain('pub fun');
      expect(result.modernizedCode).not.toContain('pub var');
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 4455. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 140, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub var balance: UFix64
```

#### 4456. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 141, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub var balance: UFix64
          
```

#### 4457. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 143, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(amount: UFix64) {
            self.balance = self.balance + amount
```

#### 4458. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 164, Column 21
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 4459. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 187, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const complexCode = `
        pub fun complexFunction() {
          account.link<&Vault>(/public/vault, target: /storage/vault)
```

#### 4460. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 198, Column 14
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // The pub fun should be auto-fixed
      expect(result.modernizedCode).toContain('access(all) fun');
```

#### 4461. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 214, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 4462. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 253, Column 26
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 4463. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 283, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles malformed code gracefully', async () => {
      const malformedCode = 'pub fun { invalid syntax }';
      
```

#### 4464. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 293, Column 30
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles very large code inputs', async () => {
      const veryLargeCode = 'pub fun test() {}\n'.repeat(1000); // Reduced size for better performance
      
```

#### 4465. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 305, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
```

#### 4466. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 306, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 4467. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 307, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
```

#### 4468. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 326, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `line 1
pub fun test() {
  account.save()
```

#### 4469. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 62, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 4470. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 105, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub var balance: UFix64
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 4471. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 145, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            self.balance = self.balance + amount
            account.save(<-create Vault(), to: /storage/vault)
          }
```

#### 4472. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 215, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
      `;
```

#### 4473. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 308, Column 15
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            pub fun deposit() {
              account.save(<-vault, to: /storage/vault)
            }
```

#### 4474. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 327, Column 3
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
pub fun test() {
  account.save()
}`;
```

#### 4475. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 306, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub resource Vault: Provider, Receiver {
            pub fun deposit() {
```

#### 4476. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 15, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract TestContract {
          pub fun getValue(): String {
            return "test"
```

#### 4477. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 43, Column 28
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract ModernContract {
          access(all) view fun getValue(): String {
            return "modern"
```

#### 4478. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 59, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4479. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 83, Column 33
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('provides accurate confidence scores', async () => {
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';
```

#### 4480. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 84, Column 43
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const criticalCode = 'pub fun test(): String { return "test" }';
      const suggestionCode = 'access(all) fun test(): String { return "test" }';

```

#### 4481. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 103, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const autoFixableCode = `
        pub fun getValue(): String { return "test" }
        pub var balance: UFix64
```

#### 4482. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 122, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String {
          return "test"
```

#### 4483. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 164, Column 25
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('respects modernization options', () => {
      const code = 'pub fun test(): String { return "test" }';

```

#### 4484. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 214, Column 13
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const code = `
        pub fun getValue(): String { return "test" }
        account.save(<-vault, to: /storage/vault)
```

#### 4485. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 253, Column 30
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates high confidence for clear patterns', async () => {
      const clearCode = 'pub fun getValue(): String { return "test" }';
      const result = await validator.validateUserInput(clearCode);
```

#### 4486. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/realtime-validator.test.ts
- **Location**: Line 262, Column 9
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This might be a function
        fun getValue(): String { return "test" }
      `;
```

#### 4487. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 30, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify the template has been migrated
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
```

#### 4488. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 31, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub contract')
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
```

#### 4489. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 32, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub var')
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
```

#### 4490. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 33, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub let')
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
```

#### 4491. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 34, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub fun')
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
```

#### 4492. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 35, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    expect(template.code).not.toContain('pub struct')
    expect(template.code).not.toContain('pub event')
  })
```

#### 4493. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/staking-rewards-migration.test.ts
- **Location**: Line 73, Column 38
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Verify core staking logic is preserved
    expect(template.code).toContain('fun calculateReward(user: Address): UFix64')
    expect(template.code).toContain('timeStaked = getCurrentBlock().timestamp - stakeInfo.stakedAt')
```

#### 4494. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 18, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('transformAccessModifiers', () => {
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
```

#### 4495. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 19, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 4496. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 32, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms multiple pub keywords', () => {
      const input = `
```

#### 4497. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 34, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract {
          pub var totalSupply: UFix64
```

#### 4498. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 35, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract {
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
```

#### 4499. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 36, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 4500. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 53, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 4501. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 55, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract Test {
          pub(set) var value: Int
```

#### 4502. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 57, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 4503. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 82, Column 19
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub in comments and strings correctly', () => {
      const input = `
```

#### 4504. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 84, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        // This is a pub comment
        pub contract Test {
```

#### 4505. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 85, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        // This is a pub comment
        pub contract Test {
          pub fun test(): String {
```

#### 4506. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 86, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 4507. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 87, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun test(): String {
            return "This contains pub in string"
          }
```

#### 4508. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 92, Column 22
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const expected = `
        // This is a pub comment
        access(all) contract Test {
```

#### 4509. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 95, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) fun test(): String {
            return "This contains pub in string"
          }
```

#### 4510. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 109, Column 32
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles code without pub keywords', () => {
      const input = `
```

#### 4511. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 318, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 4512. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 319, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
          
```

#### 4513. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 321, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
```

#### 4514. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 325, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun withdraw(): @Vault {
            return <-account.load<@Vault>(from: /storage/vault)!
```

#### 4515. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 333, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      // Should transform pub keywords
      expect(result).toContain('access(all) contract')
```

#### 4516. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 349, Column 25
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('calculates transformation statistics', () => {
      const original = `pub contract Test {
        pub var value: Int
```

#### 4517. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 350, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const original = `pub contract Test {
        pub var value: Int
      }`
```

#### 4518. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 25, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
```

#### 4519. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 26, Column 22
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub(set) to access(all)', () => {
      const input = 'pub(set) var balance: UFix64'
      const expected = 'access(all) var balance: UFix64'
```

#### 4520. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 53, Column 19
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence

    test('handles pub(set) before pub transformation', () => {
      const input = `
```

#### 4521. Legacy pub(set) keyword found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 56, Column 11
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub(set) var value: Int
          pub fun getValue(): Int
```

#### 4522. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 256, Column 22
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms account.save to account.storage.save', () => {
      const input = 'account.save(vault, to: /storage/vault)'
      const expected = 'account.storage.save(vault, to: /storage/vault)'
```

#### 4523. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 285, Column 9
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        account.save(vault, to: /storage/vault)
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 4524. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 322, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub fun deposit(vault: @Vault) {
            account.save(<-vault, to: /storage/vault)
          }
```

#### 4525. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 125, Column 36
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms comma-separated interfaces to ampersand-separated', () => {
      const input = 'resource Vault: Provider, Receiver {'
      const expected = 'resource Vault: Provider & Receiver {'
```

#### 4526. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 132, Column 34
- **Pattern**: `: Fungible, Mintable`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms multiple interfaces with spaces', () => {
      const input = 'struct Token: Fungible, Mintable, Burnable {'
      const expected = 'struct Token: Fungible & Mintable & Burnable {'
```

#### 4527. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 139, Column 41
- **Pattern**: `: ContractInterface, AnotherInterface`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles contract interface conformance', () => {
      const input = 'contract MyContract: ContractInterface, AnotherInterface {'
      const expected = 'contract MyContract: ContractInterface & AnotherInterface {'
```

#### 4528. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 152, Column 36
- **Pattern**: `:  Provider,  Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('handles interfaces with extra whitespace', () => {
      const input = 'resource Vault:  Provider,  Receiver,  Balance  {'
      const expected = 'resource Vault: Provider & Receiver & Balance {'
```

#### 4529. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 318, Column 32
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        pub contract MyContract: Interface1, Interface2 {
          pub var totalSupply: UFix64
```

#### 4530. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 19, Column 26
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('transforms pub to access(all)', () => {
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
```

#### 4531. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 20, Column 37
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'pub fun getValue(): String'
      const expected = 'access(all) fun getValue(): String'
      const result = transformer.transformAccessModifiers(input)
```

#### 4532. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 36, Column 15
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub var totalSupply: UFix64
          pub fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 4533. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 44, Column 23
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var totalSupply: UFix64
          access(all) fun getSupply(): UFix64 {
            return self.totalSupply
```

#### 4534. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 57, Column 15
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub(set) var value: Int
          pub fun getValue(): Int
        }
```

#### 4535. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 63, Column 23
- **Pattern**: `fun getValue(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          access(all) var value: Int
          access(all) fun getValue(): Int
        }
```

#### 4536. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 86, Column 15
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract Test {
          pub fun test(): String {
            return "This contains pub in string"
```

#### 4537. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 94, Column 23
- **Pattern**: `fun test(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        access(all) contract Test {
          access(all) fun test(): String {
            return "This contains pub in string"
```

#### 4538. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 187, Column 34
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to getter functions', () => {
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
```

#### 4539. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 188, Column 42
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun getValue(): String {'
      const expected = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4540. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 194, Column 34
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to read functions', () => {
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
```

#### 4541. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 195, Column 42
- **Pattern**: `fun readBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun readBalance(): UFix64 {'
      const expected = 'access(all) view fun readBalance(): UFix64 {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4542. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 201, Column 34
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('adds view modifier to check functions', () => {
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
```

#### 4543. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 202, Column 42
- **Pattern**: `fun checkPermission(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = 'access(all) fun checkPermission(): Bool {'
      const expected = 'access(all) view fun checkPermission(): Bool {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4544. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 208, Column 39
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('preserves existing view modifier', () => {
      const input = 'access(all) view fun getValue(): String {'
      const result = transformer.transformFunctionSignatures(input)
```

#### 4545. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 233, Column 21
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const input = `
        access(all) fun getValue(): String {
          return self.value
```

#### 4546. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/syntax-transformer.test.ts
- **Location**: Line 241, Column 21
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        
        access(all) fun getBalance(): UFix64 {
          return self.balance
```

#### 4547. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 174, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 4548. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 175, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
          
```

#### 4549. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 177, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 4550. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 178, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
            
```

#### 4551. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 180, Column 13
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
            
            pub fun withdraw(amount: UFix64): @Vault {
              return <-create Vault(balance: amount)
```

#### 4552. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 189, Column 52
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      
      expect(migratedTemplate.code).not.toContain('pub ')
      expect(migratedTemplate.code).toContain('access(all)')
```

#### 4553. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 256, Column 16
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        featured: false,
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
```

#### 4554. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 257, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        code: `pub contract TestContract {
          pub var totalSupply: UFix64
        }`
```

#### 4555. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 265, Column 58
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(result.errors.length).toBeGreaterThan(0)
      expect(result.errors.some(error => error.includes('pub keyword'))).toBe(true)
    })
```

#### 4556. Legacy interface conformance syntax found (in test context)

- **File**: lib/migration/__tests__/template-scanner.test.ts
- **Location**: Line 177, Column 29
- **Pattern**: `: Provider, Receiver`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub resource Vault: Provider, Receiver {
            pub var balance: UFix64
```

#### 4557. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 53, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4558. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 54, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4559. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 56, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4560. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 87, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  describe('shouldRejectGeneratedCode', () => {
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
```

#### 4561. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 88, Column 27
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 4562. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 88, Column 47
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    test('should reject code with pub keywords', () => {
      const legacyCode = 'pub contract Test { pub var value: String }'
      const result = vibeSDK.shouldRejectGeneratedCode(legacyCode)
```

#### 4563. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 146, Column 42
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      // Mock responses should use modern syntax
      expect(result.code).not.toContain('pub ')
      expect(result.code).toContain('access(all)')
```

#### 4564. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 168, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4565. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 169, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4566. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 203, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4567. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 204, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4568. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 248, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 4569. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 258, Column 35
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('access(all)')
      expect(code).not.toContain('pub ')
    })
```

#### 4570. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 307, Column 9
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      const legacyCode = `
        pub contract LegacyContract {
          pub var value: String
```

#### 4571. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 308, Column 11
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        pub contract LegacyContract {
          pub var value: String
          
```

#### 4572. Legacy pub keyword found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 368, Column 37
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        expect(validation.isValid).toBe(true)
        expect(code).not.toContain('pub ')
        expect(code).toContain('access(all)')
```

#### 4573. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 99, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4574. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 172, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4575. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 207, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
            account.link<&String>(/public/data, target: /storage/data)
```

#### 4576. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 266, Column 35
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).toContain('account.capabilities')
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
```

#### 4577. Legacy account.save() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 311, Column 13
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          init() {
            account.save("data", to: /storage/data)
          }
```

#### 4578. Legacy account.link() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 267, Column 35
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.save(')
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
```

#### 4579. Legacy account.borrow() found (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 268, Column 35
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      expect(code).not.toContain('account.link(')
      expect(code).not.toContain('account.borrow(')
    })
```

#### 4580. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 34, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4581. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 56, Column 15
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          pub fun getValue(): String {
            return self.value
```

#### 4582. Potential legacy function signature (in test context)

- **File**: lib/migration/__tests__/vibesdk-validation.test.ts
- **Location**: Line 278, Column 23
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
          
          access(all) fun getValue(): String {
            return self.value
```

#### 4583. Legacy pub keyword found (in example context)

- **File**: lib/migration/code-validator.ts
- **Location**: Line 278, Column 24
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
    // Add general modernization suggestions
    if (code.includes('pub ')) {
      suggestions.push('Replace all "pub" keywords with appropriate access modifiers like "access(all)"')
```

#### 4584. Legacy pub keyword found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 41, Column 28
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub keyword found',
      suggestedFix: 'Replace with access(all) or appropriate access modifier',
```

#### 4585. Legacy pub(set) keyword found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 49, Column 28
- **Pattern**: `pub(set) `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy pub(set) keyword found',
      suggestedFix: 'Replace with access(all) and implement setter restrictions',
```

#### 4586. Legacy account.save() found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 59, Column 28
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.save() found',
      suggestedFix: 'Replace with account.storage.save()',
```

#### 4587. Legacy account.link() found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 67, Column 28
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.link() found',
      suggestedFix: 'Replace with account.capabilities.storage.issue() + account.capabilities.publish()',
```

#### 4588. Legacy account.borrow() found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 75, Column 28
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'critical' as const,
      description: 'Legacy account.borrow() found',
      suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 4589. Legacy account.load() found (in example context)

- **File**: lib/migration/codebase-scanner.ts
- **Location**: Line 83, Column 28
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      severity: 'warning' as const,
      description: 'Legacy account.load() found',
      suggestedFix: 'Replace with account.storage.load()',
```

#### 4590. Legacy pub keyword found (in example context)

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 15, Column 5
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  const legacyCode = `
    pub contract LegacyContract {
      pub fun getValue(): String {
```

#### 4591. Potential legacy function signature

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 16, Column 11
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    pub contract LegacyContract {
      pub fun getValue(): String {
        return "legacy"
```

#### 4592. Potential legacy function signature

- **File**: lib/migration/demo-api-validation.ts
- **Location**: Line 34, Column 19
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    access(all) contract ModernContract {
      access(all) fun getValue(): String {
        return "modern"
```

#### 4593. Legacy pub keyword found (in example context)

- **File**: lib/migration/demo.ts
- **Location**: Line 10, Column 1
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 4594. Legacy interface conformance syntax found (in example context)

- **File**: lib/migration/demo.ts
- **Location**: Line 10, Column 29
- **Pattern**: `: Interface1, Interface2`
- **Type**: interface-conformance
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
const legacyCode = `
pub contract ExampleContract: Interface1, Interface2 {
    pub var totalSupply: UFix64
```

#### 4595. Potential legacy function signature

- **File**: lib/migration/demo.ts
- **Location**: Line 14, Column 9
- **Pattern**: `fun getSupply(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    
    pub fun getSupply(): UFix64 {
        return self.totalSupply
```

#### 4596. Potential legacy function signature

- **File**: lib/migration/demo.ts
- **Location**: Line 26, Column 9
- **Pattern**: `fun checkBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    
    pub fun checkBalance(): UFix64 {
        let vaultRef = account.borrow<&Vault>(from: /storage/vault)
```

#### 4597. Legacy pub keyword found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 74, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
```

#### 4598. Legacy pub keyword found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 75, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub fun" declaration found',
        suggestedFix: 'Replace "pub fun" with "access(all) fun"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+fun/, 'access(all) fun'),
```

#### 4599. Legacy pub keyword found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 84, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
```

#### 4600. Legacy pub keyword found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 85, Column 33
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        description: 'Legacy "pub var/let" declaration found',
        suggestedFix: 'Replace "pub var/let" with "access(all) var/let"',
        modernReplacement: (match: string) => match.replace(/\bpub\s+/, 'access(all) '),
```

#### 4601. Legacy pub keyword found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 104, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy "pub struct/resource/contract" declaration found',
        suggestedFix: 'Replace "pub" with "access(all)"',
```

#### 4602. Legacy account.save() found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 118, Column 30
- **Pattern**: `account.save(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.save() API found',
        suggestedFix: 'Replace with account.storage.save()',
```

#### 4603. Legacy account.link() found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 148, Column 30
- **Pattern**: `account.link(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.link() API found',
        suggestedFix: 'Replace with modern capability-based pattern',
```

#### 4604. Legacy account.borrow() found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 138, Column 30
- **Pattern**: `account.borrow(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.borrow() API found',
        suggestedFix: 'Replace with account.capabilities.borrow()',
```

#### 4605. Legacy account.load() found (in example context)

- **File**: lib/migration/legacy-pattern-detector.ts
- **Location**: Line 128, Column 30
- **Pattern**: `account.load(`
- **Type**: storage-api
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
        severity: 'critical',
        description: 'Legacy account.load() API found',
        suggestedFix: 'Replace with account.storage.load()',
```

#### 4606. Potential legacy function signature

- **File**: lib/migration/nft-marketplace-migration.ts
- **Location**: Line 42, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 4607. Potential legacy function signature

- **File**: lib/migration/nft-marketplace-migration.ts
- **Location**: Line 44, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 4608. Potential legacy function signature

- **File**: lib/migration/nft-marketplace-migration.ts
- **Location**: Line 145, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4609. Potential legacy function signature

- **File**: lib/migration/nft-marketplace-migration.ts
- **Location**: Line 153, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 4610. Potential legacy function signature

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 390, Column 20
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    this.exampleDatabase.set('access-modifier', {
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
```

#### 4611. Potential legacy function signature (in example context)

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 391, Column 27
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
      before: 'pub fun getValue(): String {\n  return self.value\n}',
      after: 'access(all) fun getValue(): String {\n  return self.value\n}',
      description: 'Replace "pub" with explicit access control'
```

#### 4612. Potential legacy function signature

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 408, Column 28
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    this.exampleDatabase.set('function-signature', {
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
```

#### 4613. Potential legacy function signature

- **File**: lib/migration/realtime-validator.ts
- **Location**: Line 409, Column 32
- **Pattern**: `fun getBalance(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
      before: 'access(all) fun getBalance(): UFix64 {\n  return self.balance\n}',
      after: 'access(all) view fun getBalance(): UFix64 {\n  return self.balance\n}',
      description: 'Add view modifier for read-only functions'
```

#### 4614. Legacy pub keyword found (in example context)

- **File**: lib/templates.ts
- **Location**: Line 919, Column 31
- **Pattern**: `pub `
- **Type**: pub-keyword
- **Impact**: low
- **Suggested Fix**: Consider if this example is still needed or should be updated

**Context:**
```cadence
  // Check for legacy patterns in code (basic check)
  if (template.code.includes("pub ") || template.code.includes("pub(")) {
    issues.push("Template code contains legacy 'pub' keywords")
```

#### 4615. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 86, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 4616. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 368, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
        access(all) fun purchase(tokenID: UInt64, recipient: &{NonFungibleToken.CollectionPublic}, buyTokens: @{FungibleToken.Vault})
        access(all) view fun idPrice(tokenID: UInt64): UFix64?
        access(all) view fun getIDs(): [UInt64]
```

#### 4617. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 370, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
        access(all) view fun getIDs(): [UInt64]
        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo?
    }
```

#### 4618. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 471, Column 26
- **Pattern**: `fun idPrice(tokenID: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

        access(all) view fun idPrice(tokenID: UInt64): UFix64? {
            return self.forSale[tokenID]
```

#### 4619. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 479, Column 26
- **Pattern**: `fun getSaleInfo(tokenID: UInt64): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

        access(all) view fun getSaleInfo(tokenID: UInt64): SaleInfo? {
            if let price = self.forSale[tokenID] {
```

#### 4620. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 579, Column 17
- **Pattern**: `fun createProposal(title: String, description: String, creator: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) fun createProposal(title: String, description: String, creator: Address): UInt64 {
        let proposal = Proposal(
```

#### 4621. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 606, Column 22
- **Pattern**: `fun getProposal(id: UInt64): P`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getProposal(id: UInt64): Proposal? {
        return self.proposals[id]
```

#### 4622. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 699, Column 22
- **Pattern**: `fun calculateReward(user: Address): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun calculateReward(user: Address): UFix64 {
        if self.stakes[user] == nil {
```

#### 4623. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 710, Column 22
- **Pattern**: `fun getStakeInfo(user: Address): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getStakeInfo(user: Address): StakeInfo? {
        return self.stakes[user]
```

#### 4624. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 776, Column 17
- **Pattern**: `fun submitTransaction(to: Address, amount: UFix64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) fun submitTransaction(to: Address, amount: UFix64): UInt64 {
        let tx = Transaction(id: self.transactionCount, to: to, amount: amount)
```

#### 4625. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 810, Column 22
- **Pattern**: `fun getTransaction(txId: UInt64): T`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getTransaction(txId: UInt64): Transaction? {
        return self.transactions[txId]
```

#### 4626. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 818, Column 22
- **Pattern**: `fun getRequiredConfirmations(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getRequiredConfirmations(): UInt64 {
        return self.required
```

#### 4627. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 822, Column 22
- **Pattern**: `fun getTransactionCount(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getTransactionCount(): UInt64 {
        return self.transactionCount
```

#### 4628. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 826, Column 22
- **Pattern**: `fun isOwner(address: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun isOwner(address: Address): Bool {
        return self.owners.contains(address)
```

#### 4629. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 830, Column 22
- **Pattern**: `fun getConfirmationCount(txId: UInt64): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun getConfirmationCount(txId: UInt64): UInt64 {
        if let tx = self.transactions[txId] {
```

#### 4630. Potential legacy function signature

- **File**: lib/templates.ts
- **Location**: Line 837, Column 22
- **Pattern**: `fun hasConfirmed(txId: UInt64, owner: Address): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence

    access(all) view fun hasConfirmed(txId: UInt64, owner: Address): Bool {
        if let confirmationMap = self.confirmations[txId] {
```

#### 4631. Potential legacy function signature

- **File**: lib/vibesdk.ts
- **Location**: Line 291, Column 21
- **Pattern**: `fun resolveView(_ view: Type): A`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
        
        access(all) fun resolveView(_ view: Type): AnyStruct? {
            switch view {
```

#### 4632. Potential legacy function signature

- **File**: lib/vibesdk.ts
- **Location**: Line 569, Column 22
- **Pattern**: `fun getValue(): S`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    // Public read-only functions
    access(all) view fun getValue(): String {
        return self.value
```

#### 4633. Potential legacy function signature

- **File**: lib/vibesdk.ts
- **Location**: Line 573, Column 22
- **Pattern**: `fun getCounter(): U`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    
    access(all) view fun getCounter(): UInt64 {
        return self.counter
```

#### 4634. Potential legacy function signature

- **File**: lib/vibesdk.ts
- **Location**: Line 589, Column 22
- **Pattern**: `fun getValueLength(): I`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    // Public utility functions
    access(all) view fun getValueLength(): Int {
        return self.value.length
```

#### 4635. Potential legacy function signature

- **File**: lib/vibesdk.ts
- **Location**: Line 593, Column 22
- **Pattern**: `fun isCounterEven(): B`
- **Type**: function-signature
- **Impact**: low
- **Suggested Fix**: Verify function uses modern Cadence 1.0 syntax

**Context:**
```cadence
    
    access(all) view fun isCounterEven(): Bool {
        return self.counter % 2 == 0
```


## Recommendations

### 🚨 Immediate Action Required

58 critical patterns must be fixed before deployment:

- **pub-keyword**: 44 instances
- **storage-api**: 14 instances

### ⚠️ High Priority

10 warning patterns should be addressed soon:

- **interface-conformance**: 9 instances
- **storage-api**: 1 instances

### 💡 Improvement Opportunities

4635 suggestion patterns could be improved when convenient:

- **pub-keyword**: 3226 instances
- **storage-api**: 489 instances
- **interface-conformance**: 188 instances
- **function-signature**: 717 instances
- **import-statement**: 15 instances

### Next Steps

1. **Fix Critical Issues First**: Address all critical patterns immediately
2. **Plan Warning Fixes**: Schedule time to fix warning patterns
3. **Consider Suggestions**: Evaluate suggestion patterns for future improvements
4. **Implement Prevention**: Add mechanisms to prevent legacy patterns from being reintroduced
5. **Re-scan Regularly**: Run this scanner periodically to catch any new legacy patterns
